# 《批量压缩解压工具》跨平台重写规格说明（可供 AI/程序员复刻）

> 本项目当前是 **C# .NET 8 WinForms（Windows-only）** 桌面程序，核心能力是：**批量压缩/批量解压**，实际压缩引擎依赖 **WinRAR.exe 命令行**。  
> 跨平台重写时，**UI 技术必须替换**（如 Avalonia等），可升级为.NET10
---

## 1. 项目入口与模块总览

## 1.1 入口点

- **入口文件**：[Program.cs](cci:7://file:///Users/x/code/trae/compress/Program.cs:0:0-0:0)
- **入口函数**：`static void Main()`
- **核心行为**
  - 检查当前用户是否管理员（UAC 提权）。
  - 管理员：`Application.Run(new Mainform())`
  - 非管理员：以 `runas` 方式重启自身并退出。

> **跨平台替代建议**
- macOS/Linux 不存在 WinForms + UAC 这一套：重写时将“权限需求”降级为“尽量不需要管理员”，或在需要权限时用平台能力（例如 macOS 提示授权访问某些目录、Linux 使用 polkit/提示 sudo）。

---

## 1.2 核心模块文件清单（按职责）

- **`批量压缩解压文件.cs`（Mainform）**
  - UI 事件处理 + 业务逻辑（压缩/解压/列表构建/密码算法/分卷识别/统计/托盘/热键）
- **`批量压缩解压文件.Designer.cs`**
  - WinForms 控件定义、布局、事件绑定（“每个按钮是什么/绑定到哪个函数”以此为准）
- **[API.cs](cci:7://file:///Users/x/code/trae/compress/API.cs:0:0-0:0)**
  - WinRAR 路径发现（注册表）+ 执行 WinRAR 进程 + 返回退出码
- **[md5.cs](cci:7://file:///Users/x/code/trae/compress/md5.cs:0:0-0:0)**
  - 多编码 MD5 截取算法（用于“随机密码/查询密码/高级功能解锁”）
- **[Win32Utility.cs](cci:7://file:///Users/x/code/trae/compress/Win32Utility.cs:0:0-0:0)**
  - Win32 `SendMessage` 设置 TextBox/ComboBox 的水印提示（Cue Banner）

---

## 2. 外部依赖与“对外接口”（跨平台必须替换/抽象）

## 2.1 外部依赖列表

- **WinRAR**
  - 依赖方式：执行 `winrar.exe`（通过注册表找到安装路径）
  - 主要命令：
    - 压缩：`A ... "output.rar" "input"`
    - 解压：`x ... "archive.rar" "outputDir"`
- **Windows 注册表**
  - 路径：`HKLM\SOFTWARE\Microsoft\Windows\CurrentVersion\App Paths\winrar.exe`
- **系统进程调用**
  - 打开目录：`explorer.exe <path>`
  - 关机：`c:\windows\system32\shutdown.exe -s` / 取消 `-a`
- **系统托盘**
  - `NotifyIcon`（提示文本、气泡通知、鼠标点击显示/隐藏）
- **剪贴板**
  - 启动时读取剪贴板文本（若是目录则填入 `tbSource`）
  - 查询密码后写入剪贴板

> **跨平台重写的最关键抽象点**  
- **压缩引擎接口**：`IArchiveEngine`
  - `Compress(inputs, output, options) -> ExitCode/Result`
  - `Extract(archive, outputDir, options) -> ExitCode/Result`
- **系统集成功能接口**：`ISystemIntegration`
  - `OpenFolder(path)`
  - `Shutdown()/CancelShutdown()`
  - `ReadClipboardText()/WriteClipboardText(text)`
  - `ShowTray()/UpdateTrayText()/Balloon(...)`

---

## 3. UI 控件/按钮/事件绑定（从 Designer 抽取，100% 可复刻）

> 下面每一项都给出：控件名、类型、UI 文案、事件处理函数、行为说明。  
> **跨平台重写时**：把“事件处理函数”作为 ViewModel/Controller 的同名用例（UseCase）实现即可。

## 3.1 顶部来源与目标区

- **`cbFrom`（ComboBox）**
  - **选项**：
    - index 0：`从txt读取要解压的文件：`
    - index 1：`压缩此文件夹内所有文件：`
  - **事件**：`CbFrom_SelectedIndexChanged`
  - **行为**：
    - index 0：把 `btnSource.Text` 改为 `选txt`
    - index 1：把 `btnSource.Text` 改为 `从哪儿来`

- **`btnSource`（Button，Text=来源/选txt/从哪儿来）**
  - **事件**：`ButtonFrom_Click`
  - **行为**：
    - 若 `cbFrom==1`：打开 FolderBrowserDialog，选源文件夹，写入 `tbSource`，调用 `AddFileToListFromPath()`
    - 若 `cbFrom==0`：打开 OpenFileDialog，选择 `.txt`，写入 `tbFileName`，调用 `AddFileToListFromTxt()`

- **`tbSource`（TextBox）**
  - **事件**：`TbSource_TextChanged`
  - **行为**：
    - 规范化路径 `\\ -> \`
    - 若 `cbFrom==0` 且 `tbSource` 是存在的 `.txt` 文件：自动触发 `btnRefresh.PerformClick()`
    - 若 `cbFrom==1` 且 `tbSource` 是存在目录：自动触发 `btnRefresh.PerformClick()`

- **`btnSavePath`（Button，Text=目的地）**
  - **事件**：`Btn2_Click`
  - **行为**：选择输出目录，写入 `tbSavePath`，并同步 `tbTmp=tbSavePath`

- **`tbSavePath`（TextBox）**
  - **事件**：`TbSavePath_TextChanged`
  - **行为**：
    - 规范化路径
    - 更新 `lOKsize = TotalSize(tbSavePath)`（显示输出目录内已有同扩展压缩包体积）
    - 同步 `tbTmp = tbSavePath`

- **`btnAsSource`（Button，Text=同上）**
  - **事件**：`btnAsSource_Click`
  - **行为**：调用 `btnAsSourceFN()`，将保存路径“按盘符/路径规则”跟随来源路径拼接（见 §6.11）

---

## 3.2 操作按钮区

- **`btnRun`（Button，Text=压缩）**
  - **事件**：`BtnRun_Click`（async）
  - **行为**：
    - 禁用 `btnRun` 与 `Btndepress`
    - 创建 `_cancellationTokenSource`
    - 创建 `_progressReporter = new Progress<CompressionProgressInfo>(ReportProgress)`
    - `Task.Run(() => Compression(token))`
    - finally 恢复按钮、释放 CTS

- **`Btndepress`（Button，Text=解压）**
  - **事件**：`Btndepress_Click`（async）
  - **行为**：与压缩同构，后台执行 `Decompression(token)`

- **`btnCancel`（Button，Text=中止）**
  - **事件**：`BtnCancel_Click`
  - **行为**：调用 `_cancellationTokenSource.Cancel()`，UI 显示“正在取消，等待当前文件解压完成...”
  - **注意**：它不会强杀 WinRAR 进程，只会在循环下一次检查 token 时退出

- **`btnRefresh`（Button，Text=更新）**
  - **事件**：`BtnrRefresh_Click`
  - **行为**：
    - `cbFrom==0`：`AddFileToListFromTxt()`
    - `cbFrom==1`：`AddFileToListFromPath()`
    - 调用 `TotalSize(tbSavePath)`刷新统计

- **`btnReset`（Button，Text=清空下面四个）**
  - **事件**：`Btnreset_Click`
  - **行为**：清空 `rtbSource/rtbOk/rtbCMD/rtbFail`

- **`btnClearAll`（Button，Text=清空↓）**
  - **事件**：`BtnClearAll_Click`
  - **行为**：清空 `rtbCMD`

- **`btnClearSource`（Button，Text=清空↓）**
  - **事件**：`BtnClearSource_Click`
  - **行为**：清空 `rtbSource`

- **`btnHide`（Button，Text=隐藏）**
  - **事件**：`BtnHide_Click`
  - **行为**：`Visible=false`（隐藏到托盘，配合 notifyIcon 恢复）

- **`btnSuccess`（Button，Text=成功的项目↓）**
  - **事件**：`Button2_Click`
  - **行为**：`explorer.exe` 打开 `tbSavePath`

- **`btnFail`（Button，Text=失败的项目↓）**
  - **事件**：`BtnFail_Click`
  - **行为**：若 `tbSource` 是目录则 `explorer.exe` 打开 `tbSource`

- **`btngetpw`（Button，Text=查询密码）**
  - **事件**：`Btngetpw_Click`
  - **行为**：根据 `tbFileName` 推导文件名并计算多套密码结果，写入 `rtbCMD`，最终密码写入 `tbgetpw` 并复制到剪贴板

- **`btnAddFile`（Button，Text=浏览）**
  - **事件**：`BtnAddFile_Click`
  - **行为**：选择一个目录路径并追加到 `enclosureList`

- **`btnZoom`（Button，Text=放大/缩小）**
  - **事件**：`BtnSize_Click`
  - **行为**：切换 Normal/Maximized 与字体大小

---

## 3.3 选项区（影响压缩/解压命令）

- **`exTension`（TextBox，默认 `rar`）**
  - **事件**：`exTension_TextChanged`
  - 作用：待处理的文件的后缀，如rar、7z、zip
  - **行为**：若输入 `7z`，弹窗提示“WinRAR 不支持压缩 7z，只能解压”，用户选择：
    - OK：改成 `rar`
    - Cancel：改成 `zip`
- **`cbExist`（ComboBox：跳过/添加并更新/替换）**
  - **压缩含义**：
    - index 0：跳过（`-o-`）
    - index 1：更新（`-u`）
    - index 2：覆盖（`-o+`，且若目标已存在会先 `File.Delete`删除已存在的文件）
  - **解压含义**：
    - index 2：覆盖（`-o+`）
    - 其它：默认 `-o-`
- **`Cbpw`（CheckBox：随机密码）**
  - **事件**：`Cbpw_CheckedChanged`
  - **行为**：
    - 勾选：隐藏 `tbPW/lpw/btnPW。（随机密码并非真的随机，而是根据文件名进行MD5计算得出固定密码）
    - 不勾选：显示这些控件（自定义密码）
- **`btnPW`（Button：确认/解锁高级功能）**
  - **事件**：`BtnPW_Click`
  - **两种模式**：
    - 若输入密码等于“解锁密码”：解锁高级功能（`tbNotice/exTension/tbVolume/enclosureList` 变为可编辑，且清空 enclosureList）
    - 否则：提示“密码设置成功”（实际就是普通确认）
- **`cbDivide + tbVolume + cbGMK`（分卷）**
  - 参数：`-v{tbVolume}{unit}`（g/m/k）
- **`cbRate`（压缩率）**
  - 构造：`raRate = "-m" + cbRate.SelectedIndex`
- **`CbSolid`（固实）**
  - **事件**：`CbSolid_CheckedChanged`
  - **限制**：当 `cbRate.SelectedIndex==0`（不压缩）且固实勾选时，提示并强制取消固实
  - 压缩命令片段：`solid = " -s -md32 -k "`
- **`cbQucikOpen`（快速打开）**
  - 参数：`-qo+`
- **`cbCheck`（校验）**
  - 参数：`-t`
- **`cbNotice + tbNotice`（注释文件）**
  - 仅`cbNotice.Checked` 并且 `File.Exists(tbNotice)` 都是true才启用
  - 参数：`-z"<noticeFilePath>"`
- **`tbTmp`（临时目录）**
  - 参数：`-w"<tmpDir>"`，并确保目录存在
- **`cbAdd + enclosureList`（添加附件/联系方式目录）**
  - 对“源为目录”时生效：会在每个待压缩目录下创建若干子目录（取 `enclosureList.Lines` 每行的文件名部分作为每个目录的名字）
- **`cbMoveSource` vs `CbDel`（互斥）**
  - `cbMoveSource_CheckedChanged`：强制 `CbDel=false`
  - `CbDel_CheckedChanged`：强制 `cbMoveSource=false`
- **`tbSize`（本次操作最大的大小，单位 GB）**
  - 压缩：若 `已压缩大小(GB) > tbSize`，中断循环并输出统计
  - 解压：若 `已解压大小(GB) > tbSize`，中断循环并输出统计
- **`cbYiYaSuo`（跳过已压缩/已解压）**
  - 压缩：若路径文件名包含 `【已压缩】`，跳过
  - 解压：若压缩包文件名包含 `【已解压】`，跳过
- **`cbShutdown`（完成后关机）
  - 完成后执行关机并弹窗允许取消

---

## 4. “API/函数/用例”全量说明（可直接用到新项目）

## 4.1 [API.cs](cci:7://file:///Users/x/code/trae/compress/API.cs:0:0-0:0)

- **`string RarPath()`**
  - **输入**：无
  - **输出**：WinRAR 安装目录（字符串）或 `null`
  - **依赖/副作用**：读取 Windows 注册表
  - **失败条件**：注册表键不存在返回 null

- **`int CompressByRar(string shellArguments)`**
  - **输入**：完整 WinRAR 命令参数字符串（不含 exe 路径）
  - **输出**：
    - WinRAR 退出码（0/1 成功；2-11 错误；255 用户中断）
    - `-2`：WinRAR 未安装或 exe 不存在
    - `-3`：执行异常（catch-all）
  - **副作用**：
    - 启动外部进程 `winrar.exe` 并阻塞等待退出（`WaitForExit`）
  
- **`Task<int> CompressByRarAsync(string shellArguments)`**
  - **输入/输出**：同上（只是 `Task.Run` 包一层）

---

## 4.2 [md5.cs](cci:7://file:///Users/x/code/trae/compress/md5.cs:0:0-0:0)（密码算法）

- **`MyMd5.MD5UTF878(string txt)`**
  - **算法**：
    - `MD5(UTF8(txt)) -> hex string`
    - 返回 `hex.Substring(7, 8)`（第 8 位开始取 8 位）
  - **用途**：随机密码核心片段、查询密码展示、解锁密码片段

- **`MyMd5.MD5UTF874(string txt)`**
  - 同上，但返回 `hex.Substring(7, 4)`

- **`MyMd5.MD5gb2312(string txt)`**
  - 使用 `gb2312` 编码（注册 `CodePagesEncodingProvider`），返回 `hex.Substring(7, 4)`
  - 用途：兼容历史密码/查询密码对比

### 业务层“随机密码”定义（核心规则）

- **压缩随机密码**（对 *压缩后的文件名*，包含扩展名）
  - `password = MD5UTF878(name + "592ptt1314") + MD5UTF878(name + "592pnn1314")`
  - 其中 `name` = `Path.GetFileName(oldFullName) + "." + exTension`

- **解压随机密码**
  - 非分卷：`name = archiveFileName`（如 `a.rar`）
  - 分卷第一卷：构造 `namePart = baseName + extension`（如 `a.rar`）
  - 同样套用上面的盐值拼接

---

## 4.3 `Mainform`（业务用例 + UI 事件）

### 4.3.1 初始化与状态

- **`Mainform_Load`**
  - 设置默认 UI 值、默认路径、默认选项
  - 从剪贴板读文本，若是目录则写入 `tbSource`
  - 初始化托盘提示、工具提示（ToolTip）
  - 初始化 `enclosureList` 默认内容（联系方式路径文本）
  - 注意：这里写死了 Windows 盘符路径（`I:\`、`H:\...`），跨平台必须改为“上次使用记录/用户选择”。

- **运行时状态字段**
  - `_cancellationTokenSource`：取消源
  - `_progressReporter`：进度回调
  - `CompressionProgressInfo`：跨线程报告结构（当前文件/成功失败/忽略/不存在/已处理大小/消息/是否错误）

---

### 4.3.2 压缩用例

- **入口按钮**：`BtnRun_Click`
- **核心函数**：`Compression(CancellationToken cancellationToken)`

#### `Compression()` 输入来源（来自 UI 状态）
- **待处理列表**：`rtbSource.Lines`（每行一个文件/目录路径）
- **输出目录**：`tbSavePath`
- **扩展名**：`exTension`（最终 `extension="."+exTension`）
- **存在处理**：`cbExist.SelectedIndex`
- **随机密码开关**：`Cbpw.Checked`
- **后处理**：
  - 删除源：`CbDel`
  - 移动源：`cbMoveSource`（移动到 `【已压缩】` 子目录）

#### 压缩命令模板（最重要的“API”）
最终构造的参数在代码中是：

`A -ep1 -IBCK {shouldSkip} -SCf "{saveRarFullName}" "{oldFullName}" {enclosure} -p"{password}" {notice} {volume} {raRate} -rr{rec} {quickOpen} {tmp} {check} {exception} {solid} {cite}`

- **固定片段说明**
  - `A`：添加（压缩）
  - `-ep1`：排除基础路径
  - `-IBCK`：后台运行
  - `-SCf`：UTF-8 文件名（防中文乱码）
  - `exception`：`-ms...` 指定“不压缩/直接存储”的扩展名列表
  - `cite`：`-oi:50000000` 大文件引用策略（50MB 以上引用）
  - `-p"{password}"`：密码带引号

#### 关键边界与错误处理
- 文件/目录不存在：计入 `nonFile`，写 `rtbFail`
- 目标压缩包已存在且选择“跳过”：计入 ignore 并 continue
- WinRAR 未安装：`API.CompressByRar` 返回 `-2`，弹窗并 break
- 压缩失败：写 `555，压缩失败了...` + `RarTips(exitCode)`
- 目标大小达成：若 `已压缩GB > tbSize` 则提前结束并输出摘要
- 完成后关机：勾选 `cbShutdown` 才执行

---

### 4.3.3 解压用例

- **入口按钮**：`Btndepress_Click`
- **核心函数**：`Decompression(CancellationToken cancellationToken)`

#### 解压的输入模式（很关键）
- `cbFrom.SelectedIndex` 决定“密码从哪里来”
  - **0（txt 模式）**：`rtbSource` 内容被 `AddFileToListFromTxt()` 构造成“文件路径行 + 下一行密码”交替
  - **1（文件夹模式）**：`rtbSource` 就是纯文件路径列表（无密码行）

> 当前代码里 `readPwFromNextLine = txtOrRtb == 0`，并且在循环里 `if (readPwFromNextLine) i++;` 取下一行作为密码。  
> 因此：**txt 模式下 `rtbSource` 必须严格遵守“文件行/密码行”交替格式**。

#### 分卷识别与处理
- 函数：`RarPart(FileInfo fileInfo, string extension)`
- 返回数组：
  - `arr[0]`：所有分卷文件大小总和（字符串的 long）
  - `arr[1..]`：每个分卷文件完整路径（从 part01/part001/part0001 起推）
- 解压循环里遇到 `.part` 文件：
  - 若不是第一卷（文件名不包含 `".part00..1" + extension` 形式）则跳过
  - 第一卷才执行解压

#### 解压命令模板
`-IBCK x {existFile} -p"{pw}" "{file}" "{savePath}"`

- `x`：解压并保留路径
- `existFile`：默认 `-o-`，覆盖时 `-o+`
- `-p"{password}"`：密码带引号

#### 解压成功后的后处理
- 移动源压缩包：移动到 `【已解压】` 目录（按分卷逐个移动）
- 删除源压缩包：分卷时应删除所有卷

#### 进度显示（NotifyIcon 气泡）
- 每个文件解压后计算：
  - 已解压 GB
  - 剩余 GB
  - 每文件速度 MB/s
- 通过 `notifyIcon.ShowBalloonTip` 展示

---

## 5. 非 UI 的“业务工具函数”清单

- **`ReportProgress(CompressionProgressInfo info)`**
  - 统一把后台状态写到 UI：
    - `lbCurrentFile`、`lokCount`、`lfail`、`lOKsize`
    - 错误写 `rtbFail`；成功写 `rtbOk`
    - 同时发托盘气泡

- **`TotalSize(string path) -> double`**
  - 统计 `path` 下所有扩展名包含 `exTension.Text` 的文件大小总和（GB）
  - 用于：
    - 压缩前后差值计算“本次已压缩体积”
    - 保存路径变化时刷新“当前目录已有压缩包大小”

- **`IsNum(string str) -> bool`**
  - 判断是否纯数字（ASCII 48-57），用于恢复记录输入校验

- **`RarTips(int exitCode) -> string`**
  - WinRAR 退出码映射（0 成功、1 警告、2 致命、11 密码错误等）

- **`btnAsSourceFN()`**
  - “同来源”拼路径规则：用 `tbFileName` 的内容当作盘符/前缀，替换来源路径的盘符部分
  - 这是非常 Windows 风格的路径拼接，跨平台建议删掉或改成“复制来源路径到目标路径”。

---

## 6. 跨平台重写建议（技术选型与落地结构）

## 6.1 UI 跨平台技术候选（按“易落地”排序）

- **Avalonia（C#）**首选
  - 优势：保留 C# 生态、UI 跨平台成熟、适合桌面工具
- **Electron（Node + Web UI）**
  - 优势：生态大；缺点：包体大

## 6.2 跨平台使用rar

- 要求“继续生成 RAR”：因为 WinRAR 不跨平台且注册表方案仅 Windows 可用，跨平台几乎只能继续走“外部可执行文件”路线。

---

## 6.3 Avalonia vs Electron 逐项对比表

| 控件/功能点/接口         | Avalonia（C#）                        | Electron（Node+Web）                | 备注/迁移建议 |
|-------------------------|---------------------------------------|--------------------------------------|--------------|
| 主窗体/多窗口           | 支持，API接近WinForms/WPF             | 支持，BrowserWindow                  | UI布局方式不同，Avalonia更易迁移 |
| 按钮/文本框/下拉框      | 全支持，控件丰富                       | 全支持，HTML控件                     | 事件绑定方式不同 |
| 富文本框                | 支持RichTextBox/第三方控件             | 需用contenteditable或第三方库        | 复杂文本建议用Avalonia |
| 托盘/气泡通知           | 支持NotifyIcon/TrayIcon               | 支持Tray/Notification                | Electron需主进程实现 |
| 系统热键                | 支持（GlobalHotKey等库）               | 支持（global-shortcut模块）           | 两者均可 |
| 剪贴板                  | 支持Clipboard API                      | 支持clipboard模块                    | 均可 |
| 文件/目录对话框         | 支持Open/SaveFolderDialog              | 支持dialog模块                       | 均可 |
| 进程调用（7z/rar等）    | Process.Start/异步进程，易于捕获输出   | child_process/spawn                  | Avalonia更接近原C#实现 |
| 注册表/系统API          | 支持（Windows平台）                    | 需用node-regedit等第三方              | 跨平台建议移除注册表依赖 |
| 关机/重启/系统命令      | 支持（Process+平台判断）                | 需用shell命令，权限需注意             | 均可，注意权限 |
| 路径/盘符处理           | Path/DriveInfo等API，跨平台兼容性好     | path模块，需注意分隔符                | 建议统一用相对路径 |
| 多线程/异步任务         | Task/async/await，线程池丰富            | Node主线程单线程，需worker/子进程      | 大批量任务Avalonia更优 |
| MVVM/数据绑定           | 原生支持，开发体验好                    | 需用第三方库（如mobx/vue/react）      | Avalonia更适合桌面MVVM |
| 业务算法/原C#代码复用    | 可100%复用，直接迁移                    | 需全部重写为JS/TS                     | 迁移成本差异极大 |
| UI美观/动画             | 支持动画，风格偏传统桌面                | Web动画丰富，UI现代                   | Electron更易做现代UI |
| 跨平台兼容性            | Windows/macOS/Linux，移动端有限         | Windows/macOS/Linux，Web端可选        | Electron可做Web版 |
| 包体体积                | 适中（几十MB）                          | 较大（>100MB）                        | Electron体积大 |
| 社区/生态               | 活跃，C#桌面开发者多                    | 极大，Web开发者多                     | 均有大量资料 |

**结论：**
- 需最大化复用C#业务、追求高效批量处理、系统集成能力强，优先选Avalonia。
- 追求Web/现代UI、团队JS/TS栈、需Web端，选Electron，但需重写全部业务。

---

- 要求“继续生成 RAR”：因为 WinRAR 不跨平台且注册表方案仅 Windows 可用，跨平台几乎只能继续走“外部可执行文件”路线。


---

## 7. 你在新项目里需要复刻的“用例列表”（可直接写成需求/任务拆分）

- **用例：从文件夹加载待处理列表**
  - 输入：sourceDir
  - 输出：paths[]
  - 规则：过滤 `desktop.ini/.tmp/.DS`，跳过包含 `【已压缩】/【已解压】`（如果开关开启）

- **用例：从 txt 加载待处理列表（文件名/密码）**
  - 输入：txtPath、sourceDir、extension
  - 输出：entries[]（包含 archivePath + password）
  - 规则：奇数行文件名、偶数行密码；支持分卷第一卷识别；若找不到文件则跳过并跳过其密码行

- **用例：批量压缩**
  - 输入：paths[]、options（密码/分卷/压缩率/固实/临时目录/存在策略/后处理/上限大小）
  - 输出：result（成功/失败/忽略/不存在/耗时/输出体积/错误列表）
  - 进度：currentFile + counters + size

- **用例：批量解压**
  - 输入：archives[]（可含密码）、options（覆盖策略/后处理/上限大小）
  - 输出：result（成功/失败/不存在/耗时/解压体积/错误列表）
  - 分卷：识别所有卷、仅对第一卷触发解压，但后处理可对全部卷操作

- **用例：查询密码**
  - 输入：fileBaseName + extension
  - 输出：多算法结果 + 最终算法结果（并复制到剪贴板）

- **用例：高级功能解锁**
  - 输入：当天 DayOfYear 计算出的动态口令
  - 输出：advancedEnabled=true（开放更多选项）

- **用例：托盘与通知**
  - 输入：进度/状态
  - 输出：托盘文本、气泡通知；点击切换显示/隐藏