# 当前解压逻辑

> 本文描述 2026-08-08 工作区中实际运行的解压逻辑。实现依据为
> `BatchOperationService`、`ArchiveVolumeResolver`、`ArchiveEngineRouter`、
> `RarArchiveEngine`、`SevenZipArchiveEngine`、命令行入口和 Avalonia ViewModel。
> 旧版的 `IsRarArchive`、`IsRarFormat`、`ResolveActualFirstVolume`、
> `GetAllVolumeFiles`、`IsVolumeSequenceContiguous` 等示例已经废止；当前调用方
> 只能使用 `ArchiveVolumeResolver.Resolve` 返回的统一结果。

## 1. 范围和结论

解压支持两类输入：单卷归档和当前程序明确识别的三类分卷。解压不创建归档，
不会根据文件内容猜测格式，文件名识别失败时才使用 `ArchiveOptions.ArchiveFormat`
兜底。

| 文件形式 | `ArchiveVolumeResolver` 类型 | 后端 |
| --- | --- | --- |
| `name.rar` | `ArchiveKind.Rar` | 官方 RAR/WinRAR `rar` |
| `name.part01.rar`、`name.part1.rar` | `ArchiveVolumeKind.RarPart` | `rar` |
| `name.7z` | `ArchiveKind.SevenZip` | 官方 7-Zip `7zz` |
| `name.7z.001`、`name.7z.1` | `ArchiveVolumeKind.SevenZipNumeric` | `7zz` |
| `name.zip` | `ArchiveKind.Zip` | `7zz` |
| `name.zip.001`、`name.zip.1` | `ArchiveVolumeKind.ZipNumeric` | `7zz` |
| `name.tar`、`name.tar.gz`、`name.iso` 等已识别格式 | `ArchiveKind.Other` | `7zz` |

当前不按本规则猜测旧式 RAR `.r00`、传统 ZIP `.z01` 或任意自定义分卷后缀。
若绕过批处理直接调用路由，并把 `ArchiveFormat` 设为非 `rar`，这类未知文件才会交给
7zz；它们不会得到本程序的三类分卷完整性保护。

解压总流程如下：

```text
收集输入
  -> 按选择的格式筛选（密码本条目另有文件名补后缀规则）
  -> ArchiveVolumeResolver.Resolve
  -> 检查大小写歧义、重复编号、首卷和缺号
  -> 折叠为一个逻辑归档任务并去重
  -> 选择条目密码、随机密码或自定义密码
  -> 按实际文件类型路由到 rar 或 7zz
  -> 创建输出目录并执行解压
  -> 记录 stdout/stderr 和返回码
  -> 成功后删除或移动单卷/整组分卷
  -> 累计归档输入大小，必要时停止或请求关机
```

## 2. 输入来源

### 2.1 命令行入口

无界面模式使用 `extract` 或 `decompress` 动词，也接受 `--decompress`/`-d`。
`--output` 是必填项；`--extension`/`--format` 必须是 `rar`、`7z` 或 `zip`。

| 参数 | 行为 |
| --- | --- |
| `--input PATH` | 文件按一个精确输入处理；目录扫描其直接文件，随后按所选扩展名筛选；可重复指定 |
| `--source PATH` | 文件按一个精确输入处理；目录使用 `LoadArchivesFromFolder` 扫描直接文件 |
| `--text-file FILE` | 读取“文件名/密码”交替的密码本；不会再把 `--source` 目录整体加入任务 |
| `--extension EXT` | 目录和精确文件的格式筛选；密码本缺少后缀时也是补充依据 |
| `--skip-processed` | 目录扫描时跳过名称含 `【已解压】` 的文件；默认开启 |
| `--no-skip-processed` | 关闭上述目录扫描过滤；执行阶段仍会使用当前选项判断已处理标记 |

多个 `--input` 最终按完整路径不区分大小写去重。输入路径必须存在，否则命令行
在启动批处理前以退出码 2 拒绝。密码本中某一条归档不存在只进入该条诊断，不会使
其它有效条目消失。

命令行收集的实际分支：

```text
有 --text-file
  -> 以 --source 目录（如果它确实是目录）作为相对文件名基准
  -> 否则以密码本所在目录作为基准
  -> 只使用密码本条目和显式 --input

没有 --text-file 且有 --source
  -> 文件：精确添加
  -> 目录：扫描直接文件并按 --extension 折叠分卷

所有 --input
  -> 文件：匹配 --extension 后添加
  -> 目录：扫描直接文件并按 --extension 折叠分卷
```

### 2.2 GUI 来源模式

GUI 的 `SourceMode` 有三个值：

| 值 | 来源 | 解压时的行为 |
| --- | --- | --- |
| `0` | 解压密码本 TXT | TXT 先解析为交替的路径和密码；执行时每个条目仍会再次统一解析 |
| `1` | 压缩路径清单 TXT | 解压时也可作为“无密码路径清单”使用；只读取路径，不读取交替密码 |
| `2` | 来源目录 | 列表加载使用兼容的压缩来源扫描（直接文件和目录都显示），点击解压时再只保留存在且匹配所选格式的文件 |

GUI 的目录列表本身可能显示目录、普通文件和不同格式文件；这不是解压任务列表。
`DecompressAsync` 执行前会按 `File.Exists` 和
`ArchiveVolumeResolver.MatchesFormat(path, Extension)` 再过滤。密码本模式则按
导入时生成的路径/密码交替文本重新构造 `FileEntry`。

### 2.3 系统元数据过滤

所有入口都会调用 `SystemMetadataFileFilter.ShouldSkip`。它检查路径的每一个片段，
会过滤 `desktop.ini`、`Thumbs.db`、`.DS_Store`、`.directory`、`.Trash-*`、
`.nfs*`、`._*`、`~$*`、锁文件目录等。被过滤的条目直接忽略，不进入引擎；
此类忽略不会增加 `IncompleteVolumeCount` 或 `NonExistCount`。

## 3. 格式识别和目录筛选

### 3.1 `DetectArchiveKind`

识别顺序是先匹配三种分卷模式，再读取普通最后一级扩展名：

```text
^(?<base>.+)\.part(?<number>\d+)\.rar$  -> RAR part
^(?<base>.+)\.7z\.(?<number>\d+)$         -> 7z 数字分卷
^(?<base>.+)\.zip\.(?<number>\d+)$        -> ZIP 数字分卷
```

三个正则都使用忽略大小写和不变区域设置。普通扩展名转为小写后按下表处理：

```text
rar                                      -> Rar
7z                                       -> SevenZip
zip                                      -> Zip
tar, gz, gzip, tgz, bz2, bzip2, tbz2,
xz, txz, iso, cab, lzh, lha, arj, z,
lzma, cpio, rpm, deb, dmg, wim          -> Other
其它或无扩展名                           -> Unknown
```

识别只看文件名，不读取文件头。一个名为 `fake.7z` 的普通文本仍会被路由到 7zz，
最终由 7zz 返回格式错误。

### 3.2 `MatchesFormat`

目录扫描和精确文件输入使用 `DetectArchiveExtension` 的结果与 GUI/CLI 扩展名
比较，比较不区分大小写：

| 所选扩展名 | 会匹配 | 不会匹配 |
| --- | --- | --- |
| `rar` | `a.rar`、`a.part01.rar` | `a.r00`、`a.7z` |
| `7z` | `a.7z`、`a.7z.001` | `a.7z001`、`a.zip.001` |
| `zip` | `a.zip`、`a.zip.001` | `a.z01`、`a.7z.001` |
| 其它（直接服务调用） | 最后一级扩展名完全相等，例如 `a.tar.gz` 对 `gz` | 不识别该扩展名的数字分卷 |

所以 `a.tar.gz` 的可匹配扩展名是 `gz`，不是 `tar`；`a.tgz` 的扩展名是
`tgz`。CLI 目前只允许 `rar`、`7z`、`zip`，其他扩展名只能通过直接
调用服务或手工编辑 GUI 列表进入路由。

### 3.3 目录扫描的分卷折叠

`LoadArchivesFromFolder(folder, extension, skipProcessed)` 只枚举目录的直接文件：

1. 过滤系统元数据、名称以 `.tmp` 开头的文件，以及（启用选项时）含 `【已解压】`
   的名称。
2. 用 `MatchesFormat` 过滤所选格式。
3. 对每个匹配文件调用 `Resolve`。
4. 取 `FirstVolumePath`；若没有首卷则取现有卷中排序最小的一卷作为诊断入口。
5. 按任务入口完整路径不区分大小写去重。

因此完整的 `a.7z.001`、`a.7z.002`、`a.7z.003` 只返回一个任务入口
`a.7z.001`，而不是三个任务。缺少 `.001` 时扫描仍可能返回 `a.7z.002` 作为
入口，但下一阶段会因缺少首卷而跳过，不会把 `.002` 交给引擎。

`LoadFilesFromFolder(folder, extension, skipProcessed)` 目前只是压缩模式兼容别名，
等价于 `LoadCompressionSourcesFromFolder(folder, skipProcessed)`，不执行解压格式
过滤。不要再依据旧文档给它增加 `isForDecompression` 参数。

## 4. 统一分卷解析器

### 4.1 `Resolve(path)` 的结果

`ArchiveVolumeResolveResult` 是解压入口、完整性检查和后处理文件集合的唯一来源，
主要字段如下：

| 字段 | 含义 |
| --- | --- |
| `ArchiveKind` | `Rar`、`SevenZip`、`Zip`、`Other` 或 `Unknown` |
| `VolumeKind` | `None`、`RarPart`、`SevenZipNumeric`、`ZipNumeric` |
| `ActualExtension` | 真实逻辑扩展名；数字分卷不会错误地变成 `001` |
| `LogicalArchiveName` | 单卷实际名，或分卷组还原后的 `name.rar`/`name.7z`/`name.zip` |
| `FirstVolumePath` | 单卷自身，或编号为 1 的实际首卷；缺失时为 `null` |
| `Volumes` | 同一目录、同一基础名的所有匹配卷，按编号数值排序 |
| `MissingNumbers` | 从 1 到最大编号之间缺少的编号，最多记录前 1000 个 |
| `HasDuplicateNumbers` | 同一编号出现多个文件，例如 `.1` 与 `.001` 同时存在 |
| `HasCaseAmbiguity` | 请求名不存在时，不区分大小写找到多个候选 |
| `FilesForPostProcessing` | 单卷为自身；分卷为完整卷组 |
| `CanExtract` | 存在首卷、无歧义、无重复编号且没有缺号 |

### 4.2 解析步骤

1. 将请求路径转为绝对路径，取得目录和文件名。
2. 精确路径存在时优先使用它。若精确路径不存在，在同目录按文件名不区分大小写
   搜索：找到一个候选可继续，找到多个设置 `HasCaseAmbiguity`。
3. 现有路径是普通单卷时立即返回单卷结果；现有路径是三类分卷之一时按其基础名
   扫描整组。
4. 请求的逻辑名不存在时，`name.rar`、`name.7z`、`name.zip` 会分别推断
   `name.partNN.rar`、`name.7z.NNN`、`name.zip.NNN`，以支持密码本只写逻辑名。
5. 收集所有匹配卷，按编号数值排序并计算重复编号、首卷和缺号。

编号检查从 1 开始，而不是从目录中最小编号开始：

```text
[1, 2, 3]       -> HasRequiredFirstVolume=true, MissingNumbers=[]     -> 可解压
[1, 3]          -> HasRequiredFirstVolume=true, MissingNumbers=[2]     -> 跳过
[2, 3]          -> HasRequiredFirstVolume=false, MissingNumbers=[1]    -> 跳过
[1, 1, 2]       -> HasDuplicateNumbers=true                            -> 跳过
```

这意味着“`002`、`003` 数字上连续”仍然不能解压；程序不会把 `.002` 当作首卷。
前导零只影响文件名，不影响编号值，`part1` 和 `part01` 会被视为同一个编号并产生
重复编号歧义。

### 4.3 入口示例

假设 `/data` 中有以下文件：

```text
album.part01.rar
album.part02.rar
album.part03.rar
movie.7z.001
movie.7z.002
broken.zip.002
broken.zip.003
```

| 请求路径 | `FirstVolumePath` | `Volumes` | 结果 |
| --- | --- | --- | --- |
| `album.part02.rar` | `album.part01.rar` | 1、2、3 | 重定向到首卷，使用 RAR |
| `album.rar` | `album.part01.rar` | 1、2、3 | 逻辑名推断分卷组，使用 RAR |
| `movie.7z` | `movie.7z.001` | 1、2 | 使用 7zz |
| `movie.7z.002` | `movie.7z.001` | 1、2 | 非首卷输入只执行一次 |
| `broken.zip.003` | `null` | 2、3 | 缺少 1，记录不完整并跳过 |

## 5. 密码本 TXT

### 5.1 文件格式

密码本是去除空白行后按两行一组的文本：

```text
report
secret-1
backup.7z.001
secret-2
missing.rar
secret-3
```

每组第一行是文件名或路径，第二行是该归档密码。最后只有一行时，密码为 `null`。
文件名不区分大小写地命中以下任意格式时原样使用：普通已识别扩展名或三类分卷
模式；否则追加 GUI/CLI 当前扩展名。例如选 `rar` 时：

```text
report              -> report.rar
backup.7z.001      -> backup.7z.001
/data/a.part02.rar -> /data/a.part02.rar
```

相对路径相对于密码本基准目录解析；绝对路径直接规范化。密码本基准目录在 CLI 中
是 `--source`（确实为目录时）或密码本所在目录，在 GUI 中是保存目录字段。

### 5.2 导入诊断和计数

`LoadFilesFromTextFileWithDiagnostics` 对每一组都会先递增 `RequestedCount`，无论之后
匹配成功、缺失、缺卷还是歧义。它不会把失败条目从请求数中扣除。

处理顺序：

1. 大小写歧义 -> 加入 `AmbiguousEntries`，跳过。
2. 重复分卷编号 -> 加入 `DuplicateVolumeEntries`，跳过。
3. 分卷缺首卷或缺中间卷 -> 加入 `IncompleteVolumes`，跳过。
4. 非分卷条目没有实际文件、命中系统元数据或文件不存在 -> 加入 `MissingEntries`，跳过；
   分卷缺首卷已经在上一步归入 `IncompleteVolumes`。
5. 有效条目去重：同一实际首卷重复出现时只保留第一条密码。
6. `FileSize` 和 `MatchedBytes` 对分卷组累加全部卷大小，对单卷计算单文件大小。

导入结果还会扫描基准目录，列出所选扩展名下未出现在密码本中的：

- `UnmatchedArchives`：未匹配的单卷归档；
- `VolumeCandidates`：未匹配但看起来属于分卷组的文件。

这些诊断不会自动加入解压任务。GUI 会把请求数、已匹配数、大小、缺失、缺卷和
歧义写入命令日志；CLI 只使用有效 `Entries` 进入批处理。

## 6. 解压后端路由

`ArchiveEngineRouter.ExtractAsync` 的优先级是“实际文件名优先，选项兜底”：

```text
DetectArchiveKind(path) == Rar                  -> RarArchiveEngine
DetectArchiveKind(path) != Unknown              -> SevenZipArchiveEngine
DetectArchiveKind(path) == Unknown
  且 ArchiveFormat 规范化为 rar                -> RarArchiveEngine
DetectArchiveKind(path) == Unknown
  且 ArchiveFormat 不是 rar                     -> SevenZipArchiveEngine
```

因此实际文件类型不会被矛盾的 `ArchiveFormat` 覆盖：

| 文件名 | `ArchiveFormat` | 后端 |
| --- | --- | --- |
| `a.rar` | `7z` | RAR |
| `a.part01.rar` | `zip` | RAR |
| `a.7z` | `rar` | 7zz |
| `a.zip.001` | `rar` | 7zz |
| `a.tar.gz` | `rar` | 7zz |
| `a.unknown` | `rar` | RAR（兜底） |
| `a.unknown` | `7z` | 7zz（兜底） |

`BatchDecompressAsync` 已解析出实际扩展名时，会把该值写入 `ArchiveOptions.ArchiveFormat`；
未知文件才保留批处理选项中的扩展名。RAR 引擎和 7zz 引擎都保留原始 stdout/stderr，
返回码 `0` 和 `1` 被视为成功，其他返回码为失败。

## 7. 单任务执行顺序

`BatchDecompressAsync` 对每个 `FileEntry` 串行执行：

1. 检查取消令牌；取消会抛出 `OperationCanceledException`，不继续后续任务。
2. 再次过滤系统元数据路径。
3. 调用 `ArchiveVolumeResolver.Resolve`。
4. 依次拒绝大小写歧义、重复编号和不完整分卷，并更新相应计数。
5. 要求 `FirstVolumePath` 存在；不存在时增加 `NonExistCount`。
6. 用实际首卷绝对路径去重；同一逻辑归档重复输入只执行一次并增加 `IgnoreCount`。
7. 若启用 `SkipAlreadyProcessed` 且首卷名称含 `【已解压】`，增加 `IgnoreCount` 并跳过。
8. 选择密码：
   - `FileEntry.Password` 非空时优先；
   - 否则启用随机密码时，以 `LogicalArchiveName` 派生默认密码；
   - 否则使用 `CustomPassword`；
   - 三者都没有则无密码。
9. 计算输出目录：配置了 `OutputPath` 就创建并使用该目录；为空时回退到归档首卷的
   父目录。
10. 调用路由后的 RAR 或 7zz 解压引擎，报告命令 stdout/stderr。
11. 引擎成功才执行删除/移动后处理。
12. 以归档输入大小累计 `ProcessedSizeGB`，检查大小上限并报告最终状态。

### 7.1 密码派生

随机密码沿用压缩侧的兼容算法。分卷会先还原逻辑名，所以同一组的所有卷使用同一
密码：

```text
album.part02.rar -> 密码依据 album.rar
movie.7z.002     -> 密码依据 movie.7z
```

`PasswordNameMode.ArchiveName` 使用带扩展名的逻辑名，`BaseName` 去掉最后一级扩展名。
算法为 UTF-8 MD5 十六进制摘要从索引 7 截取 8 位，两段分别追加固定盐：

```text
h1 = MD5_UTF8(name + "592ptt1314")[7..14]
h2 = MD5_UTF8(name + "592pnn1314")[7..14]
password = h1 + h2
```

这是旧归档兼容算法，不是新的密码学密钥派生。显式密码来源由 CLI 解析器限制为
`--password`、`--password-file`、`--password-stdin` 三选一；密码文件和标准输入
只读取第一行，并关闭随机密码派生。

### 7.2 实际命令参数

RAR 解压使用：

```text
rar x -IBCK [ -o- | -u | -o+ ] [ -p<password> ] <archivePath> <outputDirectory>
```

7zz 解压使用：

```text
7zz x [ -aos | -aou | -aoa ] [ -p<password> ] -o<outputDirectory> <archivePath>
```

路径和密码通过 `ProcessStartInfo.ArgumentList` 逐项传递；空格、中文和 `!@#$` 等
字符不会被 Shell 再次拆分。原始进程输出仍可能包含密码，日志必须按敏感数据处理。

## 8. 已有输出和后处理

### 8.1 解压目标文件策略

`ExistingFileMode` 只影响解压目标中已经存在的文件，不会在服务层预先删除整个
输出目录：

| 模式 | RAR 参数 | 7zz 参数 |
| --- | --- | --- |
| `Skip` | `-o-` | `-aos` |
| `Update` | `-u` | `-aou` |
| `Overwrite` | `-o+` | `-aoa` |

CLI 默认值是 `overwrite`，GUI 默认选择也是覆盖；直接构造 `ArchiveOptions` 时应
明确设置所需枚举值。

### 8.2 删除和移动源归档

只有引擎返回成功才会处理源归档。单卷处理一个文件，分卷处理
`FilesForPostProcessing` 中的全部卷：

```text
/data/album.part01.rar
/data/album.part02.rar
       --move-source
/data/【已解压】/album.part01.rar
/data/【已解压】/album.part02.rar
```

删除模式逐卷调用 `File.Delete`；删除失败会留下已经删掉的前序卷，并增加
`PostProcessFailCount`。移动模式先检查整组目标，只要任意目标已存在，就保留整组
源卷，不执行部分移动并增加一次后处理失败；没有预冲突时，单卷移动异常仍逐项计数。
移动从不覆盖目标。

解压成功但后处理失败仍计入 `SuccessCount`，最终消息为“成功但后处理失败”；归档
本身不会因为后处理失败而被算入 `FailCount`。

## 9. 统计、大小上限、取消和关机

### 9.1 进度字段

| 字段 | 增加时机 |
| --- | --- |
| `SuccessCount` | 引擎返回成功，后处理是否成功不影响 |
| `FailCount` | 引擎返回失败，或启动/构建命令时发生异常 |
| `PostProcessFailCount` | 删除/移动失败、移动目标冲突 |
| `IgnoreCount` | 重复任务、已处理标记、歧义或不完整分卷等前置跳过 |
| `NonExistCount` | 解析后没有实际首卷，或解析发生不可用路径错误 |
| `IncompleteVolumeCount` | 缺首卷、缺中间卷 |
| `AmbiguousArchiveCount` | 大小写歧义或重复编号 |
| `ProcessedSizeGB` | 成功解压后，以单卷或整组分卷源文件总大小累计 |

系统元数据过滤和某些手工列表过滤是静默忽略，不一定增加 `IgnoreCount`；命令行
摘要应以最终快照为准。

### 9.2 大小上限

解压侧的 `MaxSizeGB` 统计的是成功解压归档的输入大小，不是输出目录大小：

```text
单卷：FileInfo(firstVolume).Length
分卷：FilesForPostProcessing 中所有卷的长度之和
```

任务成功并累计后，如果 `MaxSizeGB > 0` 且达到上限，停止后续任务；`0` 表示不限。
上限只在当前任务完成后检查，不会中断正在运行的归档进程。

### 9.3 取消和完成后关机

GUI 的取消按钮和 CLI 的 Ctrl+C 都取消同一个 `CancellationToken`。取消会停止当前
批处理并返回取消路径，不执行“正常完成后关机”。如果所有任务遍历结束且
`ShutdownAfterComplete=true`，即使其中有归档失败，也会调用系统集成的关机请求。
CLI 真实退出码如下：

```text
0    有成功或仅有可接受的跳过
1    归档失败、后处理失败、分卷不完整、歧义或没有可处理条目
2    命令行参数/来源校验失败
130  Ctrl+C 或取消
```

## 10. 完整示例

### 示例 A：目录扫描一个 7z 分卷组

```text
/work/
├── movie.7z.001
├── movie.7z.002
├── movie.7z.003
├── photo.zip
└── readme.txt
```

执行：

```bash
BatchCompress.Avalonia extract \
  --source /work \
  --output /work/extracted \
  --format 7z \
  --no-random-password
```

扫描只选择 `movie.7z.001`、`.002`、`.003`，解析后折叠为一个任务并调用一次
`7zz x ... movie.7z.001`；`photo.zip` 和 `readme.txt` 不进入任务。

### 示例 B：密码本使用逻辑名

`passwords.txt`：

```text
movie.7z
fixture-password
album
another-password
```

当目录中实际是 `movie.7z.001`、`movie.7z.002` 和 `album.part01.rar`、
`album.part02.rar` 时，第一条通过 `movie.7z` 推断数字分卷，第二条通过 `album`+
当前选择的扩展名补出 `album.rar` 后再推断 RAR part；两组分别只执行一次，密码
来自密码本，不使用随机派生。

### 示例 C：缺少首卷或中间卷

```text
broken.7z.002       # 存在
broken.7z.003       # 存在
```

`Resolve` 返回 `FirstVolumePath=null`、`MissingNumbers=[1]`，批处理增加
`IncompleteVolumeCount` 和 `IgnoreCount`，不调用 7zz，不移动、不删除任何卷。

```text
gap.7z.001          # 存在
gap.7z.003          # 存在
```

此时首卷存在但 `MissingNumbers=[2]`，同样在引擎前跳过。

### 示例 D：格式名和选项冲突

```text
archive.zip.001 + --format rar  -> DetectArchiveKind=Zip -> 7zz
archive.rar     + --format 7z   -> DetectArchiveKind=Rar -> rar
no-extension    + --format rar  -> Unknown -> rar（兜底）
```

选项不能强制把一个已识别的 ZIP 送入 RAR，也不能把已识别的 RAR 改送 7zz。

### 示例 E：移动分卷时目标冲突

```text
/data/archive.7z.001
/data/archive.7z.002
/data/【已解压】/archive.7z.002    # 已存在
```

解压成功后移动前先检查整组目标，发现冲突就保留 `.001` 和 `.002` 原卷，目标文件
也保留，`SuccessCount=1`、`PostProcessFailCount=1`，不会出现只移动一卷的半组状态。

## 11. 已知边界和排查表

| 现象 | 实际原因 |
| --- | --- |
| 目录中一个分卷组显示多个任务 | 检查是否使用了 GUI 目录列表；GUI 先显示直接子项，执行时通过首卷和 `processedArchives` 去重；CLI 使用 `LoadArchivesFromFolder` 从源头折叠 |
| `.002` 被当成首卷 | 当前实现不会这样做；若发生，检查是否绕过 `BatchOperationService` 直接调用引擎 |
| 缺少 `.001` 仍然尝试解压 | 检查调用方是否绕过 `ArchiveVolumeResolver.Resolve` 或自行实现旧正则 |
| `a.tar.gz` 选 `tar` 扫不到 | `MatchesFormat` 比较最后一级扩展名，实际扩展名是 `gz` |
| `.r00` 或 `.z01` 没有整组处理 | 不属于当前三类分卷规则 |
| `a.7z` 和 `a.7Z` 同时存在 | 精确路径优先；不区分大小写的请求可能产生歧义，歧义会跳过以避免选错文件 |
| 归档成功但退出码为 1 | 可能是后处理失败、分卷不完整、歧义或只有失败/跳过任务；查看摘要和日志 |
| 解压后源卷只删了一部分 | 删除模式逐卷执行，后续卷失败时不会恢复已删除卷；移动模式有整组冲突预检查 |
| 错误密码没有尝试随机密码 | 密码本非空密码优先；显式密码源也会关闭随机密码 |
| 未识别后缀走错后端 | 未知名只按 `ArchiveFormat` 兜底；已识别格式永远由实际文件名决定 |

## 12. 相关实现和验证

| 文件 | 职责 |
| --- | --- |
| `Core/Services/ArchiveVolumeResolver.cs` | 文件类型、三类分卷、首卷、缺号、歧义和后处理集合 |
| `Core/Services/BatchOperationService.cs` | 输入扫描、密码本、批量解压、进度、大小上限和后处理 |
| `Core/Services/ArchiveEngineRouter.cs` | 实际归档类型到 RAR/7zz 的路由 |
| `Core/Services/WinRarCommandBuilder.cs` | RAR 解压参数和已有目标策略 |
| `Core/Services/SevenZipCommandBuilder.cs` | 7zz 解压参数和已有目标策略 |
| `HeadlessBatchRunner.cs` | CLI 输入收集、密码来源、退出码和日志 |
| `ViewModels/MainWindowViewModel.cs` | GUI 来源模式、列表编辑、密码本导入和操作入口 |
| `Core/Services/PasswordUtility.cs` | 默认密码和旧版候选密码 |

验证命令：

```bash
dotnet build BatchCompress.Avalonia.sln --nologo
dotnet run --project BatchCompress.Avalonia.Tests/BatchCompress.Avalonia.Tests.csproj --nologo
```

轻量测试覆盖路由、三类分卷、缺首卷/缺中间卷、重复编号、目录扫描折叠、密码本
诊断、后处理冲突和官方 macOS 7zz 单卷压缩解压。真实 GUI 点击和真实系统关机不
属于该无 UI 测试程序的验证范围。
