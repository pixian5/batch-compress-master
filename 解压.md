# 解压方案

## 一、核心原则

### 通用原则（与压缩共用）

1. **归档后缀识别**：`HasArchiveExtension` 维护常见单卷归档后缀列表（rar/7z/zip/tar/gz/bz2/xz/iso/cab/lzh/arj/tgz/tbz2/txz 等）**并额外匹配三种分卷模式**（`*.partNN.rar`、`*.7z.NNN`、`*.zip.NNN`）。密码本写文件名命中单卷后缀或分卷模式之一则原样使用，否则补 GUI 扩展名。
2. **大小写**：Linux 下文件系统大小写敏感，密码本写 `a.rar`、实际文件 `A.RAR` 必须能匹配。

### 解压模式

3. **解压路由**：只有 `rar` 走 RAR 后端，其它所有后缀（`7z`/`zip`/`tar`/`gz`/`bz2`/`xz`/`iso` 等）都走 7zz。
4. **解压目录扫描**：只采集匹配 GUI 扩展名的文件（含其分卷格式）。
5. **密码本文件名处理**：密码本写文件名不含归档后缀时，补 GUI 扩展名。
6. **分卷识别**：按文件实际后缀分派，不依赖 GUI 扩展名。只有 `rar`/`7z`/`zip` 三种格式有分卷，其它后缀视为单卷。
7. **分卷中间缺失**：跳过该项目，不解压、不移动、不删除，写入日志。

---

## 二、解压改动位置

### 1. [ArchiveEngineRouter.cs 第42-65行](file:///Users/x/code/Google-compress/Core/Services/ArchiveEngineRouter.cs#L42-L65)（解压路由）

**解压路由反转**：从"7z/zip 走 7zz，其它走 RAR"改为"rar 走 RAR，其它走 7zz"。

```
当前解压路由（第49-51行）：
  return IsSevenZipArchive(archivePath) || IsSevenZipFormat(options.ArchiveFormat)
      ? _sevenZipEngine.ExtractAsync(...)
      : _rarEngine.ExtractAsync(...);

改为：
  return IsRarArchive(archivePath) || IsRarFormat(options.ArchiveFormat)
      ? _rarEngine.ExtractAsync(...)
      : _sevenZipEngine.ExtractAsync(...);

新增判定函数：
  IsRarFormat(string? format):
    format?.Trim().TrimStart('.').ToLowerInvariant() == "rar"

  IsRarArchive(string archivePath):
    var fileName = Path.GetFileName(archivePath);
    return fileName.EndsWith(".rar", OrdinalIgnoreCase) ||
           Regex.IsMatch(fileName, @"\.part\d+\.rar$", IgnoreCase);
```

效果：
- `a.rar`、`a.part01.rar` → 走 RAR ✓
- `a.7z`、`a.zip`、`a.7z.001`、`a.zip.001` → 走 7zz ✓
- `a.tar`、`a.gz`、`a.iso`、`a.cab` 等 → 走 7zz ✓（不再误走 RAR）

原 `IsSevenZipArchive` 和 `IsSevenZipFormat` 保留给压缩路由使用。

##### 解压路由：全量分派示例（22 条，覆盖 IsRarArchive 命中/未命中 + IsRarFormat 命中/未命中）

> 两个 OR 条件任一个命中即走 RAR：`IsRarArchive(archivePath) || IsRarFormat(options.ArchiveFormat)`；
> 否则走 7zz。

| # | archivePath（文件名） | options.ArchiveFormat | IsRarArchive 返回 | IsRarFormat 返回 | OR 结果 | 走哪条 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `a.rar` | `""`（未传） | true（EndsWith `.rar`） | false | true | RAR ✓ | 标准单卷 RAR |
| 2 | `A.RAR` | `""` | true（OrdinalIgnoreCase） | false | true | RAR ✓ | 大写单卷 RAR |
| 3 | `a.part01.rar` | `""` | true（匹配 `\.part\d+\.rar$`） | false | true | RAR ✓ | RAR part 首卷 |
| 4 | `a.part999.rar`（非首卷） | `""` | true | false | true | RAR ✓ | 非首卷也走 RAR（首卷过滤在上游 Batch） |
| 5 | `a.PART02.RAR`（全大写 part） | `""` | true（IgnoreCase 正则） | false | true | RAR ✓ | |
| 6 | `a.rar1`（后缀不是 .rar，是 .rar1） | `""` | 既不 EndsWith `.rar` 也不含 `part\d+\.rar$` | false | false | 7zz ✗ | 不匹配 rar 识别 → 走 7zz（7zz 对未知后缀会尝试识别 header；错误就让它报错日志） |
| 7 | `a.part.rar`（part 后没数字） | `""` | EndsWith `.rar` → true | false | true | RAR ✓ | 文件名有 `.part.rar` 但 part 后无数字 → EndsWith `.rar` 仍命中 → 仍走 RAR。实际解压后 RAR 引擎会报"不是分卷/不匹配" → 正常错误流 |
| 8 | `a.7z` | `""` | false | false | false | 7zz ✓ | 7z 单卷 |
| 9 | `a.7z.001` | `""` | false（EndsWith `.001` 不是 .rar；正则也不命中） | false | false | 7zz ✓ | 7z 数字分卷首卷 |
| 10 | `a.zip.001` | `""` | false | false | false | 7zz ✓ | ZIP 数字分卷首卷 |
| 11 | `a.zip` | `""` | false | false | false | 7zz ✓ | ZIP 单卷 |
| 12 | `a.tar` | `""` | false | false | false | 7zz ✓ | tar |
| 13 | `a.tar.gz` | `""` | false | false | false | 7zz ✓ | tar.gz（Path.GetFileName 结尾非 .rar） |
| 14 | `a.gz` | `""` | false | false | false | 7zz ✓ | gzip |
| 15 | `a.iso` | `""` | false | false | false | 7zz ✓ | ISO |
| 16 | `a.cab` | `""` | false | false | false | 7zz ✓ | CAB |
| 17 | `a.txt`（误拖了非归档） | `""` | false | false | false | 7zz ✓（7zz 会报 header error） | 非归档文件；让 7zz 报 header 错并写入日志 |
| 18 | `a.7z`（**传了 ArchiveFormat = `rar`** 矛盾场景） | `rar` | false（名是 7z → IsRarArchive=false） | true（IsRarFormat：去点 ToLower = rar） | true | **RAR**（Format 参数优先于名字） | 矛盾 case：用户显式指定 format=rar → 按用户指定走 RAR；RAR 引擎识别失败则报错 |
| 19 | `a.rar`（传了 `7z` 格式 override） | `7z` | true（文件名 RAR） | false（format 7z） | true（OR 仍真，IsRarArchive=true） | **RAR**（名字命中优先） | 注意是 OR 不是 AND！只要名字命中 RAR 就走 RAR，与"format 传 7z 就强制走 7zz"的设计意图是否一致？排查点：如果需要 format 参数覆盖名字，应改为 `IsRarFormat ?? IsRarArchive`（有 format 用 format，没有再用名字）。当前 OR 设计导致 format=7z 对 rar 名文件无效。**文档先把这 case 记录为设计决策点，再决定实现** |
| 20 | `a.tar`（传了 `rar` format） | `rar` | false | true | true | **RAR**（format=rar 覆盖后缀 tar） | 显式 format 覆盖 |
| 21 | `a.part01.rar`（**传了 `zip` format**） | `zip` | true（name 命中 rar part） | false | true | **RAR** | 同 #19，format=zip 对 rar 名字无效（OR 设计） |
| 22 | `archive`（无后缀） + 传了 `rar` format | `rar` | false（无 `.rar` 尾） | true | true | RAR（显式指定 rar） | 无后缀但 format 指定 → 走 RAR |

> 排查提示：
> - "zip 文件怎么被 RAR 引擎打开报错了？" → 反查 #19/#21 模式：是否 ArchiveFormat 参数被传成 rar？或者 IsRarArchive 误把 zip 名匹配（正则漏了 IgnoreCase 外的 `\.part\d+\.rar$` 写漏了 rar 尾？比如错写 `\.part\d+\..*$` 就会误匹配一切 part 尾）
> - "rar 文件怎么跑到 7zz 去了？" → 反查 #6/#7（文件名后缀不规范导致 IsRarArchive=false 且 format 也没传 rar）

### 2. [BatchOperationService.cs 第40-89行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L40-L89)（解压目录扫描）

**解压模式按 GUI 扩展名过滤**：函数签名加 `bool isForDecompression` 参数，解压模式传 `true`。

```
签名变更：LoadFilesFromFolder(string folder, string extension, bool isForDecompression)

对每个直接子项：
  是目录 → 跳过（解压不需要目录）
  是文件：
    if (isForDecompression)
      // 解压模式：按 GUI 扩展名过滤
      后缀 == extension → 进列表
      或 匹配该扩展名对应的分卷格式 → 进列表
      否则 → 跳过
```

解压模式匹配规则：

| GUI 扩展名 | 单卷匹配 | 分卷匹配 |
| --- | --- | --- |
| `rar` | `*.rar` | `*.partNN.rar` |
| `7z` | `*.7z` | `*.7z.NNN` |
| `zip` | `*.zip` | `*.zip.NNN` |
| 其它 | `*.<extension>` | 无 |

##### 目录扫描：全量示例（18 条，覆盖 GUI 各扩展名 × 单卷/分卷/非匹配文件/子目录）

folder = `/tmp/d/`，该目录下真实文件集合 S：

```
/tmp/d/a.rar
/tmp/d/a.part01.rar
/tmp/d/a.part02.rar
/tmp/d/a.part03.rar
/tmp/d/b.7z
/tmp/d/b.7z.001
/tmp/d/b.7z.002
/tmp/d/b.7z.003
/tmp/d/c.zip
/tmp/d/c.zip.001
/tmp/d/c.zip.002
/tmp/d/d.tar
/tmp/d/d.tar.gz
/tmp/d/e.gz
/tmp/d/e.gz.001（非标准格式分卷，tar/gz 的 NNN 卷）
/tmp/d/readme.txt
/tmp/d/subdir/（真的是个目录）
/tmp/d/a.RAR（大小写混杂）
/tmp/d/b.7Z（大小写）
/tmp/d/c.ZIP.001（大小写）
/tmp/d/a.part1.rar（1 位编号）
/tmp/d/b.7z.1（1 位编号）
```

> 注意：每次调用只传一个 GUI extension，观察哪些文件被 LoadFilesFromFolder 选入返回列表（返回列表在后续 Batch 里还会被"首卷过滤+去重"再处理，但扫描阶段先只看扩展名过滤）。

**A. GUI 扩展名 = `rar` + isForDecompression = true**

| # | GUI extension=？ | 单卷后缀 / 分卷格式规则 | S 中哪些文件命中？（文件名） | 未命中、跳过的文件及原因 | 返回扫描列表（文件名） |
| --- | --- | --- | --- | --- | --- |
| 1 | `rar`（解压） | 单卷 *.rar（IgnoreCase） + 分卷 *.partNN.rar（IgnoreCase，NNN 至少 1 位数字） | `a.rar`, `a.part01.rar`, `a.part02.rar`, `a.part03.rar`, `a.RAR`, `a.part1.rar` | `b.*`（7z/zip/tar/gz 非 rar 系列）, `d.*`, `e.*` 非 rar, `readme.txt` 非归档, `subdir/` 是目录, `b.7Z`, `c.ZIP.001` 不同格式, `b.7z.1` 非 rar | [a.rar, a.part01.rar, a.part02.rar, a.part03.rar, a.RAR, a.part1.rar]（后续首卷过滤还会再处理：`a.rar` 单卷保留、`a.part01/02/03` 只保留 part01 为首卷、`a.RAR` 与 `a.rar` 是否大小写歧义 → 判重或警告，a.part1.rar 与 a.part01.rar 同编号 1 冲突？→ 歧义跳过） |

**B. GUI 扩展名 = `7z` + isForDecompression = true**

| # | GUI extension=？ | 命中文件 | 跳过文件原因 | 返回扫描列表 |
| --- | --- | --- | --- | --- |
| 2 | `7z`（解压） | `b.7z`, `b.7z.001`, `b.7z.002`, `b.7z.003`, `b.7Z`, `b.7z.1` | 其他 all（a.rar 系列、c.zip 系列、d/e/tar/gz、readme、subdir、a.*、c.*） | [b.7z, b.7z.001, b.7z.002, b.7z.003, b.7Z, b.7z.1]（后续首卷过滤：单卷 b.7z 保留；分卷中 b.7z.001/b.7z.1 都是编号 1 → **同编号 1 冲突（1 位和 3 位并存）→ 歧义跳过或报错**；b.7z.002/003 非首卷过滤） |

**C. GUI 扩展名 = `zip` + isForDecompression = true**

| # | GUI extension=？ | 命中文件 | 跳过文件原因 | 返回扫描列表 |
| --- | --- | --- | --- | --- |
| 3 | `zip`（解压） | `c.zip`, `c.zip.001`, `c.zip.002`, `c.ZIP.001` | 其他 all | [c.zip, c.zip.001, c.zip.002, c.ZIP.001]（后续首卷过滤：c.zip 单卷保留；c.zip.001 + c.ZIP.001 → 不同大小写但同编号 1 同目录不同文件 → Linux 下会视为两个文件！→ 歧义冲突跳过） |

**D. GUI 扩展名 = `tar` + isForDecompression = true（非三格式，无分卷识别）**

| # | GUI extension=？ | 命中规则 | 命中文件 | 跳过 | 返回 |
| --- | --- | --- | --- | --- | --- |
| 4 | `tar`（解压） | 单卷 *.tar（严格等于最后段后缀），**无任何分卷识别** | `d.tar`（Path.GetExtension 返回 `.tar` = tar） | `d.tar.gz`（最后段后缀 `.gz` ≠ `.tar`）、其它格式、`e.gz.001`（.001 非 tar）、其它非 tar | [d.tar]。注意：**d.tar.gz 没有被扫进来！** 非三格式也没有分卷识别 → 扫不到是预期设计 |

**E. GUI 扩展名 = `gz` + isForDecompression = true（非三格式，无分卷识别）**

| # | GUI ext | 命中 | 跳过 | 返回 |
| --- | --- | --- | --- | --- |
| 5 | `gz`（解压） | 单卷 *.gz | `e.gz` 命中；`e.gz.001` **不匹配**（最后段 .001 ≠ .gz，且 gz 非三格式，无 `.gz\.NNN` 分卷模式） | 其他 all；`d.tar.gz`（最后段是 `.gz` → 命中！`Path.GetExtension = .gz` → 会被误当成 gz 文件？→ 是的，当前只判断最后段后缀相等 → tar.gz 会被当成 gz 扫进来） | 返回 [e.gz, d.tar.gz]；其中 d.tar.gz 其实是 tar+gz 组合，7zz 仍能解（因为 7zz 会先解 gz 层），但严格意义上扩展名"tar"语义被忽略 → 记录 |

**F. isForDecompression = false（压缩模式目录扫描）——不做扩展名过滤，所有文件全进**

| # | GUI ext + isForDecompression=？ | 命中规则（和解压不同） | S 中所有文件（除目录） | 返回 |
| --- | --- | --- | --- | --- |
| 6 | 任意 ext + `isForDecompression=false`（压缩目录加文件） | 文件全进（除目录），不做任何扩展名过滤 | 除 subdir/ 外全部 21 个文件（行 118-140 共 22 行，其中 subdir/ 是目录排除，所以文件数 = 22 - 1 = 21） | 列表 21 项，全部压缩时交给压缩路由按 GUI ext 合成对应格式 |

**G. 子目录是否递归？目录嵌套深一点测**

folder = `/tmp/deep/`：

```
/tmp/deep/1.rar
/tmp/deep/sub/2.rar（二级目录里的 rar）
/tmp/deep/sub/deep/3.rar
```

| # | 说明 | 扫描策略 | 命中哪些？ | 返回 |
| --- | --- | --- | --- | --- |
| 7 | 当前：**只扫直接子项**（`Directory.EnumerateFiles(folder, "*", SearchOption.TopDirectoryOnly)`） | TopDirectoryOnly，不递归 | 只扫到 `/tmp/deep/1.rar`；sub/2.rar 因为是 sub 子目录，EnumerateFiles TopDirectoryOnly 不会进去；sub/ 本身是目录跳过 | [1.rar]。若用户想递归要在 GUI 上做"递归选目录"开关或在该函数加 SearchOption 参数。记录设计边界：当前只扫一层 |

**H. 空目录 + 全是非匹配文件**

folder 下：`readme.txt`, `log.csv`, `photo.jpg` 全部非归档，GUI ext=`rar`

| # | 命中？ | 返回 |
| --- | --- | --- |
| 8 | 0 个文件匹配 rar 系列 | 返回空 List → 压缩/解压循环无事可做 → UI 提示"没有匹配的文件" |

**I. 文件名空格/中文字符**

folder：`中文文件.rar`, `我的文档.part01.rar`（GUI ext rar）

| # | 命中？ | 返回 |
| --- | --- | --- |
| 9 | 两个均匹配 `.rar` / `part\d+\.rar$`（正则 IgnoreCase，中文不影响） | 返回 [中文文件.rar, 我的文档.part01.rar]。路径由 .NET 原生 string，不需要特别转义（只要正则没硬编码 ASCII 限定） |

**J. 同一 baseName 既有单卷又有分卷系列（典型误操作场景）**

folder：`a.rar`（独立单卷，内容 A）, `a.part01.rar`, `a.part02.rar`, `a.part03.rar`（另一组分卷，内容 B）

| # | GUI ext | 扫描阶段结果 | 后续首卷过滤阶段结果（重要！避免 2 个内容互相覆盖/冲突） |
| --- | --- | --- | --- |
| 10 | rar | [a.rar, a.part01/02/03]（全部命中 rar 系列） | 首卷过滤：`a.rar` 单卷是 1 组；`a.part01/02/03` 是另一组首卷 part01 → 两组首卷都是同"a"但文件路径不同 → 视为 2 个独立归档，顺序解压；如果它们的解压输出目录相同（都用同名目录 a/），解压后第二组会覆盖第一组内容 → 建议：解压输出去重，发现目标目录已存在 → 追加 `-2` `-3` 后缀或直接冲突报错。该处理在解压循环输出目录判定时做 |

**K. 扫描结果的首卷过滤（扫描返回 raw 列表后，Batch 还要再首卷过滤 1 次）**

扫描 raw 返回 rar 系列：`[a.rar, a.part01, a.part02, a.part03]`（4 个文件）。首卷过滤算法（在解压循环开头/或密码本加载阶段）：

| # | 文件 | IsMultiVolumeArchive 返回 true/false | firstVolumePath（out） | 是否是"自己=首卷"或"非分卷单卷"？ | 保留还是过滤？ |
| --- | --- | --- | --- | --- | --- |
| 11a | `a.rar`（单卷） | false → 非分卷 | 自身 a.rar | ✓ 单卷保留 | 保留（1） |
| 11b | `a.part01` | true，分卷；编号 1 | 首卷就是自己 a.part01.rar | ✓ 首卷自己 | 保留（2） |
| 11c | `a.part02` | true，分卷编号 2 | 首卷 = a.part01.rar（不同文件） | ✗ 非首卷：过滤 | 过滤掉 |
| 11d | `a.part03` | true，分卷编号 3 | 首卷 = a.part01.rar | ✗ 非首卷 | 过滤掉 |

最终进入解压循环真正处理的只有 2 项：`a.rar` 和 `a.part01.rar`（对应 J 场景的两组内容，后续各独立走解压）。

**L. 文件名只有扩展名（空 baseName）的极端 case**

folder：`.rar`（文件名就是 `.rar`，baseName 空）, `.part01.rar`, `.7z.001`

| # | GUI ext | 扫描命中？ | 后续阶段 |
| --- | --- | --- | --- |
| 12a | rar → 扫描 | `*.rar` 匹配 `.rar`（EndsWith `.rar`）；`*.partNN.rar` 匹配 `.part01.rar` → 2 个都命中 | 是 |
| 12b | 7z → 扫描 | `*.7z.001` 匹配 `.7z.001`（NNN=1） → 1 个命中 | 是；baseName 空也能正常处理（只要正则拼对 Escape(baseName) 后仍空，最终拼 `^\.7z\.(\d+)$` 正常匹配 `.7z.001`） |

**M. 编号超宽 + 超编号**

folder：`a.part99999999999.rar`（11 位数编号）, `a.7z.0000001`（7 位数）

| # | GUI ext | 扫描命中？ | 后续 IsMultiVolumeArchive：抓编号用 int64 不？ |
| --- | --- | --- | --- |
| 13 | rar → 扫到 | 是；正则 `\d+` 抓任意长数字串 → 命中小数正则 | 抓编号建议用 long/Int64 避免 int32 溢出（int 最大 2147483647，10 位能到；11 位就超） |

**N. 文件实际是 zip 但改后缀成 rar（伪装）**

folder：`fake.rar`（实际是 zip）, GUI ext=`rar`

| # | 扫描命中？ | 路由 | 结果 |
| --- | --- | --- | --- |
| 14 | 命中（后缀名 rar，扫描不管内容） | 解压路由走 RAR 引擎 | RAR 引擎打开失败报"不是 RAR 归档" → 写入错误日志；不会自动 fallback 到 7zz（如果需要 fallback：先试 RAR 失败→再试 7zz；但这样会让密码错误和格式错误混淆 → 先判断再 fallback 代价大；文档默认不 fallback，直接让用户改回正确后缀或重命名） |

**O. 目录扫描权限（Directory.EnumerateFiles 抛 UnauthorizedAccessException 等）**

folder 有子文件但用户无权读（如 /root/ 非 root）

| # | 扫描结果 | 返回 |
| --- | --- | --- |
| 15 | EnumerateFiles 抛异常 → 应整体 try/catch；单个文件/目录权限异常应跳过并记日志，不能整体 fail | 返回已成功扫描到的列表；日志记权限错误文件。不能让单个 0 字节无权文件导致整个目录 0 条扫描 |

**P. 符号链接 / 挂载点 / macOS bundle 目录（.app）**

folder 下：`link.rar`（是 symlink → 指向真实 rar）, `MyApp.app`（macOS app bundle，实际是目录）

| # | 行为？ |
| --- | --- |
| 16 | link.rar：File.Exists + EnumerateFiles 按符号链接走 → 正常命中（因为 File.Exists 默认跟符号链接）。如果符号链接指向不存在目标 → File.Exists 返回 false，扫描阶段会不会加进来？EnumerateFiles 会先把名字列出来（link.rar 是目录中的条目），EnumerateFiles 返回的是"符号链接这个文件"即使它指向不存在。调用方后续 File.Exists 才会判 false → 在后续阶段过滤。扫描阶段只看"枚举到该文件名且扩展名对得上" → 扫进来；后续 File.Exists 判不存在 → 跳过。 |
| 17 | MyApp.app：macOS 是目录（不是文件）。EnumerateFiles 不返回它（SearchOption.TopDirectoryOnly 只返回文件）→ 跳过。但如果用户 GUI 拖 app bundle 进来，SourceMode=Directory 走目录扫 → 仍按目录跳过 → 正确 |

**Q. Windows 盘符路径（代码跨平台，先验证 Windows 路径格式不会炸）**

folder = `D:\Archives\`，文件 `D:\Archives\a.rar`（在 macOS/Linux 上这种字符串路径是合法吗？→ .NET 在跨平台下 `Path.GetFileName(@"D:\Archives\a.rar")` 仍能按分隔符 fallback 正确提取 `a.rar`；只要不真去 Open() 就不会 IO 异常）

| # | 扫描结果（在 mac 上跑，路径字符串传入） |
| --- | --- |
| 18 | Path 解析没问题，正则匹配也正常；Directory.EnumerateFiles 在 mac 上找不到 D: → 抛 DirectoryNotFoundException → 整体 catch → 提示用户"目录不存在"。不炸进程 |

> 排查提示：
> - "我拖了 .tar.gz 进 GUI 选 tar 却没扫出来" → 查 D4（只看最后段后缀，所以扫不到；应该选 gz 才能扫 d.tar.gz），或在 D4 中给 MatchesGuiExtension 加复合后缀别名
> - "同编号冲突（1 位/3 位并存）没报但解压错误" → 查 B2 和首卷过滤的冲突检测（必须在 GetAllVolumeFiles 或首卷过滤阶段严格检测编号重复并跳过 + 日志）
> - "符号链接指向不存在的文件仍被扫进来报一堆不存在" → O16：扫描阶段加 File.Exists 过滤（EnumerateFiles 先拿到名字后立即 File.Exists，false 就跳过不要进列表），或交给后续阶段统一过滤。两种策略都可以；文档先记录，实现时选其一

调用点：
- 解压入口（模式 2 + SourceMode=2 + 解压按钮）：传 `isForDecompression=true`

### 3. [BatchOperationService.cs 第115-140行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L115-L140)（密码本加载）

改造：文件名解析 + 逻辑名→实际首卷 + 分卷首卷重定向 + 连续性检查。

```
对密码本每两行（文件名/密码）：

1. 文件名解析：
   if (HasArchiveExtension(filename))
     // 含归档后缀（1.rar、1.part01.rar、1.png.rar）→ 原样
     fullPath = Path.IsPathRooted(filename) ? filename : Path.Combine(sourceFolder, filename)
   else
     // 不含归档后缀（1、1.png、1.txt）→ 补 GUI 扩展名
     fullPath = Path.Combine(sourceFolder, filename + "." + extension)

2. 逻辑名 → 实际首卷：
   fullPath = ResolveActualFirstVolume(fullPath) ?? fullPath

3. 分卷首卷重定向：
   if (IsMultiVolumeArchive(fullPath, out firstVolumePath))
     fullPath = firstVolumePath

4. 文件存在性检查：
   if (!File.Exists(fullPath))
     日志："未找到 X（也不是 Y 分卷首卷）"
     MissingEntries.Add(fullPath); continue

5. 分卷连续性检查（新增）：
   if (IsMultiVolumeArchive(fullPath, out _))
     var volumes = GetAllVolumeFiles(fullPath)
     if (!IsVolumeSequenceContiguous(volumes))
       日志："分卷不连续，已跳过：X（缺少 ...）"
       continue   // 不计 SuccessCount/FailCount，不进入解压、移动、删除

6. 进待解压列表
```

### 4. [BatchOperationService.cs 第244-285行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L244-L285)（分卷首卷判定）

改造：移除 `extension` 参数，按文件实际后缀分派。

#### 为什么移除 `extension` 参数

与 `GetAllVolumeFiles` 同理。存在"密码本含归档后缀，与 GUI 扩展名不一致"的合法场景：

```
密码本写 1.part02.rar（含归档后缀）→ 原样使用
GUI 扩展名 = 7z
实际文件 1.part01.rar ~ 1.part03.rar
```

此时必须把 `1.part02.rar` 重定向到首卷 `1.part01.rar`。

两种取法的行为差异：

| 判定分卷用 | 构造的正则 | 对 `1.part02.rar` 的结果 |
| --- | --- | --- |
| `extension` 参数（GUI 扩展名 `7z`） | `\.part(\d+)\.7z$` | 不匹配 → 视为单卷 → 不重定向 → 重复解压 `1.part02.rar` ✗ |
| 文件实际后缀（`rar`） | `\.part(\d+)\.rar$` | 匹配 → 重定向到 `1.part01.rar` → 只解压首卷 ✓ |

保留 `extension` 参数会导致非首卷不被重定向、被当作单卷重复解压。

#### 函数签名与逻辑

```
签名变更：IsMultiVolumeArchive(string path, out string firstVolumePath)
移除 extension 参数

按文件实际后缀分派：
  .partNN.rar（后缀 .rar，文件名含 .partNN）→ RAR 分卷
    首卷：baseName + ".part" + "1".PadLeft(位数, '0') + ".rar"
  .7z.NNN（后缀是数字，文件名含 .7z）→ 7z 分卷
    首卷：baseName + ".7z." + "1".PadLeft(位数, '0')
  .zip.NNN（后缀是数字，文件名含 .zip）→ ZIP 分卷
    首卷：baseName + ".zip." + "1".PadLeft(位数, '0')
  其它 → 非分卷
```

正则全部使用 `RegexOptions.IgnoreCase`。

调用点同步改：
- [第121行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L121)（密码本加载）
- [第638行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L638)（解压循环首卷过滤）

##### IsMultiVolumeArchive：边界示例（20 条，按 4 类分派 + out 参数首卷候选 File.Exists 结果）

函数签名：`bool IsMultiVolumeArchive(string path, out string firstVolumePath)`
- 返回 true → 识别为分卷（但首卷候选是否磁盘存在会影响 out 是否真的指向可解压文件？文档语义：true = 名字是分卷形态，out = 对应首卷名；调用方需再次 File.Exists 判首卷真实存在）

| # | 输入 path 文件名 | 分派格式 | 抓到编号 | 抓到位数 digits | 首卷候选 firstVolumePath 拼接 | 同目录下首卷文件存在？ | 返回值 | 最终 out 值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `1.part01.rar` | RAR part 分卷 | `01` → 1 | 2 | `1.part01.rar`（当前就是首卷） | 是 | true | `1.part01.rar` | 首卷本身 |
| 2 | `1.part03.rar` | RAR part 分卷 | `03` → 3 | 2 | `1.part01.rar`（补 01） | 是 | true | `1.part01.rar` | 非首卷重定向到首卷 |
| 3 | `1.part1.rar`（1 位数） | RAR part 分卷 | `1` → 1 | 1 | `1.part1.rar` | 是 | true | `1.part1.rar` | 1 位编号首卷 |
| 4 | `1.part2.rar`（1 位编号非首卷） | RAR part 分卷 | `2` → 2 | 1 | `1.part1.rar` | 是 | true | `1.part1.rar` | 按同位数补 1 |
| 5 | `archive.PART999.RAR` | RAR part 分卷（IgnoreCase） | `999` → 999 | 3 | `archive.PART001.RAR`（按文件名保留大小写？文档推荐：**首卷候选保留原文件名字符大小写风格**再拼补齐位数部分） | 实际存在 `archive.PART001.RAR` → 是 | true | `archive.PART001.RAR` | 大小写敏感 + 位数补齐 |
| 6 | `1.7z.001` | 7z 数字分卷 | `001` → 1 | 3 | `1.7z.001`（首卷自身） | 是 | true | `1.7z.001` | |
| 7 | `1.7z.003` | 7z 数字分卷 | `003` → 3 | 3 | `1.7z.001` | 是 | true | `1.7z.001` | 非首卷重定向 |
| 8 | `project.v2.7z.1`（1 位数） | 7z 数字分卷 | `1` → 1 | 1 | `project.v2.7z.1` | 是 | true | `project.v2.7z.1` | |
| 9 | `1.7Z.025`（大写 7Z） | 7z 数字分卷（IgnoreCase） | `025` → 25 | 3 | `1.7Z.001` | 是 | true | `1.7Z.001` | 后缀大小写保留原风格 |
| 10 | `1.zip.001` | ZIP 数字分卷 | `001` → 1 | 3 | `1.zip.001` | 是 | true | `1.zip.001` | |
| 11 | `data.zip.002` | ZIP 数字分卷 | `002` → 2 | 3 | `data.zip.001` | 是 | true | `data.zip.001` | 非首卷重定向 |
| 12 | `data.ZIP.099`（大写 ZIP） | ZIP 数字分卷（IgnoreCase） | `099` → 99 | 3 | `data.ZIP.001` | 是 | true | `data.ZIP.001` | |
| 13 | `1.rar`（单卷） | 后缀 rar，但文件名不匹配 `part\d+\.rar$` | — | — | — | — | false | 输入自身 | 单卷 RAR 不是分卷 |
| 14 | `1.7z`（单卷） | 后缀 7z，不匹配 `\.7z\.\d+$` | — | — | — | — | false | 输入自身 | 单卷 7z 不是分卷 |
| 15 | `1.tar` | 后缀 tar，三格式分卷正则都不匹配 | — | — | — | — | false | 输入自身 | tar 没有分卷格式识别 → 单卷 |
| 16 | `1.part02.rar` + 目录里根本没有 part01 | RAR part 分卷（形态上） | 2 | 2 | `1.part01.rar` | **首卷候选不存在磁盘** | **true**（名字是分卷形态） | `1.part01.rar`（即使不存在） | 关键！返回 true 只表示"这是分卷文件名"，不保证首卷存在；调用方用 File.Exists(firstVolumePath) 判是否可解，不存在则走"缺首卷→过滤或报错" |
| 17 | `1.7z.003` + 没有 001 | 7z 数字分卷 | 3 | 3 | `1.7z.001` | 不存在 | **true** | `1.7z.001`（不存在） | 同上，调用方判不存在 → 跳过或警告 |
| 18 | `1.7z.001` → 目录里只有 001（单分卷） | 7z 数字分卷 | 1 | 3 | `1.7z.001` | 存在 | true | 自身 | 单卷也判是分卷（名字是 7z.NNN），后续连续性检查会判连续（只有 1 卷，min==max）→ 正常送解压 → 7zz 本身报"这不是分卷"或直接解 → 引擎兜底 |
| 19 | `photo.with.dots.part02.rar` | RAR part 分卷 | 2 | 2 | `photo.with.dots.part01.rar` | 存在 | true | 首卷 | baseName 取 Substring(0, m.Index) 不会因中间点号截断 |
| 20 | `a.part01.rar` + 同目录另有一组 `a.7z.001`（同名前缀多组分卷共存？不冲突） | RAR part 分卷 | 1 | 2 | `a.part01.rar` | 存在 | true | 首卷 RAR | 分派按文件实际后缀，rar/7z/zip 分派分支互斥，不会串到另外一组。RAR 不会去扫 7z 分卷表 |

> 排查提示：
> - "非首卷输入但没被重定向到首卷" → 查 #16/#17：首卷候选拼对了但磁盘不存在？调用方 File.Exists 判断后是否把 firstVolumePath 当成不存在又退回原文件？→ 应该：首卷候选存在 → 用候选；不存在 → 判为"分卷但缺首卷"→ 跳过 + 日志，不要回退原文件当单卷送引擎！
> - "位数不对导致首卷候选拼错" → 例：`part002`（3 位数）时 digits=3，首卷 `part001` 而不是 `part01`——按当前输入文件抓到的数字位数 padLeft，没错。

### 5. [BatchOperationService.cs 第888-933行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L888-L933)（取整组卷）

改造：移除 `extension` 参数，改为正则扫描目录，按文件实际后缀分派，收集所有匹配分卷（不提前终止）。

#### 为什么移除 `extension` 参数

存在"密码本含归档后缀，与 GUI 扩展名不一致"的合法场景（见核心原则第5条和示例2）：

```
密码本写 1.rar（含归档后缀）→ 原样使用
GUI 扩展名 = 7z
实际文件 1.part01.rar ~ 1.part03.rar
```

此时按核心原则第5条，必须解压 `1.part01.rar` 并清理整组 RAR 分卷。

两种取法的行为差异：

| 取整组卷用 | 构造的正则 | 对 `1.part01.rar` 的结果 |
| --- | --- | --- |
| `extension` 参数（GUI 扩展名 `7z`） | `^1\.7z\.(\d+)$` | 不匹配 → 只返回首卷本身 → `1.part02.rar`、`1.part03.rar` 残留 ✗ |
| 文件实际后缀（`rar`） | `^1\.part(\d+)\.rar$` | 匹配 → 返回 3 个卷 → 整组清理 ✓ |

保留 `extension` 参数（GUI 扩展名）会导致 RAR 分卷识别失败、后处理残留。

结论：`GetAllVolumeFiles` 必须按文件实际后缀分派，不能用 GUI 扩展名。函数内部从 `archivePath` 取实际后缀即可，调用方不需要传扩展名。

#### 函数签名与逻辑

```
签名变更：GetAllVolumeFiles(string archivePath)
移除 extension 参数

1. 按 archivePath 的实际后缀分派
2. 提取 baseName
3. 构造正则（不区分大小写）：
   rar：^baseName\.part(\d+)\.rar$
   7z： ^baseName\.7z\.(\d+)$
   zip：^baseName\.zip\.(\d+)$
4. Directory.EnumerateFiles(dir) 扫描
5. 收集所有匹配的文件
6. 按编号升序排序
7. 返回列表
```

##### GetAllVolumeFiles：边界示例（18 条，分派 + 扫描 + 排序）

扫描目录 `/tmp/d/`，archivePath 输入为该目录下的某个归档文件（后缀决定分派）。

| # | archivePath | 分派格式 | baseName 提取 | 同目录下的所有文件（其它无关文件是否会被误扫？） | 正则匹配到的卷文件 | 按编号升序排序后返回 | 说明 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `/tmp/d/1.part02.rar` | RAR part | `1` | `1.part01.rar`, `1.part02.rar`, `1.part03.rar`, `2.rar`（无关单卷）, `note.txt`（无关） | 1.part01/02/03 | [part01, part02, part03] → 编号 [1,2,3] → 返回这三个 | 正确抓取同一 baseName 的 RAR 分卷；其它文件不匹配正则，不误扫 |
| 2 | `/tmp/d/1.7z.003` | 7z 数字分卷 | `1` | `1.7z.001`, `1.7z.002`, `1.7z.003`, `1.7z`（单卷，后缀 7z 不匹配 NNN 尾） | 1.7z.001~003 | [001,002,003] | 单卷 1.7z 不匹配 `\.7z\.\d+` → 不会被当成卷文件误扫 |
| 3 | `/tmp/d/a.zip.010` | ZIP 数字分卷 | `a` | `a.zip.001` ... `a.zip.010`（全 10 个）, `a.zip`（单卷） | a.zip.001~010 | [001..010] | 10 卷全抓；a.zip 单卷不进 |
| 4 | `/tmp/d/photo.with.dots.part03.rar` | RAR part | `photo.with.dots` | `photo.with.dots.part01.rar`, `photo.with.dots.part02.rar`, `photo.with.dots.part03.rar` | 3 个 | [1,2,3] | 含点号的 baseName 需 `Regex.Escape` 才能正确匹配（未 Escape 会把 `.` 当成通配匹配任意字符 → 误匹配 `photowithdots` 也抓进来） |
| 5 | `/tmp/d/a.PART01.RAR`（全大写） | RAR part（IgnoreCase） | `a` | `a.PART01.RAR`, `a.Part02.Rar`, `a.part03.rar`（大小写混合） | 3 个（IgnoreCase 都匹配） | [1,2,3] | 大小写都被扫入 ✓ |
| 6 | `/tmp/d/a.7Z.002`（大写 7Z） | 7z（IgnoreCase） | `a` | `a.7z.001`, `a.7Z.002`（大小写不同但扩展名都是 7z）, `A.7Z.003`（baseName 首字母大写，和 `a` 不同 → 但正则 IgnoreCase 下 `a` 匹配 `A`） | `a.7z.001`, `a.7Z.002`, `A.7Z.003` | [001,002,003] | 正则整体 IgnoreCase（含 baseName 部分），所以 `A.7Z.003` 也匹配 `^a\.7z\.(\d+)$` → 3 个都进列表。**设计明确**：baseName 部分也 IgnoreCase，避免大小写同名但分成两组的问题 |
| 7 | `/tmp/d/a.7z.001` 目录里只有 001 一卷 | 7z | `a` | 只有 `a.7z.001` 本身 | 1 个 | [001] | 单卷返回 1 条；后处理删除时只会删自己 1 个 |
| 8 | `/tmp/d/1.part02.rar`（**目录没有 part01**） | RAR part | `1` | `1.part02.rar`, `1.part03.rar` 只有中尾 | 2 个（part02, part03） | [2,3] | 缺 part01 但扫描仍返回实际存在的 [2,3]，不自动补 part01；**连续性检查** 看 min=2 max=3 → 期望 [2,3]，判"连续"——然后靠 `File.Exists(firstVolumePath)` 判缺首卷，双重兜底缺一不可 |
| 9 | `/tmp/d/1.part01.rar`（part01 + part03，**缺 part02**） | RAR part | `1` | `1.part01.rar`, `1.part03.rar` | 2 个（1, 3） | [1,3] | 返回实际存在卷；连续性检查发现缺 2 → 判不连续 → 跳过 |
| 10 | `/tmp/d/a.7z.001` 同目录另一个组 `b.7z.001/002`（不同 baseName） | 7z | `a` | `a.7z.001`, `b.7z.001`, `b.7z.002` | 只 `a.7z.001` | [1] | 不同 baseName 不串组 |
| 11 | `/tmp/d/archive.part1.rar`（**位数不齐**：part01~part09 是 1 位，part10 是 2 位） | RAR part | `archive` | `archive.part1.rar`, `archive.part2.rar` ... `archive.part9.rar`, `archive.part10.rar` | **这里有坑！** 如果正则写死 `\d+` 但用户实际文件名位数不齐：全部匹配（正则抓编号值），然后按编号升序排 | 编号值 [1..10] 排序正确（part1, part2…part10 字符串排序会把 part10 放在 part2 前，但本函数按编号值排序 → part10 在最后 ✓） | 关键：**排序规则必须按 int 编号值，不是按字符串！** 否则 10 排到 2 之前，解压引擎按首卷+顺序解会乱序或报缺卷 |
| 12 | `/tmp/d/a.7z.001` 同目录另有 `a.7z.001.backup`（含额外后缀） | 7z | `a` | `a.7z.001`, `a.7z.001.backup`（实际尾是 `.backup` 不是数字），`a.7z.002` | `a.7z.001`, `a.7z.002`（`.backup` 不匹配 `\.(\d+)$` 尾） | [1, 2] | 不匹配的备份文件不会进列表 ✓ |
| 13 | `/tmp/d/项目 文档.part02.rar`（文件名含空格+中文） | RAR part | `项目 文档` | `项目 文档.part01.rar`, `项目 文档.part02.rar` | 2 个 | [1, 2] | Regex.Escape(baseName) 会把空格转义吗？.NET 中 Regex.Escape 转义空格不需要；空格在正则里就是字面空格。只要匹配严格就没问题 |
| 14 | archivePath 是**单卷** rar（不是分卷文件名）：`/tmp/d/1.rar` | 单卷 → 后缀不是 NNN，文件名不匹配 part\d+\.rar | — | 任意 | **0 个**（空 List） | 空 List | 单卷不走分卷后处理。后处理里 volumes.Count<2 → 视为单卷逻辑删/移自己 |
| 15 | archivePath 是 `/tmp/d/1.tar`（tar 单卷） | 其它 → 三格式分派都不命中 | — | 任意 | 0 个 | 空 List | 非 rar/7z/zip 分卷都不会去扫目录（tar 等没有通用分卷命名） |
| 16 | `/tmp/d/a.7z.001` 同目录还有一组 `a.7z.1` / `a.7z.2`（位数 1 和 3 并存） | 7z | `a` | `a.7z.001`, `a.7z.1`, `a.7z.2`, `a.7z.002`, `a.7z.003` | 全部 5 个都匹配 `\.7z\.(\d+)$` | 编号值 [1,1,2,2,3] → **同编号 1 重复**（001 和 1 都是编号 1） | 严重歧义！返回同编号重复，调用方要报错或记录"同编号多分卷文件宽度不一致"并跳过；建议 GetAllVolumeFiles 内部做"按编号去重 + 冲突检测"：同编号发现多个不同文件 → 记日志并返回冲突标记，避免误操作删除/移动错误卷 |
| 17 | baseName 为空（极端）：输入 `/tmp/d/.part01.rar`（文件名是 `.part01.rar`，即 baseName 是空串） | RAR part | 空串（Substring(0,0)） | `.part01.rar`, `.part02.rar` | **正则 `^.part(\d+)\.rar$` 是否匹配？** baseName 空串经 Escape 后也是空，拼接得到 `^\.part(\d+)\.rar$` → 匹配 | [1, 2] | 支持空 baseName（只要正则拼对） |
| 18 | `/tmp/d/1.7z.001` 但**实际是一个 zip 内容的文件伪装成 7z.NNN** | 看名字不看内容 → 7z 分派 | `1` | `1.7z.001`, `1.7z.002` | 2 个 | [1, 2] | 名字分派不读内容——内容错误交给解压引擎报错，不是扫描阶段的职责 |

> 排查提示：
> - "多组分卷串组（不同 baseName 混到一起）" → 查 #4（是否忘了 `Regex.Escape(baseName)` 导致点号通配）、#6（baseName 是否大小写混用没 IgnoreCase）
> - "part10 排在 part2 前" → 查 #11（是否按字符串排序而不是按 int 值编号排序）
> - "删除/移动后有分卷残留" → 查 #16（同编号不同宽度导致误把新宽度的卷漏掉，或冲突后仍继续操作）→ 严格冲突检查

调用点同步改：
- [第698行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L698)（解压后处理）

### 6. 新增函数

放在 [BatchOperationService.cs](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs)：

#### `HasArchiveExtension(string filename)`

```
取文件名小写
第一步：匹配分卷模式（任一命中则返回 true）：
  正则 \.part\d+\.rar$       → RAR part 分卷（如 1.part01.rar）
  正则 \.7z\.\d+$             → 7z 数字分卷（如 1.7z.001）
  正则 \.zip\.\d+$            → ZIP 数字分卷（如 1.zip.001）
第二步：匹配单卷后缀（以以下任一结尾则返回 true）：
  .rar .7z .zip
  .tar .gz .tgz .bz2 .tbz2 .xz .txz
  .iso .cab .lzh .arj .z .lzma .cpio .rpm .deb .dmg .wim
均不命中 → 返回 false
```

注意：
- 复合后缀如 `1.tar.gz` → 取最后一段 `.gz`，在列表中 → 返回 true
- 分卷反例 `1.7z.001` → 最后一段是数字 `.001`（不在列表），但命中 `\.7z\.\d+$` → 返回 true ✓
- 分卷反例 `1.zip.003` → 命中 `\.zip\.\d+$` → 返回 true ✓
- `1.part01.rar` → 最后一段 `.rar` 也能通过单卷后缀命中，但显式分卷匹配优先明确语义

##### HasArchiveExtension：全量边界示例（57 条）

**第一类：分卷模式命中（第一步，优先于单卷后缀判断）**

| # | 输入 filename（密码本写的文件名，不含目录） | 命中哪条 | 返回值 | 说明 |
| --- | --- | --- | --- | --- |
| 1 | `1.part1.rar` | `\.part\d+\.rar$` | true | 1 位编号 RAR part 分卷 |
| 2 | `1.part01.rar` | `\.part\d+\.rar$` | true | 2 位编号 RAR part 分卷 |
| 3 | `1.part001.rar` | `\.part\d+\.rar$` | true | 3 位编号 RAR part 分卷 |
| 4 | `1.part9999.rar` | `\.part\d+\.rar$` | true | 4 位编号 RAR part 分卷 |
| 5 | `photo.with.dots.part02.rar` | `\.part\d+\.rar$` | true | 文件名中间含点号不影响 |
| 6 | `1.PART01.RAR`（全大写） | `\.part\d+\.rar$`（IgnoreCase） | true | 大小写不敏感命中 |
| 7 | `1.Part03.Rar`（混大小写） | `\.part\d+\.rar$`（IgnoreCase） | true | 大小写不敏感命中 |
| 8 | `1.7z.1` | `\.7z\.\d+$` | true | 1 位编号 7z 数字分卷 |
| 9 | `1.7z.001` | `\.7z\.\d+$` | true | 3 位编号 7z 数字分卷（最常见） |
| 10 | `project.v2.7z.019` | `\.7z\.\d+$` | true | 文件名中间 `v2.` 的点号不误挡 |
| 11 | `archive.7Z.025`（大写 7Z） | `\.7z\.\d+$`（IgnoreCase） | true | 大小写不敏感 |
| 12 | `1.zip.1` | `\.zip\.\d+$` | true | 1 位编号 ZIP 数字分卷 |
| 13 | `1.zip.001` | `\.zip\.\d+$` | true | 3 位编号 ZIP 数字分卷 |
| 14 | `data.set.v2.zip.007` | `\.zip\.\d+$` | true | 多段点号不误 |
| 15 | `img.ZIP.999`（大写 ZIP） | `\.zip\.\d+$`（IgnoreCase） | true | 大小写不敏感 |

**第二类：单卷后缀命中（第二步，兜底）**

| # | 输入 filename | 命中后缀 | 返回值 |
| --- | --- | --- | --- |
| 16 | `a.rar` | `.rar` | true |
| 17 | `a.7z` | `.7z` | true |
| 18 | `a.zip` | `.zip` | true |
| 19 | `a.tar` | `.tar` | true |
| 20 | `a.gz` | `.gz` | true |
| 21 | `a.tgz` | `.tgz` | true | 等价于 `.tar.gz` 的简写后缀，单独识别 |
| 22 | `a.bz2` | `.bz2` | true |
| 23 | `a.tbz2` | `.tbz2` | true | 等价于 `.tar.bz2` 简写 |
| 24 | `a.xz` | `.xz` | true |
| 25 | `a.txz` | `.txz` | true | 等价于 `.tar.xz` 简写 |
| 26 | `a.iso` | `.iso` | true | 光盘镜像，7zz 支持 |
| 27 | `a.cab` | `.cab` | true | Windows 安装包 |
| 28 | `a.lzh` | `.lzh` | true |
| 29 | `a.arj` | `.arj` | true |
| 30 | `a.z` | `.z` | true | Unix compress |
| 31 | `a.lzma` | `.lzma` | true |
| 32 | `a.cpio` | `.cpio` | true |
| 33 | `a.rpm` | `.rpm` | true |
| 34 | `a.deb` | `.deb` | true |
| 35 | `a.dmg` | `.dmg` | true | macOS 磁盘镜像 |
| 36 | `a.wim` | `.wim` | true | Windows 镜像 |
| 37 | `multi.name.with.dots.tar.gz` | `.gz`（最后一段） | true | 复合后缀取最后一段命中 |
| 38 | `multi.name.tar.bz2` | `.bz2` | true | 复合后缀取最后一段命中 |
| 39 | `A.RAR`（全大写） | `.rar`（OrdinalIgnoreCase 比较） | true | 后缀不区分大小写 |
| 40 | `myfile.7Z`（大写 7Z） | `.7z`（IgnoreCase） | true | |
| 41 | `/abs/path/to/file.rar` | 取文件名 `.rar` | true | 传完整路径时 Path.GetFileName 剥离目录 |
| 42 | `相对路径/子目录/archive.zip` | 取文件名 `.zip` | true | 同上 |

**第三类：未命中（返回 false = 不含归档后缀，密码本会补 GUI 扩展名）**

| # | 输入 filename | 为什么没命中 | 返回值 | 密码本处理 |
| --- | --- | --- | --- | --- |
| 43 | `1` | 无任何点号，既无后缀也非分卷 | false | 补 GUI 扩展名 → `1.<ext>` |
| 44 | `myphoto.png` | `.png` 不在单卷后缀表，非分卷模式 | false | 补 GUI 扩展名 → `myphoto.png.<ext>` |
| 45 | `1.txt` | `.txt` 不在表中 | false | 补 GUI 扩展名 → `1.txt.<ext>` |
| 46 | `file.part` | `part` 不在列表；正则需 `part\d+\.rar` 缺数字和 `.rar` 尾 | false | 补 GUI 扩展名 → `file.part.<ext>` |
| 47 | `archive.7zz` | `.7zz` 不在后缀表，不是 7z 数字分卷（缺 `.`+编号的结尾，正则需 `\.7z\.\d+$`） | false | 补 GUI 扩展名 → `archive.7zz.<ext>` |
| 48 | `1.rar.part001` | 后缀顺序反了，正则要求 `part\d+\.rar` 尾 | false | 补 GUI 扩展名 → `1.rar.part001.<ext>` |
| 49 | `1.7z-part01` | 连字符 `-` 不是点号，正则不匹配 | false | 补 GUI 扩展名 |
| 50 | `1.rar1`（缺点号） | 末尾不是 `.rar`，正则也不匹配 | false | 补 GUI 扩展名 → `1.rar1.<ext>` |
| 51 | `.rar`（纯后缀无文件名） | `.rar` 在表中 → **这条要小心**：Path.GetFileName(".rar")==".rar"，EndsWith(".rar") → true → 返回 true。实际中密码本不写纯后缀名 | 边界：true |
| 52 | `空串` | 空文件名 | false | 抛上层处理 |

**第四类：反直觉/边界补充（矩阵 9.1 行 17-21 引用的 case）**

| # | 输入 filename | 命中哪条 | 返回值 | 说明 |
| --- | --- | --- | --- | --- |
| 53 | `1.part.rar`（part 后无数字） | 单卷表 `.rar` 命中 | true | **反直觉 case**：part 后无数字不算 RAR 分卷形态，但最后段是 `.rar` → 单卷规则仍然真。它是"单卷 rar 名里恰好含 .part 字符串"。解压路由也会因 EndsWith(".rar") 走 RAR 引擎 |
| 54 | `1.7z001`（缺点号分隔点） | 正则 2 需 `.7z.`（含点号），`.7z001` 最后段不是 `.7z` 也不满足点号分隔 → 假；单卷表里也没有 `.7z001` → 假 | false | 反例：缺分隔点号不匹配 |
| 55 | `1.zip001`（缺点号分隔点） | 正则 3 需 `.zip.`（含点号）；单卷表也无 `.zip001` | false | 反例：同 #54 |
| 56 | `1.png.rar`（含 PNG 中间名，最后段 `.rar`） | 单卷表 `.rar` 命中 | true | 用户把 PNG 文件包成 rar 的场景。只看最后段后缀 |
| 57 | `1.rar.png`（最后段 `.png`，前面 rar 是中间名） | 最后段 `.png` 不在单卷表；正则 1/2/3 均不命中 | false | 反例——**只看最后段后缀**，不看中间段。即使名字含 rar 也不算归档 |

> 排查提示：如果发现某项目"明明是分卷却被补了扩展名"，优先核对 46-50 这种"形式上接近分卷但缺字符/顺序不对/分隔符不是点号"的情况。

#### `ResolveActualFirstVolume(string logicalPath)`

逻辑名 → 实际首卷，含大小写不敏感查找。

```
输入：逻辑路径（如 1.rar、1.7z、1.7z.003、1.zip.002、1.part02.rar）
输出：实际首卷路径，或 null

1. File.Exists(logicalPath) → 返回 logicalPath

2. dir = Path.GetDirectoryName(logicalPath)
   targetName = Path.GetFileName(logicalPath)

3. 大小写不敏感查找整卷（或精确分卷名）：
   在 dir 下 Directory.EnumerateFiles
   找文件名不区分大小写等于 targetName 的文件：
     if (string.Equals(Path.GetFileName(f), targetName, OrdinalIgnoreCase))
       return f

4. 识别目标属于哪种分卷模式或单卷，取 ext / baseName：
   var m = Match(targetName, IgnoreCase)
     \.part(?<num>\d+)\.rar$  → kind=RarPart, ext="rar", baseName=前缀
     \.7z\.(?<num>\d+)$       → kind=SevenZipNumeric, ext="7z", baseName=前缀
     \.zip\.(?<num>\d+)$      → kind=ZipNumeric, ext="zip", baseName=前缀
     否则 kind=None, ext = Path.GetExtension.Trim('.').ToLower(),
          baseName = Path.GetFileNameWithoutExtension

   对 m.Success 的三种分卷模式，baseName = targetName.Substring(0, m.Index)

5. 按 ext / 分卷模式，在 dir 下不区分大小写扫描找编号最小的首卷：
   ext == "rar"：
     构造正则 ^Regex.Escape(baseName)\.part(\d+)\.rar$（IgnoreCase）
     扫描目录，对所有匹配文件取编号最小者 → 返回
     （RAR 同时兼容 "name.part1.rar"、"name.part01.rar"、"name.part001.rar"——
       正则统一抓编号值，取编号最小即可）
   ext == "7z"：
     构造正则 ^Regex.Escape(baseName)\.7z\.(\d+)$（IgnoreCase）
     扫描目录，编号最小者 → 返回
   ext == "zip"：
     构造正则 ^Regex.Escape(baseName)\.zip\.(\d+)$（IgnoreCase）
     扫描目录，编号最小者 → 返回
   其它后缀（tar/gz/iso 等）：
     无分卷格式，返回 null

6. 都找不到 → null
```

注意：
- baseName 必须 `Regex.Escape`（文件名含 `+`、`(`、`[` 等元字符时不被当作正则语法）
- `Path.GetExtension` 对 `1.7z.003` 只返回 `.001`，所以**禁止直接用它对数字分卷取主扩展名**，必须先走分卷正则匹配

反例：`1.7z.003` 的正确解析
- 错误路径（旧）：ext = Path.GetExtension → `.001` → `ext == "7z"` 不成立 → 返回 null ✗
- 正确路径（新）：第4步匹配 `\.7z\.\d+$` → kind=SevenZipNumeric, ext="7z", baseName="1"
  第5步拼正则 `^1\.7z\.(\d+)$` → 命中 `1.7z.001`（编号最小）→ 返回 ✓

##### ResolveActualFirstVolume：全量步骤级示例（30 条，按 6 个步骤分支）

前置假设：所有示例均在 `/data` 目录下执行，ResolveActualFirstVolume 收到的 logicalPath = `/data/<filename>`。
返回列写"结果文件"即返回对应的绝对路径 `/data/<file>`。

**A. 第 1 步 File.Exists(logicalPath) 精确命中（直接返回，不做后续步骤）**

| # | 输入 logicalPath 文件名 | 磁盘实际文件 | 第 1 步结果 | 返回 | 说明 |
| --- | --- | --- | --- | --- | --- |
| A1 | `1.rar` | `1.rar` | 存在 | `1.rar` | 整卷精确命中 |
| A2 | `1.part01.rar` | `1.part01.rar` | 存在 | `1.part01.rar` | RAR 分卷首卷精确命中 |
| A3 | `1.part02.rar` | `1.part02.rar` | 存在 | `1.part02.rar` | RAR 非首卷精确命中（后续 IsMultiVolumeArchive 会重定向） |
| A4 | `1.7z.001` | `1.7z.001` | 存在 | `1.7z.001` | 7z 数字分卷首卷精确命中 |
| A5 | `1.7z.003` | `1.7z.003` | 存在 | `1.7z.003` | 7z 非首卷精确命中（后续重定向） |
| A6 | `1.zip.002` | `1.zip.002` | 存在 | `1.zip.002` | ZIP 非首卷精确命中（后续重定向） |
| A7 | `my.archive.tar.gz` | `my.archive.tar.gz` | 存在 | `my.archive.tar.gz` | 复合后缀整卷精确命中 |

**B. 第 1 步不存在 → 第 3 步大小写不敏感精确查找（targetName 完全相同、大小写不同的单一文件命中）**

| # | 输入 logicalPath 文件名 | 磁盘实际文件 | 第 3 步匹配数量 | 返回 | 说明 |
| --- | --- | --- | --- | --- | --- |
| B1 | `a.rar` | `A.RAR` 一个 | 唯一匹配 | `A.RAR` | 大小写整卷匹配 |
| B2 | `1.part01.rar` | `1.PART01.RAR` 一个 | 唯一匹配 | `1.PART01.RAR` | 大小写 RAR part 首卷匹配 |
| B3 | `1.7z.001` | `1.7Z.001` 一个 | 唯一匹配 | `1.7Z.001` | 大小写 7z 数字分卷首卷匹配 |
| B4 | `1.zip.003` | `1.ZIP.003` 一个 | 唯一匹配 | `1.ZIP.003` | 大小写 ZIP 非首卷匹配（后续重定向） |
| B5 | `1.rar` | `1.RAR` 和 `1.Rar` 两个（大小写变体并存） | 2 个匹配 → 歧义 | 见 D1 类 | 歧义分支，D 类详述 |

**C. 第 1、3 步都不存在 → 第 4 步识别类型 + 第 5 步扫描目录找编号最小匹配（分卷场景）**

逻辑前提：File.Exists 失败 + 大小写精确查找也没到。现在执行第 4 步匹配 targetName 看是哪种分卷 or 单卷，然后第 5 步用 baseName 拼正则扫目录，抓编号最小的文件。

| # | 输入 logicalPath 文件名 | 第 4 步匹配结果 ext/baseName | 磁盘实际文件（同目录下） | 第 5 步扫到的匹配 | 返回（编号最小） | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| C1 | `1.rar` | kind=None, ext=`rar`, base=`1` (因为第 4 步分卷正则都不命中 → kind=None → 回退 Path.GetExtension) | `1.part01.rar, 1.part02.rar, 1.part03.rar`（没有 1.rar） | ext=`rar` → 正则 `^1\.part(\d+)\.rar$` → 抓回 [part01, part02, part03] | `1.part01.rar` | 典型场景：用户密码本写 `1.rar`，但实际是一组 RAR part 分卷（老版 RAR 常见习惯） |
| C2 | `1.7z` | kind=None, ext=`7z`, base=`1` | `1.7z.001, 1.7z.002, 1.7z.003` | 正则 `^1\.7z\.(\d+)$` → 3 卷 | `1.7z.001` | 用户写 `1.7z` 实际分 7z 分卷 |
| C3 | `1.zip` | kind=None, ext=`zip`, base=`1` | `1.zip.001, 1.zip.002` | 正则 `^1\.zip\.(\d+)$` → 2 卷 | `1.zip.001` | 用户写 `1.zip` 实际分 ZIP 分卷 |
| C4 | `1.part02.rar`（**第 4 步先命中分卷**！） | kind=RarPart, ext=`rar`, base=`1`（m.Index 是 `.part02.rar` 起点，前面就是 `1`） | `1.part01.rar, 1.part02.rar, 1.part03.rar` | `^1\.part(\d+)\.rar$` → 3 卷 | `1.part01.rar` | 第 1 步 File.Exists 假设这里失败（比如大小写/目标不同），所以走到第 5 步 |
| C5 | `1.7z.003`（数字分卷命中） | kind=SevenZipNumeric, ext=`7z`, base=`1` | 假设 File.Exists 失败（实际目录是大写 7Z：`1.7Z.001, 1.7Z.002, 1.7Z.003`），第 3 步也不命中（文件名大小写不一致 + 第 3 步精确 OrdinalIgnoreCase 其实会命中 1.7z.003↔1.7Z.003？对！OrdinalIgnoreCase 是逐字符忽略大小写比较 → "1.7z.003" vs "1.7Z.003" → 相等 → 这类情况在第 3 步就返回，不会到第 5 步。C5 列一个真正到这里的反例：目录有 `project.7z.001` 等，但用户密码本写的是 `1.7z` 而非 `project.7z`——那情况 C2 已覆盖 | 略 | 见 C2 |
| C6 | `name.with+special[chars].rar` | kind=None, ext=`rar`, base=`name.with+special[chars]`（用 Path.GetFileNameWithoutExtension 回退） | `name.with+special[chars].part01.rar ~ .part03.rar` | 正则必须 `Regex.Escape("name.with+special[chars]")` → `name\.with\+special\[chars\]` → `.part(\d+)\.rar$` → 拼回正确正则才能匹配 3 卷 | `name.with+special[chars].part01.rar` | **重点**：反例！如果忘记 Regex.Escape，`+` 和 `[chars]` 会被当作正则语法 → 一个都匹配不到 → 返回 null → MissingEntries 假阳性。这就是为什么文档反复强调 Regex.Escape。 |
| C7 | `1.rar`（目录根本没有分卷，也没整卷） | kind=None, ext=`rar`, base=`1` | 无任何相关文件 | 扫不到匹配 | null | 最后返回 null |
| C8 | `1.tar` | kind=None, ext=`tar`, base=`1` | 无任何相关（tar 没有分卷模式） | ext=tar 不在 rar/7z/zip → 第 5 步直接返回 null | null | tar 没有分卷识别能力（只有 rar/7z/zip 三种有分卷格式）→ 找不到整卷就是 null |
| C9 | `1.rar`（位数混合：1 和 01 和 001 并存） | kind=None, ext=`rar`, base=`1` | `1.part1.rar, 1.part001.rar` | 正则抓：part1（编号=1）、part001（编号=1，**同编号不同位数**） → 编号最小都是 1 → 取第一个扫到的？需明确策略 | 取**编号值最小者，若同编号则取文件名字典序最小者**作为稳定结果 | 真实环境少见，但旧版 RAR 遗留库中可能出现 |
| C10 | `1.rar`（目录中只有非首卷：`1.part02.rar`、`1.part03.rar`、没 part01） | kind=None, ext=`rar`, base=`1` | 只有 part02 part03 | 编号最小值 = 2 → 返回 `1.part02.rar` | `1.part02.rar` | **注意**：ResolveActualFirstVolume 只负责"按编号找最小卷"，不负责完整性。如果 part01 不存在，最小编号就是 part02，会返回它。后续 `IsMultiVolumeArchive` 会试图找"首卷=编号 1 或位数补齐 1 的文件"——找不到——会不重定向，直接按该文件送解压，RAR 引擎本身会因为缺 part01 报错（失败分支处理）。该流程是设计允许的：先尽量定位实体，再让引擎报错，比 MissingEntries 更有信息量。 |

**D. 歧义/边界类**

| # | 输入 logicalPath 文件名 | 磁盘实际文件 | 阶段 | 处理 | 返回 |
| --- | --- | --- | --- | --- | --- |
| D1 | `1.Rar`（**不存在的第三种变体**，故意选一个磁盘上没有的大小写形态） | 同时存在 `1.rar` 和 `1.RAR`（Linux 可同时存在两个文件！） | 第 1 步 File.Exists("1.Rar") → false 没命中；第 3 步大小写不敏感查 targetName="1.Rar" → 扫到 2 个匹配：`1.rar` 和 `1.RAR` → 歧义 | 记录日志"文件名大小写歧义：候选 [1.rar, 1.RAR]，跳过该条目" | 返回 null 并记录歧义（推荐：歧义时直接跳过避免解压错文件，由用户删除多余文件后重试。**不能用第 1 步来触发这个歧义——第 1 步只要输入对应文件存在就直接返回整卷，不会进第 3 步！**） |
| D2 | `1.rar` | 存在整卷 `1.rar` **和** 一组 `1.part01~03.rar` | 第 1 步 File.Exists("1.rar") 成功 → 直接返回整卷 `1.rar` | `1.rar` | 整卷优先。如果 `1.rar` 不存在才走 C1 抓分卷。这是"先整卷后分卷"的顺序。 |
| D3 | `1.7z.003`（第 1 步精确不存在，但第 3 步大小写找到 `1.7Z.003`） | `1.7Z.001 ~ 1.7Z.003` | 第 3 步精确匹配文件名 "1.7z.003" vs `1.7Z.003`（OrdinalIgnoreCase 相等 → 单一匹配 → 返回） | `1.7Z.003` | 精确大小写匹配优先于分卷扫描。之后 `IsMultiVolumeArchive("1.7Z.003")` 识别非首卷 → 重定向到 `1.7Z.001`，OK |
| D4 | logicalPath 只是文件名，无目录（参数允许传 bare name 吗） | — | 第 2 步 `Path.GetDirectoryName("")` → `""`，Directory.EnumerateFiles("") 会用 当前工作目录 | 直接扫当前目录 | 当前实现：Directory 空串抛？ → 实现层需对空 dir 做保护（文档记录：建议 caller 总是传绝对路径；服务端入口统一做 Path.Combine 保证是绝对路径） |

**E. 错误 / 需要兜底**

| # | 场景 | 当前处理 | 排查点 |
| --- | --- | --- | --- |
| E1 | baseName 为空（如文件名是纯 `.part01.rar`） | `Substring(0, 0)` → `""` → 正则 `^.part(\d+)\.rar$` → `^` 后直接 `.part...` 仍合法，但可能误匹配。建议：baseName 空串时跳过该分卷识别 | 日志应打 baseName 为空的情况 |
| E2 | 匹配文件数极大（百万级目录） | Directory.EnumerateFiles + 正则是线性扫描，建议在诊断里打印扫描耗时日志 | 大目录性能问题 → 上层可考虑先做扩展名单文件过滤 |
| E3 | 权限不足（目录不可读） | `Directory.EnumerateFiles` 抛 `UnauthorizedAccessException` → 必须在 ResolveActualFirstVolume 外层 try-catch，吞掉并返回 null + 记录"读取目录失败"错误日志 | 不要让该异常向上冒泡，否则整批密码本加载中断 |
| E4 | 磁盘符号链接 / 挂载点指向其他卷 | File.Exists / Directory.EnumerateFiles 都跟符号链接走 → 可能递归？需明确：不额外做链接检测（跟原生 .NET 行为一致） | |

> 排查提示：如果"明明有分卷却报 MissingEntries"，优先用 E1-E4 反查 baseName 是否空、是否权限、是否符号链接；其次用 C6 反查是否漏 Regex.Escape、是否 `Regex.Escape(baseName)` 确实执行。

#### `MatchesGuiExtension(string path, string extension)`

```
按 extension 分派（全部用 IgnoreCase）：
  rar：后缀 == .rar，或匹配 \.part\d+\.rar$
  7z： 后缀 == .7z，或匹配 \.7z\.\d+$
  zip：后缀 == .zip，或匹配 \.zip\.\d+$
  其它（tar/gz/bz2/iso 等）：后缀 == .<extension>，不识别分卷
返回 bool
```

##### MatchesGuiExtension：全量示例（28 条）

用途：解压目录扫描时、密码本诊断统计时，判断"某文件是否匹配 GUI 选的扩展名"（含 GUI 扩展名的分卷形式都算匹配）。

**A. GUI 选 rar**

| # | path 文件名 | extension=`rar` | 命中哪条 | 返回 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | `a.rar` | rar | 后缀 `.rar` 相等 | true | 单卷 RAR |
| 2 | `A.RAR` | rar | `.RAR` IgnoreCase 相等 | true | 大小写不敏感 |
| 3 | `a.part01.rar` | rar | 匹配 `\.part\d+\.rar$` | true | RAR 首卷 part 分卷 |
| 4 | `a.part999.rar` | rar | 匹配 | true | RAR 任意编号 part 分卷 |
| 5 | `a.PART02.RAR` | rar | IgnoreCase 匹配正则 | true | 全大写 RAR part 分卷 |
| 6 | `a.part` | rar | 既不 `.rar` 结尾也不含 `part\d+\.rar` 尾 | false | 缺数字和 `.rar` 尾 → 不匹配 |
| 7 | `a.rar1` | rar | 结尾不是 `.rar` | false | 缺点号 |
| 8 | `a.7z` | rar | 后缀 `7z`≠`rar`，也不匹配 rar 正则 | false | 不同格式交叉不匹配 |

**B. GUI 选 7z**

| # | path 文件名 | extension=`7z` | 命中哪条 | 返回 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 9 | `a.7z` | 7z | 后缀相等 | true | 单卷 7z |
| 10 | `a.7Z` | 7z | IgnoreCase 相等 | true | 大小写 |
| 11 | `a.7z.001` | 7z | 匹配 `\.7z\.\d+$` | true | 7z 数字分卷首卷 |
| 12 | `a.7z.025` | 7z | 匹配 | true | 7z 数字分卷中间 |
| 13 | `a.7z.1` | 7z | 匹配（1 位编号） | true | 1 位编号也识别 |
| 14 | `a.7z`（实际内容是 zip？文件内容不在考虑——本函数只看名字） | 7z | 后缀相等 | true | 不读内容，只判定名字 |
| 15 | `a.7z001`（缺点号） | 7z | 后缀 `.7z001`≠`.7z`，正则需 `.7z.` 点号 | false | 缺分隔点，不匹配 |
| 16 | `a.rar` | 7z | 交叉 | false | 不同格式 |

**C. GUI 选 zip**

| # | path 文件名 | extension=`zip` | 命中哪条 | 返回 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 17 | `a.zip` | zip | 后缀相等 | true | 单卷 ZIP |
| 18 | `a.ZIP` | zip | IgnoreCase | true | |
| 19 | `a.zip.001` | zip | 匹配 `\.zip\.\d+$` | true | ZIP 数字分卷首卷 |
| 20 | `a.zip.999` | zip | 匹配 | true | |
| 21 | `a.zip001`（缺点号） | zip | 正则需 `.zip.` 分隔 | false | 缺点号分隔符 |
| 22 | `a.tar` | zip | 交叉 | false | |

**D. GUI 选其它（tar / gz / iso…）——只匹配单卷后缀，不识别任何分卷模式**

| # | path 文件名 | extension | 命中哪条 | 返回 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 23 | `a.tar` | `tar` | 后缀 `.tar` 相等 | true | 单卷 tar OK |
| 24 | `a.TAR.GZ`（按扩展名 tar，不是 gz） | `tar` | 最后一段是 `.GZ`，但比较是 `extension=tar` → `.tar.gz` 的 Path.GetExtension 是 `.gz` 不等于 `.tar` → 不匹配。注：Path.GetExtension 拿最后一段。如果 GUI 选 tar 且文件名 a.tar.gz → 最后段 gz → 视为"扩展名不是 tar" | false | 提示：如果希望 `a.tar.gz` 在 GUI 选 tar 时也匹配，需要扩展 MatchesGuiExtension 对 tar/tgz/tbz2/txz 做复合后缀识别（文档里先记此 case，实现时决定） |
| 25 | `a.tgz` | `tar` | 最后段 `.tgz`≠`.tar` → 不匹配 | false | 同上，复合简写后缀需单独列入或做别名 |
| 26 | `a.iso` | `iso` | 后缀相等 | true | |
| 27 | `a.gz.001`（非 rar/7z/zip 外的格式写了数字分卷） | `gz` | 非三格式 → 只看单卷后缀；最后段 `.001`≠`.gz` | false | **重要！** MatchesGuiExtension 对非三格式不识别任何分卷！非三格式分卷不会被 GUI 扫描出来，必须手动加到列表 |
| 28 | `a.txt` | `iso` | 交叉 | false | |

> 排查提示：如果 GUI 选某扩展名却发现"目录扫描没扫到我预期的分卷"，先查是不是 D27 场景（非 rar/7z/zip 分卷在目录扫描阶段不被识别），不是 bug 而是设计边界——非三格式分卷没有通用命名规则，不做猜测。

#### `IsVolumeSequenceContiguous(IReadOnlyList<string> volumes)`

分卷连续性检查（新增）。

```
输入：GetAllVolumeFiles 返回的卷列表
输出：bool

逻辑：
  1. 从卷列表提取所有编号，按编号值升序排序
  2. 期望编号序列：从最小编号到最大编号，步进 1
  3. 检查列表中是否包含每个期望编号
     - 编号比较按"编号值"进行，不按字符串
  4. 任一缺失 → 返回 false
  5. 全部存在 → 返回 true

示例：
  volumes = [part01, part03]（缺 part02）
  编号 [1,3]
  期望 [1,2,3]
  缺 2 → 返回 false

  volumes = [part01, part02, part03]
  编号 [1,2,3]
  期望 [1,2,3]
  全部存在 → 返回 true
```

##### IsVolumeSequenceContiguous：边界示例（22 条）

| # | volumes（GetAllVolumeFiles 返回，按文件名排序，但这里提取编号即可） | 提取编号 | min 编号 / max 编号 | 期望序列 | 是否连续 | 返回值 | 说明 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | [part01, part02, part03] | [1,2,3] | 1→3 | [1,2,3] | 是 | true | 完美完整 3 卷 |
| 2 | [part01, part03] | [1,3] | 1→3 | [1,2,3] 缺 2 | 否 | false | 典型中间缺卷 |
| 3 | [001, 003]（7z 或 zip，只有编号没 part 字样） | [1,3] | 1→3 | 缺 2 | 否 | false | 编号比较和分卷名字无关，只看数字值 |
| 4 | [001] | [1] | 1→1 | [1] | 是 | true | 单卷（只有一卷"编号最小=最大"，天然连续） |
| 5 | [002, 003, 004]（只发现非首卷 001 不存在磁盘中） | [2,3,4] | 2→4 | [2,3,4] | 是 | true | **重要！** 连续性只看 min→max 之间是否连续，**不会因为"缺了 min 之前编号（如 001）就判 false"**。缺首卷属于"首卷不存在"问题，靠 IsMultiVolumeArchive 首卷定位失败 + 解压循环非首卷跳过双重兜底，不靠连续性检查判 |
| 6 | [1, 2, 3]（1 位数） | [1,2,3] | 1→3 | 连续 | 是 | true | 位数不齐但编号值连续 — OK |
| 7 | [001, 02, 0003]（编号宽度不一致） | [1,2,3] | 1→3 | 连续 | 是 | true | 宽度不齐 OK，按数值比较 |
| 8 | [001, 003, 004, 005] | [1,3,4,5] | 1→5 | 缺 2 | 否 | false | 中间一个缺口 |
| 9 | [001, 002, 004, 005, 006] | [1,2,4,5,6] | 1→6 | 缺 3 | 否 | false | |
| 10 | [002, 004]（min=2, max=4） | [2,4] | 2→4 | 缺 3 | 否 | false | min 之前缺 1 不关心，但 2→4 缺 3 → 不连续 |
| 11 | [100, 101, 102]（从中间大编号开始） | [100,101,102] | 100→102 | 连续 | 是 | true | 编号起点随意，只看区间 |
| 12 | [0001, 0002, 0003]（4 位数编号） | [1,2,3] | 1→3 | 连续 | 是 | true | |
| 13 | volumes 长度=0（空表传入） | [] | —（min/max 不存在） | — | 视为"没有分卷信息，单卷场景" | true（或调用方应在 IsMultiVolumeArchive 判非分卷后跳过连续性检查） | 边界：建议调用方先判 `volumes.Count<=1 → 直接视为连续`，不进本函数 |
| 14 | volumes 长度=1（只有一卷） | [5] | 5→5 | [5] | 是 | true | 单卷永远连续 |
| 15 | [part01, PART02, Part03]（大小写混，编号都是 1-3） | [1,2,3] | 1→3 | 连续 | 是 | true | 编号抓取正则应 IgnoreCase |
| 16 | RAR 兼容："name.part1.rar, name.part001.rar" 同编号不同宽度并存 | [1,1] | 1→1 | 有重复（同编号 2 个文件） | **未定义，调用方应在 GetAllVolumeFiles 去重或报告歧义** | 推荐：GetAllVolumeFiles 返回时按编号分组，同编号出现多个文件 → 记录"同编号多分卷文件歧义"日志并跳过（避免解压错卷） |
| 17 | [001, 002, 003, 010]（1-10 缺 4-9） | [1,2,3,10] | 1→10 | 中间缺 4,5,6,7,8,9 大量 | 否 | false | 跳跃不连续 |
| 18 | 负数编号？（正则抓不到非 0-9，不会出现） | — | — | — | — | 前提：编号正则 `\d+` 严格只抓数字 |
| 19 | 超大编号（part9999999999.rar） | [9999999999] | — | 单卷 | 是 | true | 按 int64 解析 |
| 20 | [.7z.001, .7z.002, .7z.003]（baseName 为空，文件名是纯 ".7z.001"） | [1,2,3] | 1→3 | 连续 | 是 | true | 抓到编号即可，baseName 空不影响连续性判断本身 |
| 21 | [part01.rar, part02.rar, other.rar]（扫描正则漏了过滤，混入非分卷文件） | 抓不到编号，可能抛或返回空 | — | — | 调用方 GetAllVolumeFiles 必须严格按 baseName + 分卷格式抓，不应把无关文件扫进来 | 边界：把"抓不到编号的文件"直接过滤出 volumes 列表，或严格正则保证只进匹配文件 |
| 22 | 前导零大小不一致 + 缺中：[0001, 02, 004] | [1,2,4] | 1→4 缺 3 | 否 | false | 按数值比较不受前导零影响 |

> 排查提示：
> - 报"分卷不连续却明明所有卷都在磁盘上" → 优先查 #16（同编号不同宽度导致编号重复出现或未被抓）、#21（混进无关文件）
> - 报"首卷不存在但连续性检查仍然 true" → 不是 bug，是 #5 的设计（连续性不管 min 之前有没有缺；缺首卷靠 IsMultiVolumeArchive 的首卷候选找不到来判）

---

## 三、分卷中间缺失处理

### 处理原则

**分卷中间缺失时，跳过该项目，不解压、不移动、不删除，写入日志。**

### 处理位置

在密码本加载阶段（[第115-140行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L115-L140)）和解压循环开头（[第602行附近](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L602)）都做检查。

#### 密码本加载阶段

在分卷首卷重定向后、文件存在性检查后，增加连续性检查：

```
if (IsMultiVolumeArchive(fullPath, out _))
  var volumes = GetAllVolumeFiles(fullPath)
  if (!IsVolumeSequenceContiguous(volumes))
    日志："分卷不连续，已跳过：X（缺少 ...）"
    continue   // 不进 MissingEntries，不计 RequestedCount
```

#### 解压循环开头（兜底）

防止用户手动编辑列表绕过加载阶段检查：

```
对每个待解压文件：
  if (IsMultiVolumeArchive(archivePath, out _))
    var volumes = GetAllVolumeFiles(archivePath)
    if (!IsVolumeSequenceContiguous(volumes))
      日志："分卷不连续，已跳过：X"
      IgnoreCount++
      continue
```

### 连续性检查逻辑

```
IsVolumeSequenceContiguous(IReadOnlyList<string> volumes)

1. 从 volumes 提取所有卷的编号，按升序排序
2. 期望编号序列：从最小编号到最大编号，步进 1
3. 检查每个期望编号是否都在 volumes 中
4. 任一缺失 → false
5. 全部存在 → true

示例：
  volumes = [part01, part02, part03]
  编号 [1,2,3]
  期望 [1,2,3]
  → true

  volumes = [part01, part03]
  编号 [1,3]
  期望 [1,2,3]
  缺 2 → false
```

---

## 四、解压数据流示例

### 示例 1：密码本 `1`，GUI 选 `rar`，实际 `1.part01.rar`~`1.part03.rar`

```
文件名"1"不含归档后缀 → 补全 1.rar
ResolveActualFirstVolume:
  File.Exists(1.rar) → false
  大小写不敏感查找 1.rar → 不存在
  按后缀 rar 找分卷首卷：
    正则 ^1\.part(\d+)\.rar$（IgnoreCase）
    扫描目录 → 匹配 1.part01.rar、1.part02.rar、1.part03.rar
    编号最小 → 1.part01.rar
  → 返回 1.part01.rar
判定分卷：1.part01.rar 是首卷本身 → 不重定向
File.Exists(1.part01.rar) → true
连续性检查：
  GetAllVolumeFiles → [1.part01.rar, 1.part02.rar, 1.part03.rar]
  编号 [1,2,3] 期望 [1,2,3] → 连续
进待解压列表
解压：实际后缀 rar → 路由到 RAR
后处理：取整组卷 → [1.part01.rar, 1.part02.rar, 1.part03.rar] → 整组删除/移动
```

### 示例 2：密码本 `1.rar`，GUI 选 `7z`，实际 `1.rar` 存在

```
文件名"1.rar"含归档后缀 → 原样 1.rar
ResolveActualFirstVolume:
  File.Exists(1.rar) → true → 返回 1.rar
判定分卷：不是分卷 → 单卷
File.Exists(1.rar) → true
连续性检查：不适用（单卷）
进待解压列表
解压：实际后缀 rar → 路由到 RAR → 解压
```

### 示例 3：密码本 `1`，GUI 选 `7z`，实际 `1.rar` 存在

```
文件名"1"不含归档后缀 → 补全 1.7z
ResolveActualFirstVolume:
  File.Exists(1.7z) → false
  大小写不敏感查找 1.7z → 不存在
  按后缀 7z 找分卷首卷：
    正则 ^1\.7z\.(\d+)$（IgnoreCase）
    扫描目录 → 无匹配
  → null
fullPath 保持 1.7z
File.Exists(1.7z) → false
→ 日志"未找到 1.7z（也不是 7z 分卷首卷）"
→ MissingEntries
```

### 示例 4：密码本 `1.part02.rar`，GUI 选 `rar`，实际 `1.part01.rar`~`1.part03.rar`

```
文件名"1.part02.rar"含归档后缀 → 原样 1.part02.rar
ResolveActualFirstVolume:
  File.Exists(1.part02.rar) → true → 返回 1.part02.rar
判定分卷：识别为 RAR 分卷，非首卷 → 重定向到 1.part01.rar
File.Exists(1.part01.rar) → true
连续性检查：
  GetAllVolumeFiles → [1.part01.rar, 1.part02.rar, 1.part03.rar]
  编号 [1,2,3] 期望 [1,2,3] → 连续
进待解压列表
解压：实际后缀 rar → 路由到 RAR
后处理：取整组卷 → [1.part01.rar, 1.part02.rar, 1.part03.rar] → 整组删除/移动
```

### 示例 5：密码本 `1.png.rar`，GUI 选 `rar`，实际 `1.png.rar` 存在

```
文件名"1.png.rar"含归档后缀 → 原样 1.png.rar
ResolveActualFirstVolume:
  File.Exists(1.png.rar) → true → 返回 1.png.rar
判定分卷：后缀 rar 但文件名不含 .partNN → 不是分卷 → 单卷
File.Exists(1.png.rar) → true
进待解压列表
解压：实际后缀 rar → 路由到 RAR → 解压
```

### 示例 6：密码本 `1.png`，GUI 选 `rar`，实际 `1.png.rar` 存在

```
文件名"1.png"不含归档后缀 → 补全 1.png.rar
ResolveActualFirstVolume:
  File.Exists(1.png.rar) → true → 返回 1.png.rar
判定分卷：不是分卷 → 单卷
File.Exists(1.png.rar) → true
进待解压列表
解压：实际后缀 rar → 路由到 RAR → 解压
```

### 示例 7：解压目录扫描，GUI 选 `rar`，目录有 `a.rar`、`b.7z`、`c.zip`

```
扫描过滤（isForDecompression=true）：
  a.rar → 后缀匹配 rar → 进列表
  b.7z → 不匹配 → 跳过
  c.zip → 不匹配 → 跳过
列表只有 a.rar
解压 a.rar
```

### 示例 8：解压目录扫描，GUI 选 `rar`，目录有 `a.part01.rar`~`a.part03.rar`、`b.7z.001`

```
扫描过滤（isForDecompression=true）：
  a.part01.rar ~ a.part03.rar → 匹配 rar 分卷格式 → 进列表
  b.7z.001 → 不匹配 rar → 跳过
解压循环：
  a.part01.rar → 首卷 → 连续性检查通过 → 解压
  a.part02.rar → 非首卷 → 跳过
  a.part03.rar → 非首卷 → 跳过
```

### 示例 9：解压目录扫描，GUI 选 `zip`，目录有 `a.zip.001`~`a.zip.003`

```
扫描过滤（isForDecompression=true）：
  a.zip.001 ~ a.zip.003 → 匹配 zip 分卷格式 → 进列表
解压循环：
  a.zip.001 → 首卷 → 连续性检查通过 → 解压（路由到 7zz）
  a.zip.002 → 非首卷 → 跳过
  a.zip.003 → 非首卷 → 跳过
后处理：取整组卷 → [a.zip.001, a.zip.002, a.zip.003] → 整组删除/移动
```

### 示例 10：密码本 `1.rar`，GUI 选 `rar`，文件不存在，无分卷

```
文件名"1.rar"含归档后缀 → 原样 1.rar
ResolveActualFirstVolume:
  File.Exists(1.rar) → false
  大小写不敏感查找 1.rar → 不存在
  按后缀 rar 找分卷首卷 → 无匹配
  → null
fullPath 保持 1.rar
File.Exists(1.rar) → false
→ 日志"未找到 1.rar（也不是 rar 分卷首卷）"
→ MissingEntries
```

### 示例 10b：密码本 `1.tar`，GUI 选 `rar`，实际 `1.tar` 存在

```
文件名"1.tar"含归档后缀 → 原样 1.tar
ResolveActualFirstVolume:
  File.Exists(1.tar) → true → 返回 1.tar
判定分卷：后缀 tar 不是 rar/7z/zip → 非分卷 → 单卷
File.Exists(1.tar) → true
进待解压列表
解压：实际后缀 tar → 非 rar → 路由到 7zz → 解压 ✓
```

### 示例 10c：密码本 `1`，GUI 选 `tar`，实际 `1.tar` 存在

```
文件名"1"不含归档后缀 → 补全 1.tar
ResolveActualFirstVolume:
  File.Exists(1.tar) → true → 返回 1.tar
判定分卷：非分卷 → 单卷
File.Exists(1.tar) → true
进待解压列表
解压：实际后缀 tar → 非 rar → 路由到 7zz → 解压 ✓
```

### 示例 11：分卷中间缺失

密码本 `1.rar`，GUI 选 `rar`，实际只有 `1.part01.rar`、`1.part03.rar`（缺 `1.part02.rar`）：

```
文件名"1.rar"含归档后缀 → 原样 1.rar
ResolveActualFirstVolume:
  File.Exists(1.rar) → false
  按后缀 rar 找分卷首卷：
    正则 ^1\.part(\d+)\.rar$
    扫描目录 → 匹配 1.part01.rar、1.part03.rar
    编号最小 → 1.part01.rar
  → 返回 1.part01.rar
判定分卷：1.part01.rar 是首卷本身 → 不重定向
File.Exists(1.part01.rar) → true
连续性检查：
  GetAllVolumeFiles → [1.part01.rar, 1.part03.rar]
  编号 [1,3] 期望 [1,2,3] 缺 2 → 不连续
→ 日志"分卷不连续，已跳过：1.part01.rar（缺少 1.part02.rar）"
→ continue（不进 MissingEntries，不进待解压列表）
→ 不解压、不移动、不删除
```

### 示例 12：Linux 大小写，密码本 `a.rar`，实际 `A.RAR`

```
文件名"a.rar"含归档后缀 → 原样 a.rar
ResolveActualFirstVolume:
  File.Exists("sourceFolder/a.rar") → false（Linux 区分大小写）
  大小写不敏感查找：
    Directory.EnumerateFiles(dir)
    Path.GetFileName("A.RAR") OrdinalIgnoreCase equals "a.rar" → true
    → 返回 "sourceFolder/A.RAR"
fullPath = "sourceFolder/A.RAR"
判定分卷：不是分卷 → 单卷
File.Exists("sourceFolder/A.RAR") → true
进待解压列表
解压：Path.GetExtension("A.RAR") → ".RAR" → ArchiveFormat = "RAR"
路由：IsRarFormat("RAR") → true → 走 RAR ✓
```

### 示例 13：Linux 大小写，密码本 `1`，GUI 选 `rar`，实际 `1.PART01.RAR`~`1.PART03.RAR`

```
文件名"1"不含归档后缀 → 补全 1.rar
ResolveActualFirstVolume:
  File.Exists("sourceFolder/1.rar") → false
  大小写不敏感查找 1.rar → 不存在
  按后缀 rar 找分卷首卷：
    正则 ^1\.part(\d+)\.rar$（IgnoreCase）
    扫描目录 → 匹配 1.PART01.RAR、1.PART02.RAR、1.PART03.RAR
    编号最小 → 1.PART01.RAR
  → 返回 "sourceFolder/1.PART01.RAR"
判定分卷：识别为 RAR 分卷首卷 → 不重定向
File.Exists → true
连续性检查：
  GetAllVolumeFiles：
    正则 ^1\.part(\d+)\.rar$（IgnoreCase）
    扫描目录 → 匹配 3 个文件
    按编号排序 → [1.PART01.RAR, 1.PART02.RAR, 1.PART03.RAR]
  编号 [1,2,3] 期望 [1,2,3] → 连续
进待解压列表
解压：路由到 RAR ✓
后处理：取整组卷 → [1.PART01.RAR, 1.PART02.RAR, 1.PART03.RAR] → 整组删除/移动 ✓
```

### 示例 14：密码本 `1.7z.003`（非首卷），GUI 选 `7z`，实际 `1.7z.001`~`1.7z.003`

```
文件名"1.7z.003"：
  HasArchiveExtension 命中 \.7z\.\d+$ → true → 原样使用
ResolveActualFirstVolume("sourceFolder/1.7z.003")：
  1. File.Exists("1.7z.003") → true → 直接返回 "1.7z.003"
IsMultiVolumeArchive("1.7z.003")：
  匹配 ^(?<base>.+\.7z)\.(?<number>\d+)$ → base="1.7z", number=003, 位数=3
  首卷候选："1.7z.001"，File.Exists → true
  → 非首卷！重定向到 firstVolumePath="1.7z.001"
File.Exists("1.7z.001") → true
连续性检查：
  GetAllVolumeFiles("1.7z.001") → [1.7z.001, 1.7z.002, 1.7z.003]
  编号 [1,2,3] 期望 [1,2,3] → 连续
进待解压列表（fullPath="1.7z.001"）
解压：Path.GetExtension="001"？不，ArchiveFormat 赋值走文件实际后缀优先——
     IsRarArchive("1.7z.001") 不匹配；IsSevenZipArchive 匹配 \.7z\.\d+$ → true
     → 路由到 7zz ✓
后处理：GetAllVolumeFiles → [1.7z.001, 1.7z.002, 1.7z.003] → 整组删除/移动 ✓
```

### 示例 15：密码本 `1.zip.002`（非首卷），GUI 选 `zip`，实际 `1.zip.001`、`1.zip.002`

```
文件名"1.zip.002"：
  HasArchiveExtension 命中 \.zip\.\d+$ → true → 原样使用
ResolveActualFirstVolume("sourceFolder/1.zip.002")：
  1. File.Exists("1.zip.002") → true → 直接返回 "1.zip.002"
IsMultiVolumeArchive("1.zip.002")：
  匹配 ^(?<base>.+\.zip)\.(?<number>\d+)$ → base="1.zip", number=002, 位数=3
  首卷候选："1.zip.001"，File.Exists → true
  → 非首卷！重定向到 firstVolumePath="1.zip.001"
File.Exists("1.zip.001") → true
连续性检查：
  GetAllVolumeFiles("1.zip.001") → [1.zip.001, 1.zip.002]
  编号 [1,2] 期望 [1,2] → 连续
进待解压列表（fullPath="1.zip.001"）
解压：IsSevenZipArchive("1.zip.001") 匹配 \.zip\.\d+$ → true
     → 路由到 7zz ✓
后处理：GetAllVolumeFiles → [1.zip.001, 1.zip.002] → 整组删除/移动 ✓
```

### 示例 16：密码本 `1.7z.003`，磁盘缺首卷，只有 `1.7z.002`~`1.7z.003`（Linux 大小写场景叠加）

```
密码本：1.7z.003，GUI：7z
实际磁盘：1.7Z.002、1.7Z.003（大小写不同，且缺 001）
HasArchiveExtension 匹配 \.7z\.\d+$ → true → 原样 1.7z.003
ResolveActualFirstVolume("sourceFolder/1.7z.003")：
  1. File.Exists("1.7z.003") → false（Linux 区分大小写，实际是 1.7Z.003）
  3. 大小写不敏感精确查 "1.7z.003"：
     Directory.EnumerateFiles 找到 "1.7Z.003" → OrdinalIgnoreCase 相等 → 返回它
→ fullPath = "1.7Z.003"
IsMultiVolumeArchive("1.7Z.003")：
  匹配 ^(?<base>.+\.7z)\.(?<number>\d+)$（IgnoreCase）→ base="1.7Z"，number=3
  首卷候选 "1.7Z.001" → File.Exists → false！
  → 返回 true（名字是分卷形态，不保证首卷存在；调用方用 File.Exists(firstVolumePath) 判是否可解）
     out firstVolumePath = "1.7Z.001"（即使不存在）
File.Exists("1.7Z.003") → true
连续性检查：
  IsMultiVolumeArchive（忽略首卷存在性，只看是不是分卷模式）
  → 是 SevenZipNumeric 分卷
  GetAllVolumeFiles：
    正则 ^1\.7z\.(\d+)$（IgnoreCase）
    扫描目录 → 匹配 1.7Z.002、1.7Z.003
    编号 [2,3]，期望 [2,3]（min 到 max）→ 注意：连续性检查是 min→max 步进1
      缺 1 属于"起始之前缺卷"，不在 [min,max] 范围内，IsVolumeSequenceContiguous 返回 true
→ 连续性通过，但该项目本身非首卷（003）！
→ 解压循环开头首卷过滤：IsMultiVolumeArchive + 非首卷 → continue + IgnoreCount++（或警告）
```

> 注：示例 16 叠加了三个问题——大小写不匹配、非首卷输入、首卷文件缺失——最终由 IsMultiVolumeArchive 首卷定位失败 + 解压循环非首卷跳过 双重兜底处理，不会把 003 当作单卷送进 7zz。

---

## 五、解压改动清单

| 位置 | 改动 |
| --- | --- |
| [ArchiveEngineRouter.cs 第42-65行](file:///Users/x/code/Google-compress/Core/Services/ArchiveEngineRouter.cs#L42-L65) | **解压路由反转**：rar 走 RAR，其它（7z/zip/tar/gz 等）走 7zz；新增 `IsRarArchive`/`IsRarFormat` |
| [BatchOperationService.cs 第40-89行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L40-L89) | 解压目录扫描加 `isForDecompression=true`，按 GUI 扩展名过滤（含分卷格式） |
| [BatchOperationService.cs 第115-140行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L115-L140) | 密码本解析：含归档后缀原样、不含归档后缀补 GUI 扩展名；逻辑名→实际首卷（含大小写不敏感查找）；分卷连续性检查 |
| [BatchOperationService.cs 第244-285行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L244-L285) | 分卷判定按文件实际后缀分派，加 `zip`，移除 `extension` 参数，正则 IgnoreCase，其它后缀视为单卷 |
| [BatchOperationService.cs 第602行附近](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L602) | 解压循环开头：分卷连续性兜底检查 |
| [BatchOperationService.cs 第888-933行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L888-L933) | 取整组卷改为正则扫描目录，按文件实际后缀分派，加 `zip`，移除 `extension` 参数，正则 IgnoreCase，收集所有匹配分卷（不提前终止） |
| 新增函数（解压） | `ResolveActualFirstVolume`（含大小写不敏感）、`MatchesGuiExtension`（非 rar/7z/zip 只匹配单卷）、`IsVolumeSequenceContiguous`（分卷连续性检查） |
| 新增函数（通用） | `HasArchiveExtension`（扩展后缀列表） |

---

## 六、解压回归测试用例

| 用例 | 输入 | 期望行为 |
| --- | --- | --- |
| 单卷 RAR | 密码本 `1.rar`，实际 `1.rar` | 解压 `1.rar`，路由到 RAR，后处理单文件 |
| 单卷 7z | 密码本 `1.7z`，实际 `1.7z` | 解压 `1.7z`，路由到 7zz |
| 单卷 ZIP | 密码本 `1.zip`，实际 `1.zip` | 解压 `1.zip`，路由到 7zz |
| 单卷 TAR | 密码本 `1.tar`，实际 `1.tar` | 解压 `1.tar`，路由到 7zz |
| 单卷 GZ | 密码本 `1.tar.gz`，实际 `1.tar.gz` | 解压 `1.tar.gz`，路由到 7zz |
| RAR 分卷完整 | 密码本 `1.rar`，实际 `1.part01.rar`~`1.part03.rar` | 解压首卷，后处理整组 |
| 7z 分卷完整 | 密码本 `1.7z`，实际 `1.7z.001`~`1.7z.003` | 解压首卷，后处理整组 |
| ZIP 分卷完整 | 密码本 `1.zip`，实际 `1.zip.001`~`1.zip.003` | 解压首卷，后处理整组 |
| RAR 分卷缺失 | 密码本 `1.rar`，实际 `1.part01.rar`、`1.part03.rar` | 跳过，日志提示缺 `1.part02.rar` |
| 7z 分卷缺失 | 密码本 `1.7z`，实际 `1.7z.001`、`1.7z.003` | 跳过，日志提示缺 `1.7z.002` |
| 密码本不含点 | 密码本 `1`，GUI 选 `rar`，实际 `1.rar` | 补全 `1.rar`，解压 |
| 密码本不含点+分卷 | 密码本 `1`，GUI 选 `rar`，实际 `1.part01.rar` | 补全 `1.rar`，找分卷首卷，解压 |
| 密码本含归档后缀不匹配 GUI | 密码本 `1.rar`，GUI 选 `7z`，实际 `1.rar` | 按实际后缀解压 `1.rar`（走 RAR） |
| 密码本含归档后缀不匹配 GUI（tar） | 密码本 `1.tar`，GUI 选 `rar`，实际 `1.tar` | 按实际后缀解压 `1.tar`（走 7zz） |
| 密码本不含归档后缀不匹配实际 | 密码本 `1`，GUI 选 `7z`，实际 `1.rar` | 补全 `1.7z`，找不到，MissingEntries |
| 多段文件名 | 密码本 `1.png.rar`，GUI 选 `rar` | 原样，解压 `1.png.rar` |
| 解压目录扫描过滤 | GUI 选 `rar`，目录 `a.rar`、`b.7z`、`c.zip` | 只处理 `a.rar` |
| 解压目录扫描分卷混放 | GUI 选 `rar`，目录 `a.part01.rar`~`a.part03.rar`、`b.7z.001` | 只处理 `a.part01.rar` |
| 解压目录扫描 tar | GUI 选 `tar`，目录 `a.tar`、`b.rar` | 只处理 `a.tar`，路由到 7zz |
| Linux 大小写整卷 | 密码本 `a.rar`，实际 `A.RAR` | 大小写不敏感查找，解压 `A.RAR` |
| Linux 大小写分卷 | 密码本 `1`，GUI 选 `rar`，实际 `1.PART01.RAR`~`1.PART03.RAR` | 大小写不敏感查找分卷，解压 |
| 文件不存在 | 密码本 `1.rar`，无任何文件 | MissingEntries + 日志 |
| 非首卷密码本 | 密码本 `1.part02.rar`，实际 `1.part01.rar`~`1.part03.rar` | 重定向到首卷，解压，后处理整组 |
| 非首卷 7z 数字分卷 | 密码本 `1.7z.003`，实际 `1.7z.001`~`1.7z.003` | HasArchiveExtension 原样；IsMultiVolumeArchive 重定向到 `1.7z.001`；路由 7zz；整组后处理 |
| 非首卷 ZIP 数字分卷 | 密码本 `1.zip.002`，实际 `1.zip.001`、`1.zip.002` | HasArchiveExtension 原样；重定向到 `1.zip.001`；路由 7zz；整组后处理 |
| 7z 分卷缺首卷 + 大小写 | 密码本 `1.7z.003`，实际 `1.7Z.002`、`1.7Z.003`（Linux，缺 001） | ResolveActualFirstVolume 大小写不敏感命中 003；IsMultiVolumeArchive 首卷候选 001 不存在→不重定向；非首卷过滤跳过或警告 |
| Linux 大小写 7z 数字分卷首卷匹配 | 密码本 `1.7z.001`，实际 `1.7Z.001`~`1.7Z.003` | ResolveActualFirstVolume 第 3 步精确大小写命中，返回 `1.7Z.001`；路由 7zz；GetAllVolumeFiles 忽略大小写抓回 3 卷；整组处理 |

---

## 七、不受影响的部分

- `ArchiveOptions.ArchiveFormat` 赋值逻辑（仍按文件实际后缀）
- `PasswordUtility.GetPasswordSourceName`（已能还原 `1.part01.rar` → `1.rar`）

---

## 八、对其他 AI 意见的分析与采纳

### 意见1：路由示例存在逻辑错误（**不成立，基于误解**）

该 AI 认为：文件 `1.tar` + GUI 选 `rar` 时，`IsRarFormat(options.ArchiveFormat)` 仍为真，会误走 RAR 后端。

**这是误解**。`options.ArchiveFormat` 不是 GUI 扩展名，而是文件实际后缀优先。见 [BatchOperationService.cs 第661-669行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L661-L669)：

```csharp
ArchiveFormat = Path.GetExtension(archivePath).TrimStart('.') is { Length: > 0 } actualFormat
    ? actualFormat          // 文件有后缀 → 取文件后缀
    : options.Extension;    // 文件无后缀 → 才回退到 GUI 扩展名
```

对 `1.tar`：`Path.GetExtension` 返回 `.tar`，`ArchiveFormat = "tar"`，`IsRarFormat("tar") = false`，走 7zz。不会误走 RAR。

只有文件**无后缀**时（如文件名就是 `1`），`ArchiveFormat` 才回退到 GUI 扩展名。此时用户选 `rar` → 走 RAR，是用户明确意图，合理。

该 AI 建议的"三层路由（实际后缀优先 → 其它可识别后缀 → GUI 兜底）"与现有方案等价，因为 `ArchiveFormat` 已经是文件后缀优先。不采纳。

### 意见2：分卷完整性检查只能检查中间缺卷（**正确，采纳**）

原设计的分卷完整性检查通过编号序列只能发现中间缺失（如 `001、003` 缺 `002`），**无法发现末尾缺失**（如本应有 `part04` 但不存在）。

**处理**：
- 函数命名 `IsVolumeSequenceContiguous`（编号连续性检查），明确语义只检查连续性
- 末尾缺卷依靠解压返回码：RAR/7zz 解压到末尾发现缺卷会返回失败，现有 `ArchiveResult.Success == false` 逻辑会处理（[第602行附近的失败分支](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L602)）
- 即：编号连续性检查（前置，跳过明显残缺）+ 解压返回码（后置，处理末尾缺卷）= 完整覆盖

### 意见3：大小写不敏感查找存在歧义（**正确，采纳**）

Linux 可能同时存在 `a.rar` 和 `A.RAR`，`ResolveActualFirstVolume` 找到第一个就返回，有歧义。

**处理**：`ResolveActualFirstVolume` 改为收集所有不区分大小写匹配的文件：
- 唯一匹配 → 使用
- 多个匹配 → 记录"文件名大小写歧义"，跳过该项目
- 无匹配 → 继续找分卷首卷（同样对分卷首卷做唯一匹配检查）

### 意见4：统一成 ArchiveVolumeResolver（**正确，采纳**）

`IsMultiVolumeArchive`、`ResolveActualFirstVolume`、`GetAllVolumeFiles`、`IsVolumeSequenceContiguous` 都各自解析文件名，长期维护容易规则分叉。

**处理**：把新增函数重组为 `ArchiveVolumeResolver` 类，一次解析返回所有信息：

```
class ArchiveVolumeResolver
{
    static VolumeResolveResult Resolve(string archivePath)
}

class VolumeResolveResult
{
    VolumeKind Kind              // None / RarPart / SevenZipNumeric / ZipNumeric
    string ActualExtension       // 从文件名捕获的实际后缀（rar/7z/zip/tar/...）
    string BaseName              // 不含编号部分的基础名
    string FirstVolumePath       // 首卷路径（可能就是 archivePath 本身）
    IReadOnlyList<string> AllVolumes  // 整组卷（按编号升序）
    IReadOnlyList<int> MissingNumbers  // 缺失的中间编号
    bool HasCaseAmbiguity        // 是否存在大小写歧义
}
```

`IsMultiVolumeArchive`、`GetAllVolumeFiles`、`IsVolumeSequenceContiguous`、`ResolveActualFirstVolume` 都改为委托 `ArchiveVolumeResolver.Resolve`，确保规则单一来源。`HasArchiveExtension` 和 `MatchesGuiExtension` 是后缀判定，与分卷解析无关，保持独立函数。

---

## 九、函数级全场景用例矩阵

> 排查定位用：快速从"函数 × 输入形态"查期望输出和反例。每行都对应上文中某一条详细示例。

**缩写说明**
- S=单卷；R=RAR part 分卷；S7=7z 数字分卷；SZ=ZIP 数字分卷；-=不适用；✓=正确/真；✗=错误/假

### 9.1 HasArchiveExtension（输入 filename 字符串 → bool）

| 行 | 输入 filename | 命中哪条规则 | 期望返回 | 对映上文 |
| --- | --- | --- | --- | --- |
| 1 | `1.rar` | 单卷表 rar | true | HasArchiveExtension 单卷表例 |
| 2 | `1.RAR` | IgnoreCase rar | true | #大小写 |
| 3 | `1.7z` | 单卷表 7z | true | |
| 4 | `1.zip` | 单卷表 zip | true | |
| 5 | `1.tar` | 单卷表 tar | true | |
| 6 | `1.tgz` | 单卷表 tgz | true | |
| 7 | `1.tbz2` | 单卷表 tbz2 | true | |
| 8 | `1.part01.rar` | 正则 `\.part\d+\.rar$` | true | 分卷模式 1 |
| 9 | `1.PART999.RAR` | IgnoreCase 正则 1 | true | 分卷模式 1 大写 |
| 10 | `1.7z.001` | 正则 `\.7z\.\d+$` | true | 分卷模式 2 |
| 11 | `1.7z.9999` | 正则 2（任意长数字） | true | 编号宽度无关 |
| 12 | `1.ZIP.003` | 正则 3 `\.zip\.\d+$` IgnoreCase | true | 分卷模式 3 |
| 13 | `1.zip.1`（1 位） | 正则 3 | true | 1 位编号 |
| 14 | `1.txt` | 全不命中 | false | 非归档反例 |
| 15 | `1.rar1`（缺点） | 最后段 `.rar1` 不在单卷表，正则也不命中 | false | 反例 |
| 16 | `1.part`（缺数字+rar） | 正则 1 缺 d+ 和 `.rar` | false | 反例 |
| 17 | `1.part.rar`（part 无数字） | 单卷表 rar 命中 → true！注意！part 后无数字不算 RAR 分卷形态，但是它最后段是 `.rar` 所以**单卷规则仍然真**——它是"单卷 rar 名里恰好有 part 字符串" | true（单卷规则） | 重要反直觉 case |
| 18 | `1.7z001`（缺点号分隔点） | 正则 2 需 `.7z.`，`.7z001` 最后段不是 7z，也不满足点号分隔 → 假；单卷表里也没有 `.7z001` → 假 | false | 反例 |
| 19 | `1.zip001`（缺点） | 同理 正则 3 需 `.zip.`；单卷也无 `.zip001` | false | 反例 |
| 20 | `1.png.rar`（含 PNG 中间后缀的复合名，最后段 `.rar`） | 单卷表 rar → 真 | true | 用户把 PNG 包成 rar 场景；详细见 #56 |
| 21 | `1.rar.png`（最后段 `.png`，前面 rar 是中间名） | 最后段 `.png` 不在单卷表；正则 1/2/3 均不命中 | false | 反例——**只看最后段后缀**，不看中间段 |
| 22 | `archive（无后缀名本身叫 archive）` | 无后缀，单卷表全不命中，正则不命中 | false | 反例 |
| 23 | `1.cab` / `1.lzh` / `1.arj` / `1.iso` / `1.gz` / `1.bz2` / `1.xz` / `1.txz` | 单卷表存在对应后缀 | true | 单卷全表覆盖断言 |

### 9.2 ResolveActualFirstVolume（输入逻辑路径 → 实际首卷路径字符串 / null）

> 函数返回：首卷路径；找不到任何匹配文件 → 抛或返回 null（语义：返回 null 表示无法解析，调用方跳过）。

| 行 | 输入 logicalPath | 磁盘上实际文件（简化：同目录下列文件名） | 期望返回 firstVolume | 对映上文示例类 |
| --- | --- | --- | --- | --- |
| 1 | `1.rar` | [1.rar] | `1.rar`（A1 精确命中） | A 类 |
| 2 | `1.rar` | [1.RAR]（mac 不敏感场景/Windows） | `1.RAR`（B1 单卷忽略大小写） | B 类 |
| 3 | `1.rar` | [a.rar, b.rar]（没有 1.* 任何东西） | null（C7 全不命中） | C7 |
| 4 | `1.part01.rar`（RAR 分卷输入自身就是首卷） | [1.part01, 1.part02, 1.part03] | `1.part01.rar`（A2） | A |
| 5 | `1.part02.rar`（输入非首卷） | [1.part01~03] | `1.part01.rar`（C4 非首卷重定向） | C4 |
| 6 | `1.part02.rar` + **缺 part01 只有 02/03** | [1.part02, 1.part03] | `1.part02.rar`（A3 第 1 步 File.Exists 精确命中，直接返回，不做首卷判定） | A3 |
| 7 | `1.rar`（密码本逻辑名单卷），磁盘实际是分卷 `1.part01~03.rar` | [1.part01,1.part02,1.part03] | `1.part01.rar`（C1 整卷→分卷首卷推断） | C1 |
| 8 | `1.rar`（逻辑单卷）+ 磁盘有分卷但**只有 part02/03 缺首卷 part01** | [part02, part03] | `1.part02.rar`（C10 按编号最小者返回，不因缺首卷返回 null） | C10 |
| 9 | `1.rar`（逻辑单卷），磁盘**同时**有：[1.rar（单卷存在）、1.part01/02/03（分卷也存在）] | 单卷+分卷都存在 | **返回逻辑单卷本身 `1.rar`**（D2 整卷优先：第 1 步就精确命中→直接返回整卷，不再推断分卷首卷——避免"逻辑写单卷、磁盘刚好有分卷，却莫名其妙把整卷忽略"的用户困惑） | D2；排查：如果你想让它**优先用分卷**就不要在磁盘上放同名单卷，或把单卷移走 |
| 10 | `1.7z`（逻辑单卷），磁盘 [1.7z.001/002/003]（只有 7z 分卷，没有单卷 1.7z） | 只分卷存在 | `1.7z.001`（C2 7z 推断） | C2 |
| 11 | `1.7z.002`（输入 7z 非首卷） | [001,002,003] | `1.7z.001` | C 类 |
| 12 | `1.zip.005`（输入 zip 非首卷） | [001-010] | `1.zip.001` | C 类 |
| 13 | Linux 大小写：逻辑 `a.Rar`（**故意选磁盘上不存在的第三种大小写变体**，否则第 1 步 File.Exists(a.rar) 直接命中就不会到歧义判定），磁盘实际 **两个大小写不同但拼写同**的独立文件都存在：`a.rar`、`A.RAR` | 第 1 步 File.Exists("a.Rar")=false → 第 3 步大小写不敏感查到 2 个匹配 | 结果有歧义→返回 null 并记"大小写同名歧义"日志（B5→D1） | B5/D1 |
| 14 | `1.part001.rar`（3 位编号），磁盘实际是 2 位：`1.part01.rar`, `1.part02.rar` | 编号位数不匹配 + baseName 同 | 首卷候选拼 `1.part001`（3 位）但磁盘不存在；再走 D 类推断：找 `^\.part(\d+)\.rar$` 同 baseName `1` 最小编号卷 → 发现 `part01=1` → 返回 `1.part01.rar`（若允许跨位数，推荐这样做；若严格同位数则返回 null。**文档推荐：允许按最小编号推断，不受位数宽度约束**，因为 7z 用户常常手动改名位数不齐） | 实现决策点 |
| 15 | `项目.rar`（中文+带点 `我的 项目.v2.rar`） | 对应首卷文件存在 | 返回磁盘实际路径（Regex.Escape 处理点号、空格等特殊字符） | C6 基础成功 |

### 9.3 IsMultiVolumeArchive（path → bool + out firstVolumePath）

| 行 | path | 识别格式？ | 编号抓取 | 返回值 bool | out firstVolumePath（候选，可能磁盘不存在） |
| --- | --- | --- | --- | --- | --- |
| 1 | `1.part01.rar` | R（是） | 01→1 | true | `1.part01.rar`（当前就是首卷） |
| 2 | `1.part02.rar` | R（是） | 02→2 | true | `1.part01.rar`（补首卷，可能实际不存在） |
| 3 | `1.7z.003` | S7（是） | 003→3 | true | `1.7z.001`（可能不存在） |
| 4 | `1.zip.005` | SZ（是） | 005→5 | true | `1.zip.001`（可能不存在） |
| 5 | `1.rar`（单卷） | N（否，后缀 rar 但没有 partNN.rar） | — | false | 自身 path |
| 6 | `1.7z`（单卷） | N | — | false | 自身 path |
| 7 | `1.tar` | N | — | false | 自身 path |
| 8 | `.part01.rar`（空 baseName） | R（是） | 01→1 | true | `.part01.rar` |
| 9 | `1.7z.001`（但实际是单卷压缩内容错误写成 .7z.001） | 仍然按名字识别：S7 是 | 001→1 | true | `1.7z.001`（名字是分卷就算；内容由引擎判定） |

### 9.4 GetAllVolumeFiles（archivePath → List<string> 卷路径列表，按编号升序）

| 行 | archivePath（后缀决定分派） | 同目录下其它文件 | 期望抓取 volumes（按编号升序，只抓 baseName 相同的同组分卷） | 返回 Count |
| --- | --- | --- | --- | --- |
| 1 | `1.part02.rar`（base=`1` R 分派） | `1.part01/02/03.rar`, `2.rar`, `note.txt` | [part01, part02, part03] | 3 |
| 2 | `1.7z.002`（base=`1` S7 分派） | `1.7z.001/002/003`, `1.7z`（单卷 7z，后缀是 7z 不匹配 NNN） | 只抓 [001,002,003]，单卷 `1.7z` 不进 | 3 |
| 3 | `a.zip.010`（base=`a` SZ 分派） | `a.zip.001~010`，`a.zip`（单卷 zip） | 只抓 001..010 | 10 |
| 4 | `1.rar`（S 单卷，不识别分卷分派） | 任意 | 空列表（单卷不扫） | 0 |
| 5 | `1.tar`（非三格式） | 任意 | 空列表 | 0 |
| 6 | `a.part03.rar`（只中缺） | `a.part01.rar`, `a.part03.rar`（缺 part02） | 抓取 [part01, part03] | 2（不自动补缺失，连续性检查随后判） |
| 7 | 分派 R，但同组文件编号位数不齐：`a.part1/part2/.../part9/part10`（1-2 位混合） | 10 个 | 全部抓，返回按**编号值**升序：[part1, part2, …, part9, part10]（part10 放最后，正确） | 10 |
| 8 | 同编号不同宽度并存冲突：`a.7z.1`, `a.7z.001`（都=1） | 2 个同编号 | 返回 2 个都进（随后 GetAllVolumeFiles 内部检测到同编号冲突→调用方跳过或报错，而不是静默消去） | 2（冲突） |

### 9.5 IsVolumeSequenceContiguous（IReadOnlyList<string> volumes → bool）

| 行 | volumes（文件名列表，从 GetAllVolumeFiles 返回） | 提取编号集合 | min/max | 期望序列 | 是否连续 | 返回 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | [part01, part02, part03] | [1,2,3] | 1→3 | [1,2,3] | 是 | true |
| 2 | [part01, part03] | [1,3] | 1→3 | [1,2,3] 缺 2 | 否 | false |
| 3 | [001, 003]（S7/SZ 编号） | [1,3] | 1→3 缺 2 | 否 | false |
| 4 | 只有一卷（单分卷）[005] | [5] | 5→5 | 连续 | true | true |
| 5 | **只存在 part02, part03（缺首卷 01 但 2-3 连续）** | [2,3] | 2→4？不：min=2 max=3 | 期望 [2,3]→是；**min 之前的 1 不存在不影响连续性（连续性只看 min..max）** → 真（缺首卷由 File.Exists(firstVolumePath) 单独判） | true | true |
| 6 | part01,part10（1 和 10，缺 2-9 大量） | [1,10] | 1→10 | 缺 8 个 | false | false |
| 7 | 同编号重复（part001/part1 并存）→ 该 case 应由 GetAllVolumeFiles 先报冲突，不要进入连续性函数（若强行进入：编号 [1,1]，min/max=1/1 → [1] 连续 → 错误返回 true！所以 **必须前置冲突检测**） | [1,1]（去重前） | 1/1 | 错误判真 | 未定义；前置必做冲突检测 | 建议去重前 fail-fast |

### 9.6 MatchesGuiExtension（path, extension → bool）

| 行 | path（文件名部分） | extension | 分派 | 返回 |
| --- | --- | --- | --- | --- |
| 1 | `a.rar` | `rar` | R | true |
| 2 | `a.part01.rar` | `rar` | R（part 形式） | true |
| 3 | `a.7z` | `7z` | S7（单卷） | true |
| 4 | `a.7z.001` | `7z` | S7（NNN） | true |
| 5 | `a.zip` | `zip` | SZ（单卷） | true |
| 6 | `a.zip.001` | `zip` | SZ（NNN） | true |
| 7 | `a.tar` | `tar` | 其它→严格单卷 | true |
| 8 | `a.tar.gz`（GUI 选 tar） | `tar` | 其它→严格最后段后缀 `.gz`≠tar | false（设计如此；否则加复合后缀别名） |
| 9 | `a.gz.001`（GUI 选 gz） | `gz` | 其它→不识别 NNN 分卷 | false |
| 10 | `a.7z.001`（GUI 选 zip） | `zip` | 交叉格式 | false |

### 9.7 解压路由：IsRarArchive(path) OR IsRarFormat(format) → 走 RAR 还是 7zz

| 行 | path 文件名 | options.ArchiveFormat | 走 RAR？（OR 任真） | 走 7zz？（都假） | 备注 |
| --- | --- | --- | --- | --- | --- |
| 1 | `a.rar` | ""（未传/空） | ✓（EndsWith rar） | → RAR | |
| 2 | `a.part01.rar` | "" | ✓（正则） | → RAR | |
| 3 | `a.7z` | "" | ✗ | ✓ → 7zz | |
| 4 | `a.7z.001` | "" | ✗ | ✓ → 7zz | |
| 5 | `a.zip.001` | "" | ✗ | ✓ → 7zz | |
| 6 | `a.tar` | "" | ✗ | ✓ → 7zz | |
| 7 | `archive`（无后缀） | `rar`（显式指定） | ✓（IsRarFormat=true） | → RAR | 用户显式指定 |
| 8 | `a.7z`（名是 7z） | `rar`（format=rar override） | ✓（OR 任真：IsRarFormat=true） | → RAR（format 覆盖名字！） | 矛盾 case：format 优先级高于名字吗？当前 OR 实现会 → RAR |
| 9 | `a.rar`（名 rar） | `7z`（format=7z override） | ✓（OR：IsRarArchive=true，虽然 IsRarFormat=7z=false）→ **仍然走 RAR** | → RAR；OR 设计导致 format=7z 对 rar 名文件不生效 | 决策点：format 是否应该覆盖名字（用 format 优先），还是 OR？当前实现是名字优先 |
| 10 | `a.txt`（非归档） | "" | ✗ | ✓ → 7zz（由 7zz 报 header error） | 非归档进入引擎后报错，正常错误流 |

### 9.8 目录扫描：LoadFilesFromFolder（folder, extension, isForDecompression）→ 返回 List<文件路径>

| 行 | GUI extension | isForDecompression | 同目录实际文件（简化） | 命中哪些？ | 返回 Count |
| --- | --- | --- | --- | --- | --- |
| 1 | `rar` | true（解压） | `a.rar`, `a.part01/02/03`, `b.7z`, `c.zip.001` | `a.rar`+a.part01/02/03（rar 系列） | 4（后续首卷过滤只保留 a.rar、a.part01，2 项实际处理） |
| 2 | `7z` | true（解压） | `b.7z`, `b.7z.001/002/003`, `a.rar` 系列 | `b.7z`+001/002/003 | 4 |
| 3 | `zip` | true（解压） | `c.zip`, `c.zip.001/002/003` + 其他 | 只 c.zip 系列 | 4 |
| 4 | `tar` | true（解压） | `d.tar`, `d.tar.gz`, `e.gz.001` | 只 `d.tar`（严格单卷 *.tar，不识别 tar.gz 复合/不识别 gz NNN 卷） | 1 |
| 5 | 任意 `rar` | false（压缩模式，不加扩展名过滤） | 全部文件 21 个（集合 S 共 22 行，含 1 个目录 subdir/，排除后 22 - 1 = 21 个真正文件） | 除目录全进 | 21 |
| 6 | `rar` | true（解压），子目录 `sub/2.rar` 嵌套 | `1.rar`（顶层） + `sub/2.rar`（子目录，TopDirectoryOnly 不进） | 只顶层 `1.rar`（**只扫一层**，设计如此） | 1 |

> 本节用于快速查"输入→输出"。当怀疑函数实现和文档不一致时，拿上表任一 case 写单测断言通过即可定位 bug。

---

## 十、附录：40+条交叉场景速查表

> 真正排查用：从"用户现象 → 可能原因 → 对应文档哪一节/哪条 case"快速反查，5 秒内定位是代码 bug 还是设计边界。每条一行，方便 grep。
>
> 每条格式：`[现象编号] 现象描述 → 可能原因 A / 可能原因 B / … → 查：文档哪节 + 哪条示例`

### 10.1 密码本加载阶段（解压前批量预处理阶段出问题）

```
[P001] 密码本写 1.rar，但磁盘实际是 1.part01/02/03.rar，解压失败"找不到 1.rar"
  → ResolveActualFirstVolume：C3（整卷推断分卷首卷）没有把 1.rar 重定向到 1.part01.rar
  → 查：ResolveActualFirstVolume 第 3/4 步"单卷找不到 → 尝试分卷首卷"
[P002] 密码本写 1.part03.rar（非首卷），解压报错"找不到 part03"
  → IsMultiVolumeArchive 未把非首卷重定向到首卷 01
  → 查：IsMultiVolumeArchive #2（非首卷 out 首卷）；ResolveActualFirstVolume C1
[P003] 密码本写 *.7z.003，解压报"没有归档后缀"
  → HasArchiveExtension 正则 2 `\.7z\.\d+$` 未命中？（常见 bug：漏写反斜杠 \d 写成 d）
  → 查：HasArchiveExtension 分卷模式 2；正反例 #10
[P004] 密码本写 *.zip.001，解压报"没有归档后缀"
  → HasArchiveExtension 正则 3 `\.zip\.\d+$` 漏
  → 查：正反例 #12
[P005] 密码本写 1.png.rar（含 PNG 中间名），解压说"没找到 1.png"（把中间名当无后缀看）
  → 密码本解析阶段 HasArchiveExtension 没命中：因为单卷 rar 表没加入 rar？或者取最后段后缀写成用 Path.GetFileNameWithoutExtension 错了？
  → 查：HasArchiveExtension #56（1.png.rar 应该 true）
[P006] 密码本写 `a.Rar`（故意用磁盘上**不存在的第三种大小写变体**，确保第 1 步 File.Exists 失败后进入大小写扫描），Linux 磁盘实际 `a.rar`、`A.RAR` 两个独立文件都存在，解压随机选到哪个不确定，解压内容不一致
  → ResolveActualFirstVolume 大小写匹配没做"唯一性判断"，发现多个都匹配就返回 First()，导致 Linux 下大小写同名歧义
  → 查：ResolveActualFirstVolume B5/D1；意见 3 采纳
[P007] 密码本写 1.rar，磁盘根本不存在 1.*，但是程序继续走解压导致引擎报"文件不存在"（应该在预处理就跳过+写日志）
  → ResolveActualFirstVolume 返回 null 后，密码本加载阶段没有过滤 null，直接丢进解压列表
  → 查：ResolveActualFirstVolume E1-E4 过滤判空
[P008] 分卷密码本：输入 1.part02.rar + 只有 part02/03（缺 01），程序仍然把 1.part01.rar（不存在的）丢进解压引擎
  → IsMultiVolumeArchive 返回 true（形态上是分卷）但首卷候选 File.Exists=false，调用方没有判 File.Exists(firstVolumePath)，直接 out 的值当存在用
  → 查：IsMultiVolumeArchive #16/#17；双重兜底：连续性（min 之间连续，但缺 min 之前的 01 不判假，靠 File.Exists）+ File.Exists 自己判
```

### 10.2 目录扫描阶段（GUI 选目录后没列出预期文件）

```
[P101] GUI 选扩展名 7z + 目录里一堆 .7z.001/002/003，但扫出来为空
  → 原因 A：MatchesGuiExtension 7z 正则没写 `\.7z\.\d+$`（比如漏写成 `\.7z\d+$` 缺中间点号）
  → 查：MatchesGuiExtension B 组 #11；反例 #15
  → 原因 B：LoadFilesFromFolder isForDecompression=true 走过滤但传成 false（变成压缩模式不过滤？不对，压缩模式全进不会空）→ 反过来：isForDecompression=true 时调用方 extension 传成空字符串？→ 所有扩展名都不等于空，全过滤掉
  → 查：目录扫描 A/B/C/D 主匹配条件
[P102] GUI 选 tar，拖入 1.tar.gz 扫描为空（没扫到）
  → MatchesGuiExtension 对 tar 只看最后段后缀；1.tar.gz 的 Path.GetExtension 是 .gz
  → 查：MatchesGuiExtension #24（D24）；目录扫描 D4
[P103] GUI 选 gz，e.gz.001（非标准 gz 分卷）没被扫进来
  → MatchesGuiExtension 非三格式不识别 .NNN 分卷；.gz.001 最后段是 .001 != .gz
  → 查：MatchesGuiExtension D27
[P104] 拖入子目录有分卷，解压目录扫描只拿到顶层（我想递归）
  → 当前只扫一层 SearchOption.TopDirectoryOnly，设计边界，不是 bug
  → 查：目录扫描 G7；如果想递归：GUI 增加"递归子目录"开关后改 SearchOption.AllDirectories + 排除目录循环引用
[P105] 同一 baseName 既有单卷又有分卷（a.rar + a.part01/02/03.rar），解压时只解压了其中一组，另一组内容丢了
  → 目录扫描阶段都扫进 raw 返回，首卷过滤时应该有 2 个首卷（a.rar 是一个，a.part01 是另一个），都应保留。首卷过滤写成按 baseName 去重合并成 1 个导致另一组丢
  → 查：目录扫描 J10（两组首卷应各自保留）；K11a+b 保留 2 项
```

### 10.3 解压路由阶段（"为什么 xxx 后缀跑到 RAR/7zz 引擎？"）

```
[P201] 1.tar 被 RAR 引擎打开报"不是 RAR 归档"
  → 解压路由仍然是旧版"IsSevenZipArchive 命中走 7zz，否则默认走 RAR"→ 旧版 bug；新版应该是 rar 走 RAR，其余 7zz
  → 查：ArchiveEngineRouter 改动；解压路由示例 #12
[P202] 1.rar 被 7zz 引擎打开
  → IsRarArchive 写成了 IsSevenZipArchive（替换时反向写错）
  → 查：解压路由示例 #1；IsRarArchive 单测断言 a.rar=true
[P203] a.7z + ArchiveFormat=rar（代码某处把 format 误写成 rar）→ 程序走 RAR 引擎，用户说"我选的 7z 怎么是 RAR？"
  → 解压路由是 OR 设计：IsRarFormat(format)=true 时无论名字是什么都强制走 RAR；format=rar 来源可能是 Batch 构造 options 时写死 rar？或用户 GUI 选了 rar？
  → 查：解压路由 #18（name 7z + format rar → RAR）；反向追踪 options.ArchiveFormat 赋值
[P204] 用户想让 format 覆盖名字（比如名是 rar 但格式选 zip）→ 仍然走 RAR，format 无效
  → 路由 OR 设计：IsRarArchive(name)=true 就走 RAR，不管 IsRarFormat(format)=false
  → 查：解压路由 #19 / #21；文档中已标记为"设计决策点：format 优先级"
```

### 10.4 分卷首卷/缺失/连续性/冲突

```
[P301] 分卷 part01/part03（缺 part02），程序仍解压 01 解压半截报错
  → 预处理阶段 IsVolumeSequenceContiguous 没调用，或结果 false 时没跳过该项目（只解首卷不管中间缺）
  → 查：IsVolumeSequenceContiguous #2；分卷中间缺失处理原则（跳过不解压不移动不删除）
[P302] 分卷只有 part02/03（缺首卷 01），程序报"分卷不连续"或"缺中间卷"（其实是缺首卷）
  → 连续性检查（IsVolumeSequenceContiguous #5 设计：min=2 max=3，中间 2→3 连续，所以 true）+ 首卷不存在由 File.Exists(firstVolumePath) 报错。当前代码可能把 File.Exists=false 也归类为"不连续"，日志文案误导
  → 查：IsVolumeSequenceContiguous #5；二者应该分开报错文案："首卷不存在"和"分卷中间缺失"
[P303] part01/02/03 齐全但解压时仍然说"缺卷 part04"（末尾缺）
  → 连续性检查 min=1 max=3 判真（#1 真）→ 没问题；末尾缺（应该有 04 但没有）由解压引擎返回码（Success=false）报告，不是连续性检查职责
  → 查：意见 2 采纳（前置连续 + 后置返回码 = 完整）
[P304] 同编号 1 位 3 位并存：a.7z.1 和 a.7z.001（两文件都是编号 1）→ GetAllVolumeFiles 抓到后 part10 排到 part2 前（字符串排序 bug）
  → 原因 A：GetAllVolumeFiles 按文件名排序不是按编号 int 值排序 → #11 排序 bug
  → 查：GetAllVolumeFiles #11；IsVolumeSequenceContiguous 里按 int 比较，但 GetAllVolumeFiles 排序本身也应该按 int（引擎命令行送卷顺序也依赖此排序）
  → 原因 B：同编号 2 卷冲突导致编号值 [1,1] → GetAllVolumeFiles 应做冲突检测 fail-fast
  → 查：GetAllVolumeFiles #16；IsVolumeSequenceContiguous #7（前置检测必须）
[P305] 文件名里有点号（photo.with.dots.part03.rar）→ GetAllVolumeFiles 抓取时把同目录下不同 baseName 的 photowithdots 也抓进来（. 当成通配匹配任意字符）
  → Regex.Escape(baseName) 漏写 → #4 bug
  → 查：GetAllVolumeFiles #4；排查提示第一项
```

### 10.5 后处理（删除/移动残留或误删）

```
[P401] 解压成功后分卷残留 1.part02/03（只删了 part01 自己）
  → GetAllVolumeFiles 调用时 extension 参数未移除（仍传 GUI 扩展名），而实际分卷是另一种格式 → 扫不到同组其它卷 → 只返回 1 条 → 只删 1 条
  → 查：改动位置 5 为什么移除 extension 参数 + 案例；GetAllVolumeFiles 必须按 archivePath 实际后缀，不能传 GUI extension
[P402] 误删了另一组分卷：解压 a.part01/02/03.rar 成功后把同目录下 b.7z.001~010 也全删了
  → GetAllVolumeFiles 正则构造时 Escape(baseName) 漏 + 正则没加 ^ 和 $，导致 `.*part\d+\.rar` 误匹配一切含 part 的 rar（或 baseName 空 .* 吞一切）
  → 查：GetAllVolumeFiles #4 Escape；#17 空 baseName 要 ^$
[P403] 解压失败后分卷仍然被移动/删除（应该失败不动）
  → 后处理逻辑只在 ArchiveResult.Success=true 时才执行 Move/Delete；失败分支写了无论成败都 Move/Delete
  → 查：分卷中间缺失处理 + 解压循环失败分支（不移动不删除）
[P404] 非三格式（tar/iso/gz）单卷后处理：解压成功仍不删/移
  → GetAllVolumeFiles 返回 0 条（#14/#15），后处理逻辑里 volumes.Count==0 分支空操作，没有"单卷自己就是 1 卷"的处理
  → 查：GetAllVolumeFiles #14，后处理 volumes.Count<2 时 fallback 到"归档文件本身就是唯一目标"
```

### 10.6 大小写/跨平台/macOS/Windows 特有

```
[P501] macOS 上 a.rar（小写）和 A.RAR（大写）其实是同一文件（APFS 默认大小写不敏感），但 ResolveActualFirstVolume 扫出来 2 条判歧义，实际是同一个
  → 跨平台区分：APFS 默认 case-insensitive；Linux 一般 case-sensitive；NTFS 可配置。在不敏感文件系统上，应"收集到大小写不同但路径映射到同一文件（同一 FileInfo unique ID）时视为同一个，不算歧义"。或简单方案：如果 OS 是 macOS/Windows，且忽略大小写匹配结果全指向同一真实文件，则去重使用。
  → 查：ResolveActualFirstVolume B5/D1；意见 3 采纳中的"Linux 才判歧义"的平台分支
[P502] Linux 下密码本写 1.rar，实际 1.RAR 和 1.rar 都真存在 → 判歧义跳过，但用户说"我确实想解 1.RAR（大写）"
  → 正确做法：如果出现歧义，预处理把两个都列成 2 个独立项目分别尝试（而不是整组跳过），或者允许密码本精确指定大小写（密码本写 1.RAR 且磁盘有 → ResolveActualFirstVolume 第 1 步 File.Exists("1.RAR") 精确命中 → 不会再查忽略大小写的 B 分支 → 无歧义）
  → 查：ResolveActualFirstVolume A1（精确命中先走）；密码本写精确大小写可规避歧义
[P503] Windows 路径反斜杠 D:\Archives\a.rar 在 mac 上 Directory.EnumerateFiles 炸
  → 扫目录前 Directory.Exists(folder) 判，不存在直接 UI 提示，不抛未捕获异常
  → 查：目录扫描 Q18
```

### 10.7 边界/反直觉（用户以为是 bug 但设计如此）

```
[P601] 密码本写 1.part.rar（part 后无数字）→ HasArchiveExtension 返回 true（"为什么算归档？它不算 RAR 分卷啊？"）
  → 单卷后缀 .rar 命中。它是"单卷 rar，名字恰好含 .part"。不算 RAR part 分卷，但仍是合法单卷归档 → HasArchiveExtension 设计为"单卷表命中 OR 分卷规则命中"，所以真
  → 查：HasArchiveExtension 反直觉 case #17；解压路由 #7（EndsWith .rar 仍走 RAR，RAR 引擎报不是分卷或正常解）
[P602] 1.rar.png（最后段 .png）→ HasArchiveExtension=false，但它实际是 rar 内容被重命名成 png
  → HasArchiveExtension 只看名字不读内容，设计边界
  → 查：HasArchiveExtension #21；非归档名的内容，GUI 拖入文件由文件内容判定不在扫描阶段
[P603] 连续性检查：part02/03 缺首卷 01，居然返回 true（"为什么不是 false？明显缺了 1"）
  → 连续性只查 min..max 区间；缺首卷由 File.Exists 判；二者结合才完整
  → 查：IsVolumeSequenceContiguous #5 + 排查提示第二项
[P604] GUI 选 rar + 同目录有 1.7z.001 也被扫进？
  → 不会，MatchesGuiExtension 按 GUI 扩展名分派 rar 只匹配 .rar / .partNN.rar
  → 查：MatchesGuiExtension A8；目录扫描 A1 返回列不含 7z
[P605] 压缩模式（isForDecompression=false）拖入目录：为什么 .txt / .png 也全进？我只想要归档
  → 设计：压缩模式目录扫描不过滤扩展名，所有文件交给引擎按用户指定 GUI 扩展名合成新归档。用户选 7z → 整个目录全打包 1 个 7z，不是"只打包目录中的归档"（后者没意义，用户不会把归档再二次压缩成同一格式）
  → 查：目录扫描 F6（LoadFilesFromFolder isForDecompression=false 不过滤）
```

### 10.8 macOS 本机/权限/符号链接

```
[P701] /Applications/XXXX.app/Contents/Resources 里的归档：拖入 app bundle → 目录扫描返回 0
  → MyApp.app 是目录（SearchOption.TopDirectoryOnly）；不是 bug，要右键"显示包内容"进入 Contents 再拖 Resources 目录
  → 查：目录扫描 P17
[P702] 归档路径是符号链接指向 SMB/NFS 挂载 → 解压时报 Input/Output error，但扫描能扫到
  → File.Exists 跟符号链接；EnumerateFiles 也会列出；解压引擎真正 Open() 时远端 IO 错误
  → 正常远端问题；不是代码 bug；日志里记录真实 exception 方便用户定位网络问题
[P703] 下载目录里 .7z.001 刚下完但 002 还在 .download（浏览器临时后缀名）→ GetAllVolumeFiles 没把 002 当成分卷（后缀 .download），连续性检查判缺 002，跳过
  → 扫描阶段 .download 后缀不匹配 `\.7z\.\d+$` → 不扫进来。正确！应该等下载完再批量
  → 查：GetAllVolumeFiles #12（额外后缀 .backup 不进）
```

### 10.9 密码本 & 后处理组合（连续 3 阶段交叉）

```
[P801] 密码本：逻辑名 1.rar；磁盘实际：1.part02/03.rar 缺首卷 part01
  → 全链路期望：ResolveActualFirstVolume 推断 part01 → File.Exists(false) → 跳过，日志"首卷不存在 1.part01.rar"
  → 如果实际：解压引擎 part02 被送进去报错 → 首卷过滤失效（非首卷没过滤）
  → 查：ResolveActualFirstVolume C10 + 解压循环首卷过滤（改动位置 4 调用点 #2 638 行）
[P802] 密码本 1.7z.001；磁盘实际 1.7z.001/003（缺 002）
  → 期望：GetAllVolumeFiles 返回 [001,003] → IsVolumeSequenceContiguous=false（缺 2）→ 项目跳过，不动文件 + 日志"分卷不连续，缺失编号 2"
  → 实际如果解了 001 半截失败 → 连续检查 false 跳过逻辑失效
[P803] 密码本 1.zip.001；磁盘实际 1.zip.001~010 齐全；解压成功后 → 期望：GetAllVolumeFiles 返回 10 个 → 后处理 Move/Delete 全 10 个
  → 实际残留 006-010（只删前 5 个）→ GetAllVolumeFiles 扫描时用了 `Take(5)` 或用 `First()` 提前终止（旧版逻辑）→ 新版应"收集所有匹配"不提前终止
  → 查：改动位置 5 改造：收集所有匹配分卷（不提前终止）；旧版残留 bug
[P804] 密码本文件名无后缀：逻辑 1；GUI ext=rar；磁盘实际 1.part01.rar/02/03
  → 期望：HasArchiveExtension("1")=false（无后缀名）→ 密码本解析补 GUI ext → 逻辑名 1.rar → ResolveActualFirstVolume 推断到 1.part01.rar → 流程全通
  → 实际如果说"找不到 1.rar 没有补扩展名"→ 密码本解析阶段 HasArchiveExtension=false 判断后没做补扩展名
  → 查：改动位置 3 密码本加载逻辑 1-4 步
```

> 以上 40+ 条覆盖：常见现象 × 可能原因 × 文档对应位置。排查时从上面任一条 grep 到对应大类，再回查 九、 中的函数断言表和一~八节中的详细示例表。最后写单元测试钉住对应 case 防止回归。
