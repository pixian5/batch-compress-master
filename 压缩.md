# 压缩方案

## 一、核心原则

### 通用原则（与解压共用）

1. **归档后缀识别**：`HasArchiveExtension` 维护常见归档后缀列表（rar/7z/zip/tar/gz/bz2/xz/iso/cab/lzh/arj/tgz/tbz2/txz 等），密码本写文件名含这些后缀之一则原样使用，否则补 GUI 扩展名。
2. **大小写**：Linux 下文件系统大小写敏感，密码本写 `a.rar`、实际文件 `A.RAR` 必须能匹配。

### 压缩模式

3. **压缩路由**：按 GUI 扩展名分派，7z/zip 走 7zz，其它（rar）走 RAR。
4. **压缩目录扫描**：不过滤输入文件（压缩时 GUI 扩展名是输出格式，不用于筛选源文件）。

---

## 二、压缩改动位置

### 1. [ArchiveEngineRouter.cs 第37-39行](file:///Users/x/code/Google-compress/Core/Services/ArchiveEngineRouter.cs#L37-L39)（压缩路由）

**压缩路由不变**：仍按 GUI 扩展名分派。

```
压缩路由逻辑：
  IsSevenZipFormat(options.ArchiveFormat)
    ? _sevenZipEngine.CompressAsync(...)   // 7z/zip 走 7zz
    : _rarEngine.CompressAsync(...)        // 其它（rar）走 RAR

判定函数：
  IsSevenZipFormat(string? format):
    format?.Trim().TrimStart('.').ToLowerInvariant() is "7z" or "zip"
```

效果：
- GUI 选 `7z` 或 `zip` → 走 7zz ✓
- GUI 选 `rar` → 走 RAR ✓

### 2. [BatchOperationService.cs 第40-89行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L40-L89)（压缩目录扫描）

**压缩模式不过滤**：函数签名加 `bool isForDecompression` 参数，压缩模式传 `false`，直接进列表。

```
签名变更：LoadFilesFromFolder(string folder, string extension, bool isForDecompression)

对每个直接子项：
  是目录 → 进列表（压缩场景需要）
  是文件：
    if (isForDecompression)
      // 解压模式：按 GUI 扩展名过滤（见解压.md）
    else
      // 压缩模式：不过滤（GUI 扩展名是输出格式，不筛选输入）
      直接进列表
```

调用点：
- 压缩入口（模式 1 + 模式 2 + 压缩按钮）：传 `isForDecompression=false`

---

## 三、压缩数据流示例

### 示例 C1：压缩目录扫描，GUI 选 `7z`，目录有 `a.txt`、`b.png`、`c.rar`

```
扫描（isForDecompression=false）：
  a.txt → 不过滤 → 进列表
  b.png → 不过滤 → 进列表
  c.rar → 不过滤 → 进列表
列表：[a.txt, b.png, c.rar]
压缩：GUI 选 7z → 路由到 7zz → 分别压缩为 a.txt.7z、b.png.7z、c.rar.7z
```

### 示例 C2：压缩目录扫描，GUI 选 `rar`，目录有 `a.txt`、`b.png`

```
扫描（isForDecompression=false）：
  a.txt → 不过滤 → 进列表
  b.png → 不过滤 → 进列表
列表：[a.txt, b.png]
压缩：GUI 选 rar → 路由到 RAR → 分别压缩为 a.txt.rar、b.png.rar
```

---

## 四、压缩改动清单

| 位置 | 改动 |
| --- | --- |
| [ArchiveEngineRouter.cs 第37-39行](file:///Users/x/code/Google-compress/Core/Services/ArchiveEngineRouter.cs#L37-L39) | 压缩路由不变：7z/zip 走 7zz，其它（rar）走 RAR |
| [BatchOperationService.cs 第40-89行](file:///Users/x/code/Google-compress/Core/Services/BatchOperationService.cs#L40-L89) | 目录扫描加 `isForDecompression` 参数：压缩模式传 `false`，不过滤输入 |

---

## 五、压缩回归测试用例

| 用例 | 输入 | 期望行为 |
| --- | --- | --- |
| 压缩目录扫描不过滤 | GUI 选 `7z`，目录 `a.txt`、`b.png`、`c.rar` | 全部进列表，分别压缩为 .7z |
| 压缩路由 7z | GUI 选 `7z`，源文件 `a.txt` | 路由到 7zz，输出 `a.txt.7z` |
| 压缩路由 zip | GUI 选 `zip`，源文件 `a.txt` | 路由到 7zz，输出 `a.txt.zip` |
| 压缩路由 rar | GUI 选 `rar`，源文件 `a.txt` | 路由到 RAR，输出 `a.txt.rar` |
