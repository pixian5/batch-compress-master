# 当前压缩逻辑

> 本文描述 2026-08-08 当前代码的真实行为，来源是 `BatchOperationService`、`ArchiveEngineRouter`、`WinRarCommandBuilder`、`SevenZipCommandBuilder` 和命令行入口。它只描述“压缩”流程；归档扫描、分卷解析和密码本解压规则以 [`解压.md`](解压.md) 为准。

## 1. 支持范围与不变量

当前压缩只创建三种格式：

| 用户选择的格式 | 后端 | 创建能力 |
| --- | --- | --- |
| `rar` | RAR/WinRAR `rar` | 创建 RAR |
| `7z` | 官方 7-Zip `7zz` | 创建 7z |
| `zip` | 官方 7-Zip `7zz` | 创建 ZIP |

压缩模式有以下固定不变量：

1. 每一个来源文件或目录都是一个独立任务，逐项串行执行，不会把目录中的多个直接子项合并成一个归档。
2. GUI 选择的扩展名是**输出格式**，不会用来筛选来源文件。`a.txt`、`b.png`、`c.rar` 在选择 `7z` 时都可以作为输入。
3. 只有 `rar`、`7z`、`zip` 被路由到对应创建后端；其它格式不会自动降级到 RAR，最终由路由抛出“不支持创建格式”的错误。
4. 路径和密码通过 `ProcessStartInfo.ArgumentList` 逐项传递，不拼接 Shell 命令字符串；空格、中文和大多数特殊字符不会改变参数边界。
5. 压缩成功和“成功后的删除/移动”是两个阶段，后处理失败不会把归档删除，但会单独计入 `PostProcessFailCount`。

核心调用链可以概括为：

```text
收集来源
  -> 过滤系统元数据、空路径和重复项
  -> 逐项检查文件/目录是否存在
  -> 跳过已处理项目
  -> 计算输出目录和输出文件名
  -> 处理已有输出文件策略
  -> 生成密码
  -> 组装 ArchiveOptions 和附件输入
  -> ArchiveEngineRouter 按格式路由
  -> RAR 或 7zz 创建归档
  -> 记录 stdout/stderr 和结果
  -> 成功后删除或移动源
  -> 累计输出大小，必要时停止或关机
```

## 2. 来源收集

### 2.1 GUI 的三种来源模式

GUI 的来源模式由 `SourceMode` 控制：

| `SourceMode` | 界面含义 | 压缩时的读取方式 |
| --- | --- | --- |
| `0` | 解压密码本 | 不能用于压缩；点击压缩会提示切换模式 |
| `1` | 压缩路径清单 | 读取 TXT，每个非空行表示一个文件或目录路径 |
| `2` | 来源目录 | 只扫描目录的直接子项 |

来源列表显示在 GUI 中后，用户可以手工编辑。点击压缩时仍会重新执行空行、系统元数据、存在性等防御性检查，不会盲目相信列表内容。

### 2.2 来源目录模式

`LoadCompressionSourcesFromFolder(folder, skipProcessed)` 使用 `Directory.GetFileSystemEntries` 扫描一层：

- 文件和直接子目录都加入候选列表；不递归进入子目录。
- 不按 `rar`、`7z`、`zip` 或任何其它扩展名过滤。
- 跳过系统元数据路径，以及名称以 `.tmp` 开头的项目。
- `skipProcessed=true` 时，名称包含 `【已压缩】` 或 `【已解压】` 的直接子项被跳过。
- 目录不存在或读取异常时返回空列表并写日志。

系统元数据过滤器会检查路径的每一个片段。典型被过滤的名称包括 `desktop.ini`、`Thumbs.db`、`.DS_Store`、`.directory`、`.Trash-*`、`.nfs*`、`._*`、`~$*` 和锁文件目录等；因此即使手工列表或 TXT 写入这些路径，也不会进入引擎。

示例：

```text
/data/
├── report.txt
├── photo.png
├── old.rar
├── project/              # 直接子目录，作为一个整体输入
├── project【已压缩】/    # skipProcessed=true 时跳过
├── .DS_Store             # 跳过
└── nested/
    └── deep.txt          # 不会单独扫描到
```

此时来源列表是 `report.txt`、`photo.png`、`old.rar`、`project`；`nested/deep.txt` 不会单独成为任务。

### 2.3 压缩路径 TXT

`LoadCompressionPathsFromTextFile` 的格式是“一行一个路径”，它不是解压密码本：

```text
./data/report.txt
./data/project
/Users/x/share/photo.png
```

处理规则：

1. 去除每行首尾空白，空行忽略。
2. 相对路径相对于 TXT 文件所在目录解析；绝对路径原样规范化为绝对路径。
3. 文件或目录存在才加入 `Paths`；不存在的行加入 `MissingEntries` 诊断。
4. 同一路径不区分大小写去重；目录本身作为一个输入，不展开其子项。
5. `RequestedCount` 统计所有非空、非元数据行，`MatchedBytes` 只累计已匹配文件的大小，目录本身不递归计算大小。

例如，下面第二行不会被解释成密码，而会被报告为缺失路径：

```text
/data/source.txt
this is not a password
```

### 2.4 CLI 来源

无界面模式与 GUI 共用 `BatchOperationService`：

| 参数 | 目录输入 | 文件输入 |
| --- | --- | --- |
| `--source PATH` | 扫描该目录直接子项 | 将该文件作为一个精确输入 |
| `--input PATH` | 将该目录本身作为一个精确输入 | 将该文件作为一个精确输入 |
| 多次 `--input` | 每项均加入，最后按路径去重 | 每项均加入，最后按路径去重 |

因此，下面两条命令的归档数量不同：

```bash
# 目录中的每个直接子项分别建立归档
BatchCompress.Avalonia compress --source ./data --output ./out --format 7z

# 把 data 目录整体建立为一个归档
BatchCompress.Avalonia compress --input ./data --output ./out --format 7z
```

`--dry-run` 只执行来源收集和打印，不创建输出目录、归档或附件暂存目录。

## 3. 单个任务的执行顺序

`BatchCompressAsync` 对每个来源按下面的顺序处理。

### 3.1 取消、元数据和存在性检查

每轮开始先检查 `CancellationToken`。如果已取消，直接抛出 `OperationCanceledException`，当前进程由 GUI/CLI 处理取消，不再继续下一个任务。

随后再次过滤系统元数据，并检查来源仍然是文件或目录。来源可能在刷新列表后被删除；此时：

- 不调用归档引擎；
- `NonExistCount` 加一；
- 写入失败进度和日志；
- 继续处理下一个来源。

### 3.2 已处理项目

如果 `SkipAlreadyProcessed=true` 且来源的最后一级名称包含 `【已压缩】`，该项目被忽略，`IgnoreCount` 加一。目录扫描阶段还会提前过滤包含 `【已解压】` 的名称；手工列表在执行阶段主要检查 `【已压缩】`。

### 3.3 输出目录和文件名

输出目录由 `OutputPathResolver.ResolveAndCreate` 计算：

- `OutputPath` 非空：转换为绝对路径并创建目录。
- `OutputPath` 为空：回退到来源文件或目录的父目录。

输出文件名永远使用来源项目的最后一级名称，再追加一个格式后缀：

```text
sourcePath = /data/report.txt,  format = rar  -> report.txt.rar
sourcePath = /data/project,     format = 7z   -> project.7z
sourcePath = /data/photo.png,   format = zip  -> photo.png.zip
```

注意：不会自动删除输入文件原扩展名，因此 `report.txt` 的输出是 `report.txt.rar`，不是 `report.rar`。开启分卷后，`outputPath` 仍先以这个基础名称传给引擎，实际卷文件名由 RAR/7zz 生成。

### 3.4 已有输出文件

服务先检查基础输出路径（例如 `project.7z`）是否已经存在：

| `ExistingFileMode` | 服务行为 |
| --- | --- |
| `Skip` | 不启动引擎，`IgnoreCount` 加一，记录“已存在，跳过” |
| `Update` | 保留已有文件并继续调用引擎；RAR 额外使用 `-u`，7zz 使用其普通 `a` 命令行为 |
| `Overwrite` | 先删除基础输出文件，再调用引擎 |

这里的预检查只针对基础输出路径，不会预先枚举所有分卷文件。覆盖删除失败会由异常流程报告，不能继续假定归档已被覆盖。

### 3.5 密码选择和确定性密码

密码优先级是：

1. `UseRandomPassword=true` 时，按当前输出归档名派生密码。
2. 随机密码关闭且 `CustomPassword` 非空时，使用自定义密码。
3. 两者都没有时创建无密码归档。

压缩阶段传入的是基础输出文件名，例如 `report.txt.rar`；分卷尚未生成，所以不会把 `.part001` 或 `.001` 纳入计算。

`PasswordNameMode` 有两种：

| 模式 | 计算依据（以 `report.txt.rar` 为例） |
| --- | --- |
| `ArchiveName`（默认） | `report.txt.rar` |
| `BaseName` | 去掉最后扩展名后的 `report.txt` |

算法保持旧版兼容：

```text
h1 = MD5_UTF8(name + "592ptt1314") 的十六进制字符串，从索引 7 截取 8 个字符
h2 = MD5_UTF8(name + "592pnn1314") 的十六进制字符串，从索引 7 截取 8 个字符
password = h1 + h2
```

例如 `name = report.txt.rar` 时，当前算法得到：

```text
MD5(name + 592ptt1314) = 1a34a31a8f27de3c671d0dc3dd9de87f
MD5(name + 592pnn1314) = 95c1c2ff047a594870987bdd1ce0f503
password = a8f27de3f047a594
```

该算法是历史兼容的确定性派生，不应当被当作通用密码学密钥派生方案。GUI 的“密码查询”使用同一个命名规则和算法。

### 3.6 `ArchiveOptions` 映射

批处理选项在每个任务开始时快照一次，转换为引擎契约：

| 批处理选项 | 传给引擎的值 |
| --- | --- |
| `Extension` | `ArchiveFormat`，规范化为 `rar`、`7z` 或 `zip` |
| `CompressionLevel` | `Store`、`Fastest`、`Fast`、`Normal`、`Good`、`Best` |
| `SolidArchive` | 固实压缩开关；级别为 0 时 GUI 会自动关闭 |
| `VolumeSize` + `VolumeSizeUnit` | 例如 `20` + `g` 变为 `20g`，传给 `-v` |
| `TempDirectory` | 用户配置值；为空时使用输出目录 |
| `CommentFile` | 文件存在时传入；不存在时置空 |
| `RecoveryRecordPercent` | RAR 恢复记录百分比 |
| `QuickOpen` | RAR 快速打开信息 |
| `LockArchive` | RAR 锁定归档 |
| `TestArchive` | RAR 使用创建命令测试；7zz 创建成功后再执行 `t` |
| `AdditionalInputs` | 附件目录/文件的额外根级输入 |

CLI 会在解析阶段拒绝无效格式、分卷数值、已有文件策略和“更新 + 锁定”冲突；GUI 在开始压缩前也会拒绝“更新现有文件 + 锁定归档”。

## 4. 附件目录

`AddEnclosures` 开启且存在附件列表时，附件会对**每一个来源任务**追加为额外根级输入：

```text
主输入：/data/project
附件：  /share/contact.txt
       /share/license

归档内的顶层项目：
project/       （主输入目录本身的内容）
contact.txt    （附件 basename）
license/       （附件目录 basename）
```

实现细节：

- 已存在的文件或目录转成绝对路径，直接交给 RAR/7zz。
- 不存在的附件不会写入来源目录；程序只在系统临时目录创建一个以附件 basename 命名的空目录，加入当前任务，命令结束后递归清理。
- 空行、系统元数据路径和重复路径被忽略，去重不区分大小写。
- 暂存目录清理失败只写警告，不改变归档引擎已经返回的结果。

因此，附件列表中的“联系信息目录”即使当前不存在，也不会因为准备附件而修改用户源目录。

## 5. 格式路由与实际命令

### 5.1 路由规则

`ArchiveEngineRouter.CompressAsync` 先去除前导点并转小写：

```text
rar       -> RarArchiveEngine
7z / .7Z -> SevenZipArchiveEngine
zip       -> SevenZipArchiveEngine
其它      -> NotSupportedException
```

此规则只用于创建。解压时由实际文件类型和 `ArchiveVolumeResolver` 判定，不能把解压路由规则倒推到压缩。

### 5.2 RAR 参数

`WinRarCommandBuilder` 生成参数的结构如下（每一项都是独立参数）：

```text
rar a -ep1 -IBCK -SCf
    [已有文件策略]
    [密码]
    -m0..-m5
    [固实 -s -md32]
    [锁定 -k]
    [分卷 -v<size>]
    [恢复记录 -rr<percent>]
    [快速打开 -qo+]
    [测试 -t]
    [注释 -z<comment-file>]
    [临时目录 -w<temp-directory>]
    [仅存储清单 -ms<ext;ext;...>]
    -oi:50000000
    <output>
    <input>
    [additional inputs...]
```

RAR 后端的已有文件参数为：`Skip -> -o-`、`Update -> -u`、`Overwrite -> -o+`。密码使用 `-p<password>`，不会在应用日志里主动替换为 `***`。

RAR 默认对已经压缩或编码的内容使用“仅存储”策略（`-ms`），这不是排除规则，文件仍然进入归档。当前清单包括：

```text
7z ace arj bz2 cab gz lha lz lzh rar taz tgz xz z zip zipx
aac avi flac flv m4a mkv mov mp3 mp4 ogg opus rm rmvb webm
avif gif heic jpeg jpg png webp
docx odg odp ods odt pptx xlsx
```

### 5.3 7z/ZIP 参数

`SevenZipCommandBuilder` 的创建命令结构如下：

```text
7zz a -t7z|-tzip -mx=0|1|3|5|7|9
    [7z 固实 -ms=on|-ms=off]
    [密码 -p<password>]
    [7z 文件名加密 -mhe=on]
    [ZIP 密码加密 -mem=AES256]
    [分卷 -v<size>]
    [临时目录 -w<temp-directory>]
    <output>
    <input>
    [additional inputs...]
```

压缩级别映射为：

| UI/CLI 级别 | RAR | 7zz `-mx` |
| --- | --- | --- |
| 0 存储 | `-m0` | `-mx=0` |
| 1 最快 | `-m1` | `-mx=1` |
| 2 快速 | `-m2` | `-mx=3` |
| 3 标准 | `-m3` | `-mx=5` |
| 4 较好 | `-m4` | `-mx=7` |
| 5 最佳 | `-m5` | `-mx=9` |

RAR 的恢复记录、快速打开、注释和锁定参数不会错误地传给 7zz；RAR 的 `-ms` 清单也不会被转换成 7z/ZIP 的排除项。

密码行为有一个格式差异：7z 会同时加入 `-mhe=on` 以加密文件名；ZIP 使用 `-mem=AES256`，但不使用 7z 的文件名加密开关。

### 5.4 创建后校验

- RAR：在创建命令中加入 `-t`，由 RAR 在同一进程中完成测试。
- 7z/ZIP：先执行 `7zz a ...`，成功后再执行 `7zz t [-p<password>] <archive>`；两次进程的 stdout/stderr 合并返回。
- RAR 和 7zz 返回码 `0`、`1` 都被视为成功，`1` 表示带警告但可用；其它返回码计为归档失败。

## 6. 成功后的后处理

只有 `ArchiveResult.Success=true` 时才进入后处理：

| 选项 | 行为 |
| --- | --- |
| 无 | 保留源文件/目录 |
| `DeleteSourceAfter=true` | 删除源文件；源为目录时递归删除 |
| `MoveSourceAfter=true` | 移动到来源父目录下的 `【已压缩】/` |

如果删除和移动同时为真，当前代码优先删除（`if ... else if ...`）。移动永远不覆盖目标：

```text
/data/project       -> /data/【已压缩】/project
/data/report.txt    -> /data/【已压缩】/report.txt
```

目标已存在、权限不足或移动/删除异常时：

- 已创建的归档保留；
- 源文件保留（移动冲突时明确保留双方）；
- `PostProcessFailCount` 加一并写入警告日志；
- 最终状态显示为“成功但后处理失败”。

后处理只处理主来源，不会删除附件文件，也不会删除附件暂存目录之外的用户文件。

## 7. 大小限制、进度和关机

### 7.1 大小上限

`MaxSizeGB` 是“成功输出归档大小”的累计上限，不是源数据大小：

1. 任务成功后检查基础 `outputPath` 是否存在。
2. 存在时用该文件大小除以 `1024^3` 累加到 `ProcessedSizeGB`。
3. 本任务结束后，如果 `MaxSizeGB > 0` 且累计值达到上限，停止后续任务。
4. `MaxSizeGB == 0` 表示不限。

分卷场景下，当前累计检查直接针对基础 `outputPath`；如果具体引擎把首卷命名为其它分卷文件名而基础路径不存在，本次输出大小不会被这段代码累计。这是当前实现边界，不应把它理解为“按全部分卷总和统计”。

### 7.2 进度统计

GUI 和 CLI 共用 `OperationProgressInfo`：

| 字段 | 变化时机 |
| --- | --- |
| `SuccessCount` | 引擎成功，不论后处理是否失败 |
| `FailCount` | 引擎返回失败或创建异常 |
| `PostProcessFailCount` | 主来源删除或移动失败，以及移动目标冲突；附件暂存目录清理失败只写 warning，不增加此计数 |
| `IgnoreCount` | 已处理、已有输出跳过、重复任务等 |
| `NonExistCount` | 来源在执行时不存在 |
| `ProcessedSizeGB` | 成功且基础输出文件存在时累计 |

归档进程的 stdout/stderr 会以 `[压缩命令]` 消息送入 GUI 命令日志，也写入 CLI 日志；当前实现保留原始输出，不主动脱敏。因此使用命令行参数传入的密码可能出现在归档工具原始输出中，生产环境应按敏感日志处理。

### 7.3 完成后关机

所有任务正常遍历结束后，如果 `ShutdownAfterComplete=true`，调用系统集成请求关机。归档失败不会阻止到达该步骤；取消抛出异常时不会继续执行正常完成路径。

## 8. 完整示例

### 示例 A：目录直接子项分别压缩为 7z

目录：

```text
/work/
├── a.txt
├── photo.png
├── docs/
└── old.rar
```

选择来源目录、格式 `7z`、输出 `/archives` 后，任务和路由为：

```text
a.txt   -> /archives/a.txt.7z   -> 7zz a -t7z ...
photo.png -> /archives/photo.png.7z -> 7zz a -t7z ...
docs    -> /archives/docs.7z    -> 7zz a -t7z ...
old.rar -> /archives/old.rar.7z -> 7zz a -t7z ...
```

`old.rar` 不会因为自身是 RAR 就改走 RAR 后端；输出格式决定路由。

### 示例 B：TXT 每行一个路径

`compress.txt`：

```text
./a.txt
./docs
/tmp/photo.png
missing.bin
```

TXT 位于 `/work/compress.txt` 时，前三行按 `/work` 解析相对路径，`missing.bin` 进入缺失诊断。若前三项存在并选择 `rar`，每项分别生成 `a.txt.rar`、`docs.rar`、`photo.png.rar`。

### 示例 C：确定性密码

源文件为 `/data/report.txt`，选择 `rar`，随机密码开启，密码依据为完整归档名：

```text
输出名       = report.txt.rar
密码依据     = report.txt.rar
生成密码     = a8f27de3f047a594
归档后端     = RAR
RAR 参数     = ... -pa8f27de3f047a594 ... report.txt.rar report.txt
```

实际参数列表中的 `-p...` 是一个参数元素；上面的空格只是说明格式，不代表会拆成多个 Shell 参数。

### 示例 D：7z 分卷、校验和附件

```bash
BatchCompress.Avalonia compress \
  --input ./project \
  --output ./archives \
  --format 7z \
  --volume-size 2 --volume-unit g \
  --enclosure ./contact.txt \
  --test \
  --password-file ./password.txt
```

执行顺序是：生成 `project.7z` 基础路径和 `-v2g` 参数 -> 追加 `contact.txt` 根级输入 -> 7zz 创建分卷 -> 再执行 `7zz t` -> 成功后按 `--move-source`/`--delete-source`（若指定）处理 `project`。密码文件只读取第一行，显式密码源会关闭随机密码派生。

当前实现有一个明确边界：7z/ZIP 的 `--test` 是在创建成功后用基础输出路径再次调用 `7zz t`。启用分卷时实际文件通常是 `project.7z.001` 或 `project.zip.001`，基础 `project.7z`/`project.zip` 不存在，因此第二阶段会失败，整个任务计为归档失败；关闭 `--test` 才能正常使用当前 7z/ZIP 分卷创建流程。RAR 分卷的 `-t` 由 RAR 创建命令直接完成，不受此问题影响。

### 示例 E：已有输出和移动冲突

```text
/data/source/              来源目录
/out/source.rar             已存在
/data/【已压缩】/source/      也已存在
```

- `ExistingFileMode=Skip`：不调用 RAR，计为忽略。
- `ExistingFileMode=Overwrite`：先删除 `/out/source.rar`，重新创建。
- 重新创建成功且选择移动：发现 `/data/【已压缩】/source/` 已存在，不覆盖；归档计成功，后处理计失败，源目录仍保留。

## 9. 常见边界和排查

| 现象 | 当前原因 |
| --- | --- |
| 选择 7z 后 `.rar` 输入仍被压缩 | 压缩扫描不按输入扩展名过滤，这是预期行为 |
| `report.txt` 生成 `report.txt.rar` | 输出名是“来源 basename + 输出扩展名” |
| 选择解压密码本后点击压缩无动作 | GUI `SourceMode=0` 明确禁止密码本模式压缩 |
| TXT 中第二行密码没有生效 | 压缩 TXT 每行都是路径；密码交替格式只属于解压密码本 |
| 输出已有但仍看到跳过 | 已有输出策略为 `Skip`，服务在启动引擎前就结束该项 |
| RAR 恢复记录在 ZIP 中没有效果 | `-rr` 只由 RAR builder 生成，7zz 不接收该参数 |
| “附件路径不存在”却没有写入来源目录 | 程序在系统临时目录创建空目录，不修改来源 |
| 归档成功但最终显示失败 | 后处理移动/删除目标冲突或权限错误，查看 `PostProcessFailCount` |
| 7z/ZIP 校验看起来执行了两次 | 创建后校验由第二次 `7zz t` 进程完成 |
| 输出日志里出现密码 | 归档工具原始 stdout/stderr 按当前设计原样记录，日志应视为敏感信息 |
| 不支持 `tar`、`gz` 等创建格式 | 当前路由只允许创建 RAR、7z、ZIP；这些格式仅可能由 7zz 解压 |

## 10. 相关实现位置

| 文件 | 当前职责 |
| --- | --- |
| [`Core/Services/BatchOperationService.cs`](Core/Services/BatchOperationService.cs) | 来源扫描、TXT 路径导入、逐项压缩、密码、附件、后处理、进度和大小上限 |
| [`Core/Services/ArchiveEngineRouter.cs`](Core/Services/ArchiveEngineRouter.cs) | `rar -> RAR`、`7z/zip -> 7zz` 的创建路由 |
| [`Core/Services/WinRarCommandBuilder.cs`](Core/Services/WinRarCommandBuilder.cs) | RAR 创建参数、`-ms`、恢复记录、锁定、注释和测试参数 |
| [`Core/Services/SevenZipCommandBuilder.cs`](Core/Services/SevenZipCommandBuilder.cs) | 7z/ZIP 创建参数、固实、AES256、分卷和校验参数 |
| [`Core/Services/PasswordUtility.cs`](Core/Services/PasswordUtility.cs) | 默认密码命名规则和 MD5 兼容算法 |
| [`Core/Services/OutputPathResolver.cs`](Core/Services/OutputPathResolver.cs) | 输出目录回退和创建 |
| [`Core/Models/ArchiveDefaults.cs`](Core/Models/ArchiveDefaults.cs) | RAR 默认仅存储扩展名清单 |
| [`HeadlessBatchRunner.cs`](HeadlessBatchRunner.cs) | CLI 来源收集、选项转换、dry-run、退出码和日志 |
| [`ViewModels/MainWindowViewModel.cs`](ViewModels/MainWindowViewModel.cs) | GUI 来源模式、选项快照、进度显示和压缩命令入口 |

验证当前实现时，优先运行：

```bash
dotnet build BatchCompress.Avalonia.sln --nologo
dotnet run --project BatchCompress.Avalonia.Tests/BatchCompress.Avalonia.Tests.csproj --nologo
```
