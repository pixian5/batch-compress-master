移到这里

## 问题分析
用户希望将"完成后关机"复选框移动到与"处理后删除源"和"处理后移动源"同一行，使界面布局更加紧凑和合理。

## 修复过程
1. **定位代码**：在`/Users/x/code/trae/compress/Views/MainWindow.axaml`文件中找到后处理选项部分的代码

2. **分析布局**：原来的布局是：
   - "处理后删除源"和"处理后移动源"在一个水平StackPanel中
   - "完成后关机"在单独的另一个水平StackPanel中

3. **实施修复**：将"完成后关机"复选框移动到第一个水平StackPanel中，与其他两个复选框放在同一行

## 修复代码
修改前：
```xaml
<StackPanel Orientation="Horizontal" Spacing="15">
    <CheckBox Content="处理后删除源" IsChecked="{Binding DeleteSourceAfter}"/>
    <CheckBox Content="处理后移动源" IsChecked="{Binding MoveSourceAfter}"/>
</StackPanel>

<StackPanel Orientation="Horizontal" Spacing="15">
    <CheckBox Content="完成后关机" IsChecked="{Binding ShutdownAfterComplete}"/>
</StackPanel>
```

修改后：
```xaml
<StackPanel Orientation="Horizontal" Spacing="15">
    <CheckBox Content="处理后删除源" IsChecked="{Binding DeleteSourceAfter}"/>
    <CheckBox Content="处理后移动源" IsChecked="{Binding MoveSourceAfter}"/>
    <CheckBox Content="完成后关机" IsChecked="{Binding ShutdownAfterComplete}"/>
</StackPanel>
```

## 验证结果
- 程序运行正常，无编译错误
- "完成后关机"复选框已成功移动到与其他两个复选框同一行
- 界面布局更加紧凑和合理

## 总结
成功将"完成后关机"复选框移动到了与"处理后删除源"和"处理后移动源"同一行，优化了界面布局，使后处理选项部分更加紧凑和直观。