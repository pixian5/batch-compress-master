# 用户问题：下拉列表选择【从txt读取要压缩的文件】后，点击选择来源按钮应该选择txt文件

## 修改内容

### 1. 功能实现
- 实现了根据来源模式（SourceMode）自动切换文件选择类型的功能
- 当选择"从txt读取要解压的文件"（SourceMode=0）时，点击"选择来源"按钮会打开文件选择对话框，仅显示txt文件
- 当选择"压缩此文件夹内所有文件"（SourceMode=1）时，点击"选择来源"按钮会打开文件夹选择对话框

### 2. 验证结果
- 程序成功运行，无编译错误
- 根据不同的来源模式，选择来源按钮会打开相应的选择对话框
- 选择txt文件模式时，文件选择对话框正确显示txt文件过滤器
- 选择文件夹模式时，文件夹选择对话框正常工作
- 所有功能保持正常，界面响应正确

## 代码修改点

### MainWindow.axaml.cs
```csharp
private async Task BrowseSourceAsync()
{
    if (DataContext is not MainWindowViewModel viewModel)
        return;
    
    if (viewModel.SourceMode == 0) // 从txt读取要解压的文件
    {
        var options = new FilePickerOpenOptions
        {
            Title = "选择TXT文件",
            AllowMultiple = false,
            FileTypeFilter = new[]
            {
                new FilePickerFileType("文本文件") { Patterns = new[] { "*.txt" } },
                new FilePickerFileType("所有文件") { Patterns = new[] { "*.*" } }
            }
        };
        
        var files = await StorageProvider.OpenFilePickerAsync(options);
        if (files.Count > 0)
        {
            viewModel.SourcePath = files[0].Path.LocalPath;
        }
    }
    else // 压缩此文件夹内所有文件
    {
        var options = new FolderPickerOpenOptions
        {
            Title = "选择来源文件夹",
            AllowMultiple = false
        };
        
        var folders = await StorageProvider.OpenFolderPickerAsync(options);
        if (folders.Count > 0)
        {
            viewModel.SourcePath = folders[0].Path.LocalPath;
        }
    }
}
```

## 测试情况
- 程序启动正常
- 选择"从txt读取要解压的文件"模式，点击"选择来源"按钮打开文件选择对话框，显示txt文件
- 选择"压缩此文件夹内所有文件"模式，点击"选择来源"按钮打开文件夹选择对话框
- 选择文件/文件夹后，来源路径文本框正确显示所选路径
- 所有其他功能保持正常工作

## 总结
本次修改成功实现了根据来源模式自动切换文件选择类型的功能，提升了用户体验，使操作更加符合直觉。当用户选择从txt读取文件时，点击"选择来源"按钮会直接打开文件选择对话框并筛选txt文件，避免了用户误操作选择文件夹的情况。