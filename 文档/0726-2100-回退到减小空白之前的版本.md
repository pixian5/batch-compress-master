用户要求回退到减小空白之前的版本

## 执行的操作
1. 停止当前运行的程序（dotnet run）
2. 查看git提交历史，确定需要回退到ec8386e版本（将附件列表文本框高度从150调整为100）
3. 执行git reset --hard ec8386e命令回退版本
4. 重新构建项目（dotnet build），构建成功
5. 重新运行程序（dotnet run），程序已正常启动

## 当前状态
- 项目已回退到减小UI空白之前的版本
- 程序正在运行中，UI显示为原始状态
- 本地git HEAD已指向正确的提交