删除掉前缀。

## 问题分析
用户要求删除成功记录中的"Success: "前缀，使文本显示更加简洁明了。

## 修复过程
1. **定位代码**：在`/Users/x/code/trae/compress/Core/Services/BatchOperationService.cs`文件中找到生成成功消息的代码

2. **分析内容**：程序在两个地方生成成功消息：
   - 第366行：`progressInfo.Message = $"Success: {name}";`
   - 第515行：`progressInfo.Message = $"Success: {archiveName}";`

3. **实施修复**：
   - 将第366行修改为：`progressInfo.Message = name;`
   - 将第515行修改为：`progressInfo.Message = archiveName;`

## 修复代码
修改前：
```csharp
// 第366行
progressInfo.Message = $"Success: {name}";

// 第515行
progressInfo.Message = $"Success: {archiveName}";
```

修改后：
```csharp
// 第366行
progressInfo.Message = name;

// 第515行
progressInfo.Message = archiveName;
```

## 验证结果
- 程序运行正常，无编译错误
- 成功记录中不再显示"Success: "前缀，只显示文件名
- 其他功能保持正常

## 总结
成功删除了成功记录中的"Success: "前缀，使显示的文本内容更加简洁明了，提升了用户体验。