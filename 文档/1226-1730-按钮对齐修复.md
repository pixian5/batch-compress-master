# 问题：和下方按钮对齐。

## 修复内容
我已经将所有按钮的宽度统一调整为80像素，与下方的操作按钮（压缩、解压等）保持一致对齐：

1. **来源与目标部分**：
   - "选择目录"按钮（3个）：宽度从100调整为80
   - "选txt"按钮：宽度从100调整为80

2. **密码查询部分**：
   - "查询密码"按钮：宽度从100调整为80
   - "确认密码/解锁"按钮：宽度从120调整为80

3. **下方操作按钮**：保持原有80宽度不变

## 技术细节
修改了`/Views/MainWindow.axaml`文件中的5个按钮宽度属性，确保整个界面的按钮视觉一致性。

## 测试结果
- 项目构建成功：`dotnet build` 返回0错误
- 程序运行正常：`dotnet run` 成功启动GUI应用

所有按钮现在都与下方的主要操作按钮保持统一的宽度和对齐方式，提升了界面的视觉协调性。